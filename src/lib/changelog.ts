// Workspace changelog — every version, newest first.
// When bumping the footer `workspace.v#`, prepend a new entry at the TOP of
// the CHANGELOG array (the page renders in order, so position 0 = latest).

export type ChangelogEntry = {
  /** Number or "68.1" string. While polishing SEO Audit we use minor
   *  versions (68.1, 68.2, …) instead of bumping the major. */
  version: number | string;
  date: string; // ISO YYYY-MM-DD
  title: string;
  highlights: string[];
};

export const CHANGELOG: ChangelogEntry[] = [
  {
    version: "76.6",
    date: "2026-07-22",
    title: "Relatório Mensal — vitórias automáticas no Slack #client-wins",
    highlights: [
      "**🏆 Cada relatório novo festeja-se no Slack.** Quando geras pela **primeira vez** o relatório mensal de um cliente, a app publica automaticamente no canal **#client-wins** o **resumo executivo** desse relatório (os principais destaques positivos do mês — leads, orgânico, clicks, posições, IA) e faz **@channel** para toda a gente ver. Inclui o cliente, o mês, o consultor e um link direto para abrir o relatório.",
      "**🔂 Sem spam.** Só dispara na **primeira geração** de cada cliente+mês — regenerar ou editar o rascunho a seguir **não** volta a tocar o canal. Uma mensagem por relatório.",
      "**🔌 Fica inerte até ligares o webhook.** Requer definir `SLACK_CLIENT_WINS_WEBHOOK_URL` (Incoming Webhook do Slack apontado ao #client-wins) nas Environment Variables da Vercel. Sem essa variável, não acontece nada — nada parte e nada é enviado.",
    ],
  },
  {
    version: "76.5",
    date: "2026-07-22",
    title: "Add Call Notes — quando o link Fathom não é da conta, cai já no modo colar",
    highlights: [
      "**🔗 Fim do beco sem saída no Fathom.** Se colares um link Fathom que foi gravado numa conta pessoal (ou é de uma chamada antiga), a app deixa de mostrar só o erro técnico: abre automaticamente o campo de **colar o AI Summary** e explica em português-claro que basta colar o texto, ou partilhar a gravação com a equipa Wonder Ads no Fathom e tentar de novo.",
    ],
  },
  {
    version: "76.4",
    date: "2026-07-22",
    title: "Relatório Mensal — resumo só positivo, AI Visibility melhor, keywords overview",
    highlights: [
      "**✅ Resumo executivo só com pontos positivos.** Passa a mostrar no mínimo 3 destaques positivos do mês (leads, orgânico, clicks, posições, IA) e **nunca menciona descidas**.",
      "**🤖 AI Visibility renovado.** Explica como os dados são obtidos (sessões por origem de referral no GA4) e mostra por fonte de IA as **sessões + utilizadores + engagement** em cards, com total.",
      "**🔑 Keywords & Posições (fim do mês).** Nova secção no fim do relatório via GSC: nº de keywords com impressões, quantas em **Top 3 / Top 10**, **posição média** e as **maiores subidas de posição** do mês.",
      "**🩺 Diagnóstico + guias.** Quando o GBP ainda não liga, o relatório explica porquê (aprovação da API pendente / ficha por associar). Quando faltam eventos de lead, sugere como automatizá-los via **Google Tag Manager**.",
    ],
  },
  {
    version: "76.3",
    date: "2026-07-22",
    title: "SEO — logótipos reais nos cartões Spine Center, MyMedic e Cidália",
    highlights: [
      "**🖼️ Logótipos das marcas.** Os cartões (e páginas de projeto) do **Spine Center**, **MyMedic (Clínicas MyMedic)** e **Cidália Cabeleireiros** deixam de mostrar o emoji sobre o gradiente e passam a apresentar o logótipo real de cada marca, puxado do site respetivo, sobre chip branco — igual aos restantes clientes SEO.",
    ],
  },
  {
    version: "76.2",
    date: "2026-07-22",
    title: "Relatório Mensal — histórico com Abrir + PDF por relatório",
    highlights: [
      "**🗂️ Relatórios anteriores.** No hub de relatórios do cliente, cada geração passada aparece com o estado + data e botões **Abrir** (ver/inspecionar) e **PDF** (exportar) — dá para ter o overview de todos e sacar o PDF de qualquer mês diretamente da lista.",
    ],
  },
  {
    version: "76.1",
    date: "2026-07-22",
    title: "Relatório Mensal SEO — integração Google Business Profile",
    highlights: [
      "**📍 GBP automático.** O relatório passa a puxar os cliques do **Google Business Profile** (cliques p/ website, pedidos de direções, cliques p/ ligar) via a Business Profile Performance API, com o mesmo service account do GA4/GSC. Enquanto a API não estiver ligada/aprovada, mantém-se o preenchimento manual (nunca um 0 falso). Requer setup do lado do Google (ver instruções).",
    ],
  },
  {
    version: 76,
    date: "2026-07-22",
    title: "Relatório Mensal SEO — input manual, PDF, link do cliente e aprovação",
    highlights: [
      "**✍️ Preencher dados em falta.** Na página do relatório dá para introduzir manualmente as métricas que a app não puxa (GBP + eventos não instrumentados) ou marcá-las **N/A**. O total de leads, o resumo e o estado (rascunho→pronto) recalculam-se automaticamente.",
      "**📄 PDF branded + 🔗 link read-only.** Botão **Gerar PDF** (`Wonderads_SEO-Report_{Cliente}_{AAAA-MM}.pdf`) e **Copiar link do cliente** (página pública, só leitura, sem chrome). **Enviar para aprovação** cria a linha na Pending Review (categoria *Monthly Report*) apontando ao link do cliente.",
      "**🦋 Logótipo corrigido** no topo do relatório (estava em branco) e o relatório passa a mencionar o **consultor SEO do projeto** (nome + email).",
      "**🔀 Ação *Monthly Report* reformulada.** O cartão passa a abrir o novo relatório (dados reais + PDF) em vez do texto antigo; a página da ação redireciona para lá.",
    ],
  },
  {
    version: "75.9",
    date: "2026-07-21",
    title: "Relatório Mensal SEO — interface (Fase 4): gerar + ver relatório",
    highlights: [
      "**📊 Relatório Mensal na página do cliente.** Nova secção *Relatório Mensal* (fundo da página do cliente SEO) com botão **Gerar** e lista de relatórios anteriores. Abre a página do relatório com o **documento branded Wonder Ads** (8 secções: Resumo, Leads, GBP, Orgânico, AI Visibility, Top Queries/Páginas, Notas), tira interna de proveniência (GA4/GSC/GBP), e aviso de *rascunho*. A regra de ouro está visível: métricas por validar aparecem como *não instrumentado* / *a preencher* — nunca um 0 falso. Dados reais entram assim que o service-account correr no deploy.",
    ],
  },
  {
    version: "75.8",
    date: "2026-07-21",
    title: "Relatório Mensal SEO — camada GSC + montagem do relatório",
    highlights: [
      "**🧩 Montagem completa dos dados do relatório.** Camada GSC por mês de calendário (totais + top-10 queries/páginas), store de snapshots por cliente+mês, e o agregador que junta GA4+GSC nas 8 secções fixas, calcula o total de leads consolidado, gera o *Executive Summary* por regras e aplica a regra de ouro (métrica em falta = *pendente/manual*, nunca um 0 falso; relatório fica *draft* até estar validado). Falta a interface (página + PDF + preview), que vem já a seguir.",
    ],
  },
  {
    version: "75.7",
    date: "2026-07-21",
    title: "Relatório Mensal SEO — fundações (Fase 0 + camada de dados GA4)",
    highlights: [
      "**🧱 Arranque do módulo de Relatório Mensal de SEO & Lead Gen.** Fundações: janelas por **mês de calendário** (mês anterior completo, MoM, YoY) com respeito pelo lag do GSC, config de analytics por cliente (property GA4 / site GSC / location GBP / timezone / mapa de eventos / regex de LLMs), e a camada de dados GA4 do relatório — leads por evento nomeado (com deteção de instrumentação), tráfego orgânico e AI Visibility. Sem alterações visíveis ainda; a montagem do relatório e o PDF vêm nas próximas fases.",
    ],
  },
  {
    version: "75.6",
    date: "2026-07-21",
    title: "Acessos — João B. entra, Yenisey sai do login",
    highlights: [
      "**🔑 Login do João B.** Novo acesso de consultor SEO (`joao-b`) — vê o SEO DPT (a sua coluna) e o Web, como os restantes consultores. Password entregue à parte.",
      "**🚪 Yenisey removida do login.** A conta `yenisey-r` já não consegue entrar no workspace (continua, para já, como opção no inquérito NPS).",
    ],
  },
  {
    version: "75.5",
    date: "2026-07-21",
    title: "Onboarding — guardar progresso nos formulários (auto-save 60s + botão)",
    highlights: [
      "**💾 Já não se perde meio formulário.** Os formulários de onboarding passam a **guardar o progresso automaticamente a cada 60s** e em cada mudança de passo, mais um botão **“Guardar progresso”** manual. Quem preenche a meio e volta no dia seguinte (mesmo noutro dispositivo) **retoma exatamente de onde parou** — respostas + passo restaurados, com um aviso *“Retomámos as suas respostas guardadas”*.",
      "**🔒 Sem efeitos secundários.** O rascunho é guardado num store KV próprio (`onboarding-draft:<slug>:<track>`), separado do envio final — não gera PDF nem promove o cliente. Ao submeter o formulário, o rascunho é apagado para não ressurgir.",
    ],
  },
  {
    version: "75.4",
    date: "2026-07-21",
    title: "SEO DPT — reorganização de consultores + coluna João B.",
    highlights: [
      "**🔀 Carteira da Yenisey redistribuída** (coluna removida): **IHN → Manuel Silva**; **White Clinic + Spine Center + CDT → André Pereira**; **Monte Mar + Fisio Restelo → Fran. Rosa**. Os emails nos entregáveis/detalhes seguem automaticamente o novo consultor (via `getConsultantEmailForSlug`).",
      "**🆕 Nova coluna João B.** (`joao.batista@wonder-ads.com`) com **Cidália Cabeleireiros** e **MyMedic** — ambos ainda em fase de onboarding (SuperAdmin a preencher os forms). Aparecem já na coluna; quando submeterem o formulário de onboarding o mesmo slug promove sem duplicar e os dados/PDF passam direto para a página do cliente em `/seo/<cliente>`.",
    ],
  },
  {
    version: "75.3",
    date: "2026-07-21",
    title: "Onboarding — vídeo de boas-vindas correto",
    highlights: [
      "**🎥 Vídeo de boas-vindas.** Atualizado o vídeo do 1º passo (*Começar Aqui*) para o correto (`AJyxJmV0pIc`).",
    ],
  },
  {
    version: "75.2",
    date: "2026-07-21",
    title: "Onboarding — vídeo de boas-vindas, passo final novo e ajustes",
    highlights: [
      "**🎥 Vídeo de boas-vindas.** Embed do vídeo de introdução no 1º passo (*Começar Aqui*) e novo vídeo no passo de **Google Analytics 4** do SEO.",
      "**🏁 Passo final refeito.** Removido o passo *Sessão de Estratégia*. O último passo passa a ser **“Feito! 🎉”** — agradece, pede para agendar a próxima reunião (ou enviar disponibilidades) e para avisar no grupo de **WhatsApp** que terminou o onboarding.",
      "**⏱️ Formulário SEO ~2 min.** Ajustada a estimativa de tempo do *Formulário SEO* de ~8 para ~2 min (novo override `estMinutes` por passo).",
    ],
  },
  {
    version: "75.1",
    date: "2026-07-21",
    title: "Onboarding — vídeos nos passos de acessos SEO",
    highlights: [
      "**🎥 Vídeos de acesso.** Embed dos vídeos de *como dar acesso* nos passos de consultoria SEO: **Google Analytics 4**, **Google Search Console** e **Google My Business (GMB)**.",
    ],
  },
  {
    version: 75,
    date: "2026-07-21",
    title: "Onboarding — Último Passo só com a Sessão de Estratégia",
    highlights: [
      "**🎯 Último Passo.** Removido o passo *“Feito!”* final. O último passo passa a ter apenas a **Sessão de Estratégia** — agendar a reunião onde apresentamos o plano inicial.",
    ],
  },
  {
    version: "74.99",
    date: "2026-07-21",
    title: "Onboarding — passo de acesso ao Website só para WordPress",
    highlights: [
      "**🌐 Acesso ao Website.** O passo passou a focar-se em **WordPress**: explica como criar um utilizador de administrador nesse caso e, se o site não for WordPress, convida o cliente a falar connosco para indicarmos a melhor forma de dar acesso. Removida a referência ao Shopify.",
    ],
  },
  {
    version: "74.98",
    date: "2026-07-20",
    title: "ADS — aceitar token direto da Meta",
    highlights: [
      "**🔑 Token direto da Meta.** Se o gestor fornecer um access token de longa duração / system user, basta pôr `META_ADS_ACCESS_TOKEN` nas env vars — já não é obrigatório criar app OAuth (META_APP_ID/SECRET) para a Meta. O Google continua a usar o botão *Ligar* para gerar o refresh token.",
    ],
  },
  {
    version: "74.97",
    date: "2026-07-17",
    title: "Onboarding — formulários alinhados com os questionários WonderAds",
    highlights: [
      "**📝 Formulário geral (Cliente).** Fundidas as perguntas do *Client Questionnaire*: *serviços/produtos mais vendidos E mais rentáveis*, novo *serviço a fazer crescer* e *regras de marca* (o que sempre dizer / nunca dizer). A secção mal-rotulada *SEO* passou a **Mercado** (área de influência + concorrência).",
      "**🎯 Formulário de Ads (lead gen).** Acrescentadas as perguntas do *Lead Gen Questionnaire*: passo-a-passo do contacto (quem/canal/rapidez), *sequência de follow-up*, *lead boa vs. fraca*, *% de conversão + ciclo de venda*, *CRM*, *custo máximo por lead (CPA-alvo)* e *materiais criativos*.",
      "**🛒 Formulário de Ads e-commerce.** Fundido o *E-commerce Questionnaire*: gama de preços + AOV, *margem e recompra*, *produto/categoria hero a crescer*. Sem duplicar o que já vinha do formulário geral/Ads.",
      "**🧹 Sem repetições.** Perguntas iguais ou muito parecidas entre formulários foram unificadas (ex.: *regras de marca* saiu do SEO para o geral) — cada cliente responde uma só vez.",
      "**➖ Overview afinado.** Removidos os chips *passos/min/autosave* do hero e a checklist *Acessos que vamos precisar* da barra lateral (da v74.96) a pedido — mantém-se a barra de progresso fixa, o cartão *A seguir* e o tempo por passo.",
    ],
  },
  {
    version: "74.96",
    date: "2026-07-17",
    title: "Onboarding — página de overview mais clara + título da aba",
    highlights: [
      "**🏷️ Título da aba corrigido.** Os links de onboarding mostravam *Pending Review · Wonder Ads* na aba/partilha (herdado do layout público). Agora cada onboarding tem o seu próprio título: *Onboarding · {Cliente} · Wonder Ads* (hub) e *{Passo} · Onboarding {Cliente} · Wonder Ads* (lição).",
      "**📊 Barra de progresso fixa.** Ao fazer scroll, aparece no topo uma barra com o **% concluído**, passos e **~min restantes** + botão *Continuar* — sempre à mão, sobretudo no telemóvel.",
      "**⏱️ Cartão A seguir + tempo por passo.** No hero, cartão **A seguir** com miniatura do próximo passo; cada lição mostra a sua estimativa de tempo.",
    ],
  },
  {
    version: "74.95",
    date: "2026-07-17",
    title: "ADS — ligar contas via OAuth dentro da app",
    highlights: [
      "**🔗 Botão \"Ligar\" (OAuth).** O gestor de Ads liga as contas dentro da app: clica *Ligar Google Ads* / *Ligar Meta*, faz login com a **conta dele** (que tem acesso às contas dos clientes) e a app guarda o token. Não é preciso o dev ter acesso às contas dos clientes.",
      "**🧱 Separação de responsabilidades.** O dev define **só as credenciais da app** nas env vars (OAuth client do Google, developer token, app da Meta). O acesso às contas é autorizado pelo gestor via frontend, uma vez, e reutilizado em todos os clientes.",
      "**🗝️ Por cliente.** Depois da agência estar ligada, em cada cliente cola-se apenas o Customer ID (Google) / Ad Account ID (Meta).",
    ],
  },
  {
    version: "74.94",
    date: "2026-07-17",
    title: "Onboarding — 3 formulários (Geral, SEO, Ads)",
    highlights: [
      "**🗂️ Três formulários.** O onboarding passa a ter (1) **Sobre a Vossa Empresa** — perguntas gerais para vos conhecermos (objetivos, marca, público, concorrência, histórico…), mostrado a todos; (2) **Formulário SEO** — só perguntas específicas de SEO (keywords, quem revê conteúdo, tom de voz, palavras, objeções); (3) **Questionário de Ads** — quando há Google/Meta Ads (com bloco de e-commerce quando aplicável).",
      "**🧩 Composição por serviço.** O formulário geral aparece sempre; o de SEO só para clientes de SEO; o de Ads só para clientes de Ads. Um cliente SEO+Ads preenche os três e tudo junta num único PDF.",
      "**🛠️ Editável.** No editor de onboarding cada passo pode ser marcado como *Geral*, *SEO* ou *Ads*.",
    ],
  },
  {
    version: "74.93",
    date: "2026-07-17",
    title: "ADS — ligar contas Google/Meta e puxar métricas reais",
    highlights: [
      "**🔌 Ligações por cliente.** Na página de cada cliente de Ads há um painel *Ligações de Plataforma* para colar o **Customer ID (Google Ads)** e o **Ad Account ID (Meta)**. Quando ligado, o dashboard puxa métricas reais (ROAS, CTR, CPA, Spend, conversões, top campanhas e série de conversões) — nada é inventado.",
      "**📊 Fetchers reais.** Implementados os fetchers de Google Ads (GAQL) e Meta (Graph insights). Se a API falhar ou não houver credenciais, o dashboard mostra *Conectar API* em vez de números falsos.",
      "**🔑 Credenciais de app.** Os tokens partilhados (developer token, OAuth do Google Ads, system-user token da Meta) ficam nas env vars da Vercel; cada cliente só precisa dos IDs de conta.",
    ],
  },
  {
    version: "74.92",
    date: "2026-07-17",
    title: "SEO — remover bloco de Onboarding da página de cliente",
    highlights: [
      "**🧹 Página de cliente de SEO mais limpa.** Removido o bloco *Onboarding do Cliente* que aparecia em todos os projetos de SEO. A gestão e o estado do onboarding vivem apenas na área de SuperAdmin (`/admin` → Onboarding).",
    ],
  },
  {
    version: "74.91",
    date: "2026-07-17",
    title: "Onboarding — eixo e-commerce (GMC + perguntas) e afinações",
    highlights: [
      "**🛒 Onboarding e-commerce.** Ao escolher Google e/ou Meta Ads, o SuperAdmin indica se é um negócio **e-commerce**. Se sim: adiciona o passo **Acesso ao Google Merchant Center** (só Google Ads) e um bloco de **perguntas de e-commerce** ao formulário de Ads (as mesmas para Google e Meta).",
      "**🧹 Passos removidos.** As lições *Introdução* (Acessos SEO) e *Introdução (Ads)* foram removidas.",
      "**🔒 Popup legal mais claro.** A confirmação destaca agora os **2 requisitos** — *Contrato assinado* e *Fatura liquidada* — sublinhados e em cartões próprios.",
      "**📣 ADS DPT.** Adicionada a **Wonder Ads** à lista de clientes do departamento de Publicidade.",
      "**🛠️ Editável.** Lições e passos podem ser marcados como *só para e-commerce* no editor de onboarding.",
    ],
  },
  {
    version: "74.90",
    date: "2026-07-17",
    title: "Onboarding — instrutores por track, gate legal e afinações",
    highlights: [
      "**👤 Instrutor por serviço.** O cartão de instrutor mostra agora a pessoa certa: **André Pavlenco (Instrutor · COO)** para SEO e **Germano Cunha (Instrutor · Diretor DPT Publicidade)** para Ads, com fotografia. Clientes com ambos veem os dois.",
      "**🔒 Confirmação antes de começar.** Na 1ª visita ao onboarding aparece um aviso: o processo só pode ser feito após assinar o contrato e liquidar a fatura (confidencial e legalmente registado). O cliente escreve «Confirmar» para prosseguir — a confirmação fica registada.",
      "**✍️ Textos afinados.** Categorias renomeadas (*Vamos conhecer-nos melhor!*, *Acessos Consultoria SEO*, *Acessos Consultoria Ads*, *Último Passo*) e nova descrição no hero (“leva cerca de 30 minutos…”).",
      "**🎨 Logótipos corrigidos.** Ícones do Google Ads e da Meta redesenhados; sem serviço pré-selecionado no *Iniciar onboarding*.",
    ],
  },
  {
    version: "74.89",
    date: "2026-07-16",
    title: "Onboarding — overview redesenhado + ícones de plataforma",
    highlights: [
      "**✨ Overview premium.** A página de onboarding do cliente foi redesenhada de raiz: hero com saudação, **anel de progresso** e botão *Continuar* (leva ao próximo passo por concluir), uma **linha temporal** de passos com estados (concluído / próximo / por fazer) e uma barra lateral com o instrutor e apoio.",
      "**🎨 Ícones de plataforma.** Os acessos passam a mostrar o **logótipo real da plataforma** (Google Analytics, Search Console, Perfil de Empresa, Google Ads, Meta, Website…) em vez de emoji.",
      "**🏷️ Badge de roadmap.** Cada acesso/passo indica se pertence ao **roadmap de SEO** ou de **Ads**, para se perceber de relance a que consultoria se refere.",
      "**☑️ Sem serviço pré-selecionado.** No cartão *Iniciar onboarding*, nenhum serviço vem ticado por defeito — é preciso escolher explicitamente.",
      "**🛠️ Editável.** O ícone de plataforma de cada lição também se escolhe no editor de onboarding.",
    ],
  },
  {
    version: "74.88",
    date: "2026-07-16",
    title: "Onboarding por serviço (SEO / Ads) + bloco movido para SuperAdmin",
    highlights: [
      "**🎯 Onboarding por serviço.** Ao criar o link escolhe-se os serviços — **Consultoria SEO, Google Ads e/ou Meta Ads**. O link gerado compõe os passos certos: só SEO → fluxo A; só Ads → fluxo B; SEO + Ads → fluxo C com passos de ambos.",
      "**📣 Conteúdo de Ads.** Novo formulário de Ads (14 perguntas em 5 secções, adaptado do questionário de Google/Meta Ads) e nova categoria de acessos *Acessos Ads* (Conta Google Ads, Meta / Facebook Business).",
      "**🧩 Sistema de tracks.** Cada lição e cada pergunta tem um *track* (SEO / Ads / Comum). O editor de onboarding ganhou um seletor de track por lição e por passo, para gerir tudo sem código.",
      "**🗂️ Bloco movido para o SuperAdmin.** O *Iniciar onboarding* saiu do board de SEO e passou a viver na **Suite de SuperAdmin** (`/admin` → Onboarding), com lista de clientes em processo e atalho para o editor.",
      "**📄 PDF combinado.** Um cliente com SEO + Ads preenche os dois formulários e as respostas juntam-se num único PDF anexado ao projeto.",
    ],
  },
  {
    version: "74.87",
    date: "2026-07-16",
    title: "SEO — editor do processo de Onboarding (curso + formulário)",
    highlights: [
      "**✏️ Editor de onboarding na app.** Nova página `/seo/onboarding-editor` (só SuperAdmin) para editar tudo sem tocar em código: **lições do curso** (título, emoji, resumo, link de vídeo, blocos de texto/listas/emails) e **perguntas do formulário** (texto, tipo, opções, obrigatório).",
      "**➕ Adicionar / remover / reordenar.** Categorias, lições, passos e perguntas podem ser criados, apagados e movidos (setas cima/baixo). Botão *Repor original* volta ao conteúdo por defeito.",
      "**🛡️ À prova de falhas.** O conteúdo passou a ser guardado em KV, mas com validação/normalização no servidor e *fallback* automático para o conteúdo original — uma edição inválida nunca parte o fluxo.",
      "**🔗 Aplica-se de imediato.** As alterações refletem-se em todos os links de onboarding e no PDF das respostas.",
      "**👁️ Só SuperAdmin.** O cartão *Iniciar onboarding* e o botão *Editar processo de onboarding* no board de SEO aparecem apenas a SuperAdmins.",
    ],
  },
  {
    version: "74.86",
    date: "2026-07-16",
    title: "SEO — fluxo de Onboarding de clientes (formulário + curso + PDF)",
    highlights: [
      "**🚀 Onboarding de clientes end-to-end.** Novo fluxo público (sem login) em `/[cliente]/onboarding`: um **hub de \"lições\"** com barra de progresso e passos organizados em *Onboarding PT*, *Acessos* e *Último Passo e Obrigado!*.",
      "**📝 Formulário passo-a-passo.** Recriação fiel do Google Form de onboarding (35 perguntas, 11 secções) como quiz premium — uma pergunta por ecrã, barra de progresso, validação por passo, animações e upload de ficheiros. Tudo em PT-PT.",
      "**📄 PDF automático.** Ao submeter, é gerado um **PDF das respostas** com o logótipo Wonder Ads e data DD/MM/AAAA (via `pdf-lib`), anexado automaticamente à zona *Onboarding Form* do projeto de SEO.",
      "**🎬 Páginas de passo.** Cada acesso (GA4, Search Console, GMB, Website) tem a sua página com player de vídeo (campos prontos para os links), bloco *Sobre Este Passo* com os emails da equipa a adicionar como administradores, e botão *Marcar como Concluído* que atualiza o progresso em tempo real.",
      "**👀 Vista interna + link partilhável.** No projeto de SEO, a equipa vê o estado do onboarding (passos concluídos, acessos confirmados, PDF do formulário) e pode **copiar o link público** para enviar ao cliente. SuperAdmins podem **iniciar o onboarding de um novo cliente** (que passa a projeto de SEO ao submeter o formulário).",
    ],
  },
  {
    version: "74.85",
    date: "2026-07-16",
    title: "NPS — barra de secções mais equilibrada (grelha 5×2)",
    highlights: [
      "**📐 Cabeçalho de progresso reformulado.** As secções deixam de estar espalhadas com `justify-between` (que deixava buracos na 2ª linha) e passam a uma **grelha alinhada de 5 colunas** (2 linhas de 5), equilibrada entre linhas e ao longo de cada linha. No telemóvel usa 2 colunas.",
    ],
  },
  {
    version: "74.84",
    date: "2026-07-16",
    title: "NPS — afinações de texto e ordem das secções",
    highlights: [
      "**🏷️ Serviços com “Consultoria de …”** (Consultoria de SEO/GEO, de Google Ads, de Meta Ads, de CRM, de Web Design & Desenvolvimento).",
      "**🗓️ “neste último período de 60 dias”** na pergunta de seleção da equipa.",
      "**🔤 Títulos de secção mais curtos:** *Equipa*, *Desempenho*, *Resultados*, *Recomendação*.",
      "**🔀 Nova ordem:** Progresso → Resultados → Continuidade.",
      "**✍️ “Serviços” simplificado** para *“Há algum serviço ou apoio que sentes que te falta?”*.",
    ],
  },
  {
    version: "74.83",
    date: "2026-07-16",
    title: "NPS — seleção de equipa obrigatória + avaliação individual por pessoa",
    highlights: [
      "**👥 Nova 2ª pergunta (obrigatória): “Seleciona quem te acompanhou neste último período”** — com toda a equipa (SEO, Ads, Web, SuperAdmins), escolha múltipla. É preciso selecionar pelo menos uma pessoa.",
      "**⭐ Nova 3ª pergunta: avaliar cada pessoa.** Aparece uma escala 0–10 **por cada pessoa selecionada**, com o respetivo nome. A média destas notas passa a ser a métrica “Equipa (desempenho)” (substitui a antiga nota única do consultor).",
      "**🧹 Menos SEO no texto.** Removido “SEO” do cabeçalho e da introdução (o inquérito serve todos os serviços) e do título da pergunta do consultor.",
      "**✍️ Perguntas mais simples.** Removido “em conjunto” na pergunta dos indicadores; a pergunta dos temas passa a *“Que temas não abordámos mas gostarias que tivéssemos falado mais nos últimos tempos?”*.",
      "**🎨 Barra de progresso.** Passa a ter só 2 estados de cor (secção ativa a preto, restantes a cinzento).",
    ],
  },
  {
    version: "74.82",
    date: "2026-07-16",
    title: "NPS — destacar membros da equipa (toda a Wonder Ads)",
    highlights: [
      "**🙌 Nova pergunta na secção Recomendação.** *“Há algum membro da equipa que gostasses de destacar pelo impacto positivo que teve na tua parceria?”* — com todas as pessoas da app (SEO, Ads, Web e SuperAdmins), em opções com letras (A, B, C…) e seleção múltipla. Aparece na vista do consultor e no PDF.",
    ],
  },
  {
    version: "74.81",
    date: "2026-07-16",
    title: "NPS — 1ª pergunta = serviço, opção “Outro” com texto, e limpeza de perguntas",
    highlights: [
      "**🧭 Nova 1ª secção “Serviço”.** Antes da satisfação, o cliente indica que serviço(s) da Wonder Ads está a acompanhar — SEO/GEO, Google Ads, Meta Ads, CRM, Web Design & Desenvolvimento (ou Outro). Deixa de assumir que é só SEO.",
      "**✍️ Opção “Outro” com campo aberto.** Ao escolher “Outro” (na secção Serviço e no “O que correu bem”), aparece uma caixa para escrever. Aparece também na folha PDF e na vista do consultor.",
      "**🗺️ “Grande Lisboa”.** A nota da opção de presencial semanal passa a *“Para clientes de Cascais e Grande Lisboa”*.",
      "**🧹 Menos atrito.** Removida a pergunta aberta “Das ações acima, qual teve maior impacto…” e o campo do nome no fim (deixou de ser pedido).",
    ],
  },
  {
    version: "74.80",
    date: "2026-07-16",
    title: "NPS — botão “Download PDF” + PDF em estilo formulário com tick boxes",
    highlights: [
      "**📄 Botão “Download PDF” mais visível** ao lado de “Respondido em…” e “Apagar última”, na página de NPS do consultor.",
      "**☑️ PDF em estilo formulário preenchido.** A folha imprimível passa a mostrar as respostas como o cliente as viu: escala 0–10 com o valor destacado, e todas as opções com tick boxes marcadas/não marcadas (não só as escolhidas) — muito mais fácil de ler de relance.",
    ],
  },
  {
    version: "74.79",
    date: "2026-07-16",
    title: "NPS — pergunta das ações mais simples e focada no resultado",
    highlights: [
      "**🗣️ Linguagem mais clara.** A pergunta das ações de maior impacto passa a estar em linguagem simples e focada no que o cliente sente: *“Das ações que fizemos por ti, quais sentes que tiveram maior impacto na tua faturação e na entrada de contactos/leads?”* (os clientes não têm de perceber de SEO).",
    ],
  },
  {
    version: "74.78",
    date: "2026-07-16",
    title: "NPS — ecrã final: mensagem “pode fechar” com cruz animada",
    highlights: [
      "**✖️ Fim do inquérito mais claro.** O botão “Fechar janela” foi substituído por uma mensagem *“Já pode fechar esta janela.”* com uma pequena cruz animada ao lado (roda suavemente entre × e +).",
    ],
  },
  {
    version: "74.77",
    date: "2026-07-16",
    title: "NPS — UX do formulário melhorada, ajustes de perguntas e download PDF",
    highlights: [
      "**✨ Formulário do cliente com muito melhor UX.** Transições animadas entre secções (slide direcional), entrada das perguntas em cascata, animações de hover/seleção e barra de progresso com brilho. Respeita `prefers-reduced-motion`.",
      "**📝 Ajustes de perguntas.** A 1ª pergunta passa a referir “neste último período desde o último formulário que preencheste”. As notas “Seleciona todas as opções aplicáveis” ficam mais legíveis. Removida a pergunta “Em que tipo de momentos… presencial”. Na opção “1 momento presencial por semana” foi adicionada a nota *“Para clientes de Cascais e Lisboa”*.",
      "**⭐ Testemunho → Google Review no fim.** Removida a caixa de testemunho; passa a haver uma mensagem a explicar que, após submeter, o ecrã final tem o link para deixar a Google Review. O ecrã de agradecimento ganhou o botão de review e um botão **Fechar janela**.",
      "**📄 Download PDF (lado do consultor).** Na página de NPS há agora um botão **PDF** que abre uma folha imprimível (tema claro, com o nosso branding) com todas as perguntas e as respostas mais recentes do cliente — pronta a guardar em PDF.",
    ],
  },
  {
    version: "74.76",
    date: "2026-07-16",
    title: "NPS — botão “Deixar review no Google” no fim do inquérito",
    highlights: [
      "**⭐ CTA de Google Review.** Na secção final (Testemunho), o cliente tem agora um botão direto para deixar a review na ficha Google da Wonder Ads, além do testemunho que guardamos internamente.",
    ],
  },
  {
    version: "74.75",
    date: "2026-07-16",
    title: "NPS de SEO — inquérito reformulado (escala 0–10, perguntas mais ricas)",
    highlights: [
      "**🔁 Inquérito de satisfação totalmente renovado.** Substituímos as antigas 13 perguntas 1–5 por um inquérito inspirado nas melhores consultorias, adaptado ao nosso serviço de SEO: 8 secções (Satisfação, Progresso, Consultor, Continuidade, Resultados & impacto, Serviços, Recomendação e Testemunho).",
      "**📊 Escala 0–10 (NPS real).** As perguntas-chave — satisfação geral, desempenho do consultor e probabilidade de continuidade — passam a 0–10. A categoria segue a regra clássica (Promotor 9–10 · Passivo 7–8 · Detrator 0–6).",
      "**🧩 Novos tipos de resposta.** Escolha única, multi-escolha (com limite, ex.: “até 5”), e perguntas abertas — com o nosso branding e uma UX bem mais cuidada no formulário do cliente.",
      "**🆕 Recomeço limpo.** O histórico anterior (formato 1–5) fica arquivado e deixa de contar; os novos resultados começam do zero para os números serem comparáveis.",
      "**⭐ Fecho com testemunho.** No fim, pedimos ao cliente um testemunho orientado (antes/o que fizemos/o que mudou) para reforçar prova social.",
    ],
  },
  {
    version: "74.74",
    date: "2026-07-16",
    title: "SEO — seta decorativa removida dos cartões (sobrepunha o botão de pausa)",
    highlights: [
      "**🔧 Correção visual.** Nos cartões de cliente do SEO, a setinha animada no canto superior direito deixava de fazer sentido — ficava por baixo do botão de pausar/reativar. Foi removida desses cartões (mantém-se nos cartões de ADS).",
    ],
  },
  {
    version: "74.73",
    date: "2026-07-16",
    title: "SEO — secção de clientes Pausados / Suspensos",
    highlights: [
      "**⏸️ Nova secção “Pausados / Suspensos” no SEO DPT.** Por baixo do board de clientes ativos aparece agora uma segunda grelha, com a **mesma estrutura por consultor**, para clientes em pausa. Ficam atenuados e fora da contagem principal.",
      "**🔒 Só SuperAdmin pode pausar/reativar.** Cada cartão passa a ter um botão (visível apenas a SuperAdmins) para **pausar** ou **reativar** o projeto. A permissão é validada também no servidor.",
      "**⏸️ CuidaMais movido para Pausados.** Sai da coluna ativa do André Pereira (mantendo-o como consultor) e passa para a nova secção.",
    ],
  },
  {
    version: "74.72",
    date: "2026-07-16",
    title: "Voltar de um ticket leva ao dashboard de Web",
    highlights: [
      "**↩︎ Botão “voltar” num ticket regressa ao Web.** Dentro de um ticket (*/web/tickets/…*), a seta de voltar apontava para a lista de tickets. Passa agora a levar diretamente ao **dashboard geral de Web** (*/web*).",
    ],
  },
  {
    version: "74.71",
    date: "2026-07-14",
    title: "Fernando Almeida — consolidado num único cliente SEO",
    highlights: [
      "**🧹 Fernando Almeida deixa de estar duplicado.** Existiam duas entradas — *“Clínica Fernando Almeida”* (SEO, com o brief e os dados) e *“Prof. Fernando Almeida”* (Web, vazia). Ficam agora **um único cliente de SEO**, com o nome **“Prof. Fernando Almeida”**, mantendo **todos os dados** (brief, tier, NPS, faturação, logótipo). A entrada duplicada no departamento Web foi removida. O *slug* interno manteve-se para não perder nada.",
    ],
  },
  {
    version: "74.70",
    date: "2026-07-14",
    title: "Clients — editar serviços, aba Contrato e upload de logótipo",
    highlights: [
      "**🛠️ Editar os serviços de um cliente já criado.** Na ficha do cliente (clica no nome na tabela) há agora um editor de **Serviços** — liga/desliga **SEO · ADS · Web · CRM** e Guarda. Resolve casos como o **Kings Gyms** (passar a SEO + ADS) ou **mover** um cliente de um departamento para outro. As linhas na tabela financeira atualizam-se logo a seguir.",
      "**📄 Aba “Contrato” na ficha.** Ao lado de *Faturas*, uma nova aba **Contrato** para anexar o(s) contrato(s) assinado(s) do cliente (PDF ou imagem), guardados no Blob tal como as faturas.",
      "**🖼️ Upload de logótipo do teu PC.** Passa o rato por cima do logótipo no topo da ficha e faz **upload de uma imagem** — fica como logótipo do cliente na tabela de Clients, na grelha do **Departamento SEO** e na página do cliente. Só SuperAdmins. Substitui o emoji/placeholder por marcas reais.",
    ],
  },
  {
    version: "74.69",
    date: "2026-07-14",
    title: "Finanças/Clients — one-off, total upfront, CRM e cancelar cliente",
    highlights: [
      "**🔁 Recorrência “One-off”.** A coluna de *Billing cadence* passa a ter a opção **One-off** (pagamento único). Como não é recorrente, **não conta para o MRR** e não gera próxima faturação.",
      "**💶 Nova coluna “Total (upfront)”.** Entre *Monthly value* e *IVA* há agora uma coluna para o **valor total que o cliente paga quando paga adiantado**. É informativa — não entra no MRR.",
      "**🟢 Departamento CRM.** Ao adicionar um cliente (e em toda a tabela) passa a existir o departamento **CRM**, a par de SEO / ADS / Web.",
      "**↕️ Ordenação estilo Notion, agora persistente.** Continua a ordenar por qualquer coluna (clica no cabeçalho: ascendente → descendente → sem ordem) e a escolha **fica guardada** entre visitas.",
      "**🗑️ Cancelar cliente dentro da app.** Cada linha tem um **“Cancelar cliente”** que abre uma confirmação onde é preciso **escrever CONFIRMAR** antes de apagar. Remove o cliente (todas as linhas de departamento) do roster financeiro; se tinha sido adicionado manualmente, é apagado por completo.",
    ],
  },
  {
    version: "74.68.6",
    date: "2026-07-14",
    title: "NPS — perguntas de escolha múltipla",
    highlights: [
      "**☑️ Perguntas de escolha múltipla no formulário.** O NPS passa a suportar perguntas qualitativas de **seleção múltipla** (para além das notas 1–5). A primeira é, na secção *Resultados*, **“Diria que o investimento em SEO teve impacto direto em:”** com as opções **Melhor qualidade de leads · Mais visibilidade · Melhor posicionamento no mercado · Mais vendas / conversões · Ainda sem impacto claro**. São opcionais (não bloqueiam o avanço) e **não entram na média de satisfação** — servem para contextualizar o resultado.",
      "**🔎 Visíveis na revisão do consultor.** Na página de NPS, o bloco “Respostas do cliente” mostra as opções escolhidas destacadas a verde (e as não escolhidas esbatidas), para o consultor ver exatamente o que o cliente assinalou.",
    ],
  },
  {
    version: "74.68.5",
    date: "2026-07-14",
    title: "NPS — onboarding e mês de acompanhamento",
    highlights: [
      "**📅 Contexto de antiguidade na página de NPS.** O cabeçalho da página de NPS (`/seo/<cliente>/nps`) passa a mostrar **desde quando o cliente está connosco** (data de onboarding) e **em que mês de acompanhamento se encontra** (ex.: *“Cliente desde 01/06/2026 · 2º mês de acompanhamento”*). A data vem da mesma fonte do roadmap (onboarding fixado, com fallback para a data de início oficial do cliente), para o consultor ler o NPS já com a noção de há quanto tempo trabalhamos com aquele cliente.",
    ],
  },
  {
    version: "74.68.4",
    date: "2026-07-14",
    title: "NPS — apagar última avaliação (só SuperAdmins)",
    highlights: [
      "**🗑️ Apagar a última avaliação de NPS, direto na app.** No bloco “Respostas do cliente” da página de NPS há agora um botão **“Apagar última”**. Só os **3 SuperAdmins** (Alex, Alice, André) o conseguem executar — qualquer outro utilizador que clique recebe **“Não há permissões suficientes.”**. A proteção é garantida no servidor (não depende de esconder o botão) e o apagar pede confirmação antes de avançar.",
    ],
  },
  {
    version: "74.68.3",
    date: "2026-07-14",
    title: "NPS — respostas detalhadas por pergunta",
    highlights: [
      "**🔎 Ver o que o cliente respondeu a cada pergunta.** A página de NPS (`/seo/<cliente>/nps`) passa a ter um bloco **“Respostas do cliente — última avaliação”** que mostra **todas as 13 perguntas**, agrupadas por secção, com a **marca exata (1–5)** que o cliente deu em cada uma, os rótulos de cada extremo (1 = … · 5 = …) e a média por secção. O comentário aberto aparece no fim. Tudo no idioma em que o cliente respondeu.",
    ],
  },
  {
    version: "74.68.2",
    date: "2026-07-14",
    title: "NPS — escala 0–5, envio a cada 60 dias e alerta pulsante",
    highlights: [
      "**5️⃣ Tudo passa a ser de 0 a 5.** Todas as perguntas — incluindo a final de recomendação — usam a mesma escala **1–5** que o cliente assinala. O índice de satisfação global, as médias por secção, o histórico e o *chip* na página do cliente passam a ser mostrados **em /5** (deixa de existir o /10), para refletir exatamente o que o cliente marcou.",
      "**🗓 Envio a cada 60 dias.** A cadência de reenvio do NPS passa a ter como padrão **60 dias** (opções 30 / 60 / 90). O “Próximo envio” é sempre calculado a partir da última atividade (envio ou resposta) + cadência, pelo que a data à esquerda deixa de estar errada.",
      "**❤️ Alerta pulsante na página do cliente.** Quando o NPS **nunca foi enviado** ou está **na altura de enviar** (a 3 dias ou em atraso), o botão **NPS** no topo da página do cliente fica **vermelho e a pulsar como um coração**, com a etiqueta **“Enviar”**, para o consultor não deixar passar.",
    ],
  },
  {
    version: "74.68.1",
    date: "2026-07-14",
    title: "NPS — afinação de textos e intro animada",
    highlights: [
      "**✍️ Enquadramento de agência.** A pergunta de NPS deixa de dizer “recomendar como consultor de SEO” (que nos reduzia a um consultor individual) e passa a “recomendar a **nossa agência** a um colega ou parceiro de negócio”. A secção do consultor foi personalizada para **“o seu consultor dedicado”** e a intro fala do **nosso trabalho de SEO** e da **equipa** que acompanha a conta.",
      "**🙋 Campo de identificação mais lógico.** Como já sabemos de que cliente é o link, deixámos de pedir “Nome — Empresa”. Agora o campo pergunta apenas **“Quem está a responder por «{cliente}»?”** — só o nome da pessoa.",
      "**⌨️ Intro com efeito de escrita.** O texto de abertura do formulário aparece **letra a letra** (como se estivesse a ser escrito) e, no fim, surge um selo **“Formulário de 4 minutos”**.",
    ],
  },
  {
    version: "74.68",
    date: "2026-07-13",
    title: "SEO — Avaliação de satisfação do cliente (NPS)",
    highlights: [
      "**⭐ Novo formulário de avaliação (modo quiz) para o cliente.** Cada cliente de SEO passa a ter um formulário de satisfação em **modo quiz** — secção a secção, com barra de progresso — que avalia o serviço, os resultados, a comunicação, o consultor e a execução, mais a clássica pergunta de **NPS (0–10)**. É uma página pública e sem chrome da app (`/<cliente>/survey`), com o logótipo e o nome do cliente, e adapta-se automaticamente ao idioma do cliente (PT/EN).",
      "**🎯 Pontuação 0–10 na própria página do cliente.** No topo da página de cliente (`/seo/<cliente>`), ao lado do link do website, aparece um novo *chip* **NPS** com a pontuação global de satisfação (0–10), a verde/âmbar/vermelho consoante o resultado. Um clique abre a página de gestão.",
      "**📊 Página de gestão de NPS (`/seo/<cliente>/nps`).** Mostra o **último resultado** (índice global /10, valor de NPS e categoria Promoter/Passive/Detractor), o **detalhe por secção** com destaque para o ponto mais fraco, a **evolução** ao longo do tempo, o **histórico** completo com comentários, e a data do **próximo envio**.",
      "**📨 Enviar o link e agendar o próximo.** Botões para **copiar o link**, **enviar ao cliente** (regista o envio e abre um email pré-preenchido) ou apenas **marcar como enviado**. Uma cadência configurável (**3 / 6 / 12 meses**) agenda automaticamente o próximo envio, com aviso a **vermelho quando está em atraso**. Cada avaliação respondida reinicia o contador.",
    ],
  },
  {
    version: "74.67",
    date: "2026-07-10",
    title: "Departamento Web com acesso de leitura ao SEO",
    highlights: [
      "**👀 Os designers Web passam a ver as páginas de projeto do SEO — mas em modo só-leitura.** O Mike, o Gustavo, o Renan e o Cylas já conseguem abrir o **Departamento SEO** e entrar em qualquer **página de cliente** (`/seo/<cliente>`) para ver o brief, o roadmap, os ficheiros, as ações e os resultados. **Não podem alterar nada:** edições, botões de gerar com IA, aprovações (“Enviar para revisão”), uploads e apagar estão escondidos, e há um aviso amarelo de _“Read-only”_ no topo de cada página.",
      "**🔒 Bloqueio garantido no servidor, não só na interface.** Mesmo que um botão escape, o _middleware_ rejeita qualquer escrita SEO (POST/PUT/PATCH/DELETE) de quem só tem acesso de leitura — a proteção não depende de esconder controlos.",
      "**🚧 O que fica de fora para o Web.** As ferramentas ao nível do departamento continuam reservadas à equipa de SEO: **KPIs**, **Diretórios**, os **roadmaps por consultor** e a **consola de revisão interna** (`/review`). Aí, um utilizador Web vê o ecrã de _Sem acesso_.",
      "**✅ Sem mudanças para a equipa de SEO.** Consultores de SEO e SuperAdmins mantêm exatamente o mesmo acesso e capacidade de edição de sempre.",
    ],
  },
  {
    version: "74.66",
    date: "2026-07-09",
    title: "Senior Resort — cliente removido",
    highlights: [
      "**🗂️ Senior Resort saiu do workspace.** O cliente **Senior Resort** foi dado como terminado e deixa de aparecer em qualquer lado — some das colunas do **Departamento SEO**, do **SuperAdmin → Clientes** e de todos os seletores de cliente. Passou a estar na lista de _slugs_ excluídos (mesmo mecanismo usado no offboarding anterior), pelo que não volta a ser puxado do Notion.",
      "**🧹 Limpeza dos dados associados.** Removidas as entradas específicas do cliente (tier, indústria, cores/logótipo, brief, data de início e consultor), incluindo o ficheiro de logótipo. Nada foi apagado do KV — os registos ficam apenas por ler.",
    ],
  },
  {
    version: "74.65",
    date: "2026-07-07",
    title: "Roadmaps SEO — botão “Estender +3 meses”",
    highlights: [
      "**➕ Estende o roadmap sem recomeçar do zero.** Os roadmaps eram sempre de **12 semanas (3 meses)** e, quando um cliente chegava ao fim, a única opção era **regenerar** (arquivava o plano e apagava tudo). Agora há um botão **“Estender +3 meses”** na board do roadmap (`/seo/<cliente>/roadmap`) que acrescenta mais um trimestre — as semanas e tarefas existentes **mantêm-se intactas** e as novas semanas aparecem como colunas vazias prontas a planear. Vai crescendo em passos de 3 meses até ao limite de **12 meses (48 semanas)**.",
      "**🔔 Aviso quando o plano está a acabar.** Ao entrar no **último mês** do roadmap (ou ao passar do fim), a board mostra um cartão destacado a sugerir estender já o próximo trimestre — e o próprio botão “Estender” fica com o gradiente da marca para não passar despercebido. É exatamente quando um roadmap a terminar precisa da continuação.",
      "**🗓️ Meses dinâmicos em todo o lado.** A grelha de meses, o chip **“Semana X de N”**, a pré-visualização do cliente (roadmap completo + mês atual), o **SuperAdmin → Roadmaps** e o **“A minha semana”** de cada consultor passam todos a refletir a duração real do roadmap (12, 24, 36…), em vez de assumirem sempre 12.",
      "**🕓 Registado no changelog.** Cada extensão fica no changelog do roadmap do cliente (ex.: _“Extended roadmap to 24 weeks (6 months)”_), com data/hora e quem a fez.",
    ],
  },
  {
    version: "74.64.2",
    date: "2026-07-01",
    title: "Weekly Update sem badge 'NEW'",
    highlights: [
      "**🧹 Botão Weekly Update.** Removido o badge **'NEW!'** do botão 'Gerar Weekly Update' nos roadmaps — já não é novidade.",
    ],
  },
  {
    version: "74.64.1",
    date: "2026-06-30",
    title: "Spine Center — URL atualizado",
    highlights: [
      "**🔗 Spine Center.** Website corrigido de `www.spinecenter.pt` para **`spinecenter.pt`** (sem www) em toda a app — alimenta crawls, schema, internal linking e rank tracking.",
    ],
  },
  {
    version: "74.64",
    date: "2026-06-30",
    title: "Changelog nos roadmaps dos clientes (SEO)",
    highlights: [
      "**🕓 Changelog em cada roadmap.** A página de roadmap de cada cliente SEO (`/seo/<cliente>/roadmap`) passa a ter um painel **Changelog** (recolhível) com o histórico de alterações: tarefas adicionadas/removidas, mudanças de estado (ex.: _In progress → Implemented_), mudanças de semana, renomeações e gerações do roadmap — com data/hora e **quem** fez a alteração.",
      "**💾 Otimizado para armazenamento.** Os registos são gravados de forma **compacta** (chaves de 1 caractere, timestamp em segundos, estado como índice 0–3, títulos cortados a 60 caracteres) e **limitados a 120 entradas por cliente** (FIFO). Só alterações **relevantes** são registadas — reordenar tarefas por drag não escreve nada, por isso não há desperdício de KV.",
    ],
  },
  {
    version: "74.63",
    date: "2026-06-29",
    title: "Schema Markup action — 10x mais rico + deteção de mercado",
    highlights: [
      "**🌍 Fim do 'Portugal' errado.** A Kings Gyms é um ginásio do **Reino Unido** (Londres, GBP, +44) mas não tinha geo definido, por isso caía no default Portugal — e isso contaminava o schema **e** o rank tracking (DataforSEO). Adicionado override `kings-gyms → United Kingdom / en`. Tópico de fundo: a ação de schema passa a **detetar o mercado pela própria página** (morada, indicativo telefónico, TLD, idioma da cópia), nunca pelo geo da agência.",
      "**🧠 Schema muito mais rico.** A ação Schema Markup agora **faz crawl da página em tempo real** (NAP, horários, serviços, FAQs e o JSON-LD que já lá está) e gera um **@graph** completo cruzado por `@id`: o negócio (tipo mais específico — ExerciseGym/HealthClub/MedicalClinic/…), WebPage, WebSite, Organization, BreadcrumbList e FAQPage. Preenche morada+região, geo, horários, priceRange, moeda, amenities, e ofertas/serviços (OfferCatalog).",
      "**✅ Regras de qualidade.** `@id` em todos os nós, telefone E.164, datas ISO-8601, país ISO-3166, idioma BCP-47, sem placeholders, `sameAs` sem vazios, e **só inclui rating/reviews se estiverem visíveis na página** (evita ação manual do Google). O output traz mercado detetado, porquê dos tipos, o que falta, oportunidades, checklist de validação e onde colar.",
      "**🎛️ Novo campo Mercado/Idioma** (Auto-deteção · EN-UK/US · PT-PT/BR · ES · FR) + opção **Auto / @graph** por defeito no tipo de schema. Conteúdo de origem passa a ser opcional quando há URL.",
    ],
  },
  {
    version: "74.62",
    date: "2026-06-29",
    title: "SEO Directories — +21 diretórios de saúde + pipeline com aprovação",
    highlights: [
      "**➕ 21 novos diretórios (agora 199).** Mais diretórios de **saúde / bem-estar / psicologia** nos nichos dos nossos clientes: lista internacional (US/UK) de saúde (credihealth, healthmanagement.org, mind.help, counselingnow, etc.) e portais PT (profissionaisdesaude.pt, Ordem dos Médicos, saudebemestar.pt, agilcare, nutriama, notícias regionais). Deduplicados por domínio — sem repetir os que já existiam.",
      "**🔁 Pipeline com aprovação.** Os estados dos alvos passam a ser **A tentar → Aguarda aprovação → Submetido → Publicado → Rejeitado**, e o seletor de estado ganhou a opção **'Remover do pipeline'** (além do botão de lixo).",
    ],
  },
  {
    version: "74.61",
    date: "2026-06-29",
    title: "SEO Directories — motor de backlinks por cliente",
    highlights: [
      "**🔗 SEO Directories.** Nova ferramenta no SEO DPT (botão por cima dos KPIs): uma base de dados partilhada de **178 diretórios** de backlinks (as bibliotecas Free + Paid do Notion, deduplicadas por domínio, + ~18 diretórios bem conhecidos). Cada um com nicho, idioma, país, DA/DR, tráfego orgânico, spam score e requisitos de submissão — tudo **editável, adicionável e removível** na app.",
      "**🎯 Sugestões por cliente.** Escolhe um cliente e a app ordena os diretórios com melhor fit. O idioma e o nicho são *gates* (um ginásio em Londres só recebe diretórios **ingleses** de **fitness/saúde** + diretórios gerais), e o país + autoridade fazem o ranking. O nicho detetado é ajustável ao vivo para refinar.",
      "**📌 Pipeline de backlinks.** Adiciona diretórios ao pipeline de cada cliente e segue o estado: **A tentar → Submetido → Live → Rejeitado**. Também dá para navegar/filtrar toda a base de dados e selecionar vários de uma vez.",
      "**🔢 Visitantes orgânicos sem abreviar.** O badge de orgânico do SEO DPT passa a mostrar o número completo (ex.: 9 127) em vez de '9.1k'.",
    ],
  },
  {
    version: "74.60",
    date: "2026-06-29",
    title: "Novo bloco Finances no Control Suite",
    highlights: [
      "**💸 Finances.** O SuperAdmin Control Suite passa a ter **4 blocos** — junta-se o **Finances** (`/admin/finances`) aos Clients, Employees e Roadmaps. Reúne a parte financeira num só sítio.",
      "**📅 Calendário mudou de casa.** O botão **'Overview Calendário'** saiu do separador Clients e passou para o novo bloco Finances.",
      "**⏰ Próximas obrigações à mão.** Os blocos **'Próx. 7 dias'** e **'Próximos 30 dias'** (faturas e obrigações a tratar) aparecem agora também no Finances, sem deixarem de estar nos Clients. O badge do bloco mostra o número de faturas agendadas.",
    ],
  },
  {
    version: "74.59.2",
    date: "2026-06-26",
    title: "Fix — página de Employees rebentava (client-side exception)",
    highlights: [
      "**🐛 Employees a abrir outra vez.** A página `/admin/employees` rebentava com 'client-side exception' sempre que existia um colaborador **sem portfólio de cliente** (admins como o Alex, designers de Web, etc.): o objeto de fallback tinha um campo antigo (`sampleTitles`) em vez de `breakdown`, e a linha tentava `breakdown.slice()` sobre `undefined`. Corrigido o fallback + guarda defensiva.",
    ],
  },
  {
    version: "74.59.1",
    date: "2026-06-26",
    title: "Fix — badge de visitantes orgânicos não aparecia",
    highlights: [
      "**🐛 Visitantes orgânicos a aparecer.** O badge não surgia porque a soma vinha a 0: as ~21 chamadas GA4 em paralelo disparavam cada uma a resolução completa de propriedades (request storm) e estouravam o tempo. Adicionado **dedup in-flight** (uma única construção do índice de propriedades partilhada) + **timeout por cliente**. O badge passa a aparecer sempre que o GA4 está configurado (mostra o número real, ou '—' se ainda não houver dados).",
    ],
  },
  {
    version: "74.59",
    date: "2026-06-25",
    title: "Novo cliente Kings Gyms (André Pereira) + adicionar clientes direto no Admin",
    highlights: [
      "**🏋️ Kings Gyms (kingsgyms.com).** Novo projeto SEO atribuído ao **André Pereira**, com o **logo real** puxado do site (coroa dourada sobre preto), website, paleta e chip escuro. Aparece automaticamente no SEO DPT, nas páginas do cliente e no Admin (com os campos de faturação a vazio, prontos a preencher).",
      "**➕ Adicionar clientes no Admin.** Em `/admin/projects` há agora um botão **'Add client'**: dás nome, escolhes departamento(s) (SEO/ADS/Web) e website opcional, e o cliente entra como nova linha (uma por departamento) com os campos a vazio. Clientes adicionados assim podem ser removidos (botão 'Remove client' na linha).",
    ],
  },
  {
    version: "74.58",
    date: "2026-06-25",
    title: "SEO DPT — total de visitantes orgânicos (30 dias) ao lado do nº de clientes",
    highlights: [
      "**📈 Visitantes orgânicos do departamento.** Ao lado do badge de clientes no topo do SEO DPT aparece agora um novo badge (verde) com a **soma dos visitantes orgânicos dos últimos 30 dias de todos os clientes** (GA4 · Organic Search). Dados reais, em cache de 30 min; clientes sem GA4 são simplesmente ignorados (nunca inventado).",
    ],
  },
  {
    version: "74.57",
    date: "2026-06-25",
    title: "Creatives Studio — geração de IMAGENS reais (Gemini Flash Image), on-brand",
    highlights: [
      "**🖼️ Gerar imagem.** O Creatives Studio passa a gerar **imagens reais** com o mesmo motor da action de GMB Posts do SEO (Gemini Flash Image) — de acordo com o brief/diretrizes que dás antes de começar.",
      "**🎨 On-brand automático.** As imagens usam a **paleta da marca** e até **4 referências** das fotos do cliente (Files + Vault) para ficarem no estilo certo; sem inventar logótipos.",
      "**💾 Guardar.** As imagens geradas aparecem numa galeria — podes **abrir/descarregar**, **guardar no Campaign Vault** (como criativo) e **guardar na história** (junto com o conceito + copy). O histórico mostra as imagens.",
      "**Nota:** requer `GEMINI_API_KEY` (a mesma já usada na action de GMB). Sem ela, o botão devolve um aviso claro em vez de falhar.",
    ],
  },
  {
    version: "74.56",
    date: "2026-06-25",
    title: "ADS DPT — Creatives Studio (Claude Creatives Pro Max) + reports em inglês para clientes ingleses",
    highlights: [
      "**🪄 Gerar Creatives.** Novo botão (grande, com gradiente) na zona de Files que abre uma **nova aba — Creatives Studio**.",
      "**🎨 Creatives Studio.** Tem **brief** (ideia/ponto de partida, direcionamentos, **copy a usar**, plataforma, formato), chat com o **Claude Creatives Pro Max** (agente especialista em criativos de Google/Meta — gera conceitos, hooks, headlines, copy e direção visual) e **histórico de criações anteriores** (guardar, reabrir, apagar).",
      "**🇬🇧 Reports em inglês.** Para clientes ingleses (ex.: **IHN**), os reports do ADS passam a ser **sempre em inglês** (label, período e textos). Os criativos gerados também saem na língua do cliente.",
    ],
  },
  {
    version: "74.55",
    date: "2026-06-25",
    title: "ADS DPT — Campaign Vault + ADS Advisor (Claude pro em ads) + tags mais bonitas",
    highlights: [
      "**🗂️ Campaign Vault.** Nova zona só do ADS DPT, **única por cliente** (separada dos Client Files): guarda **briefings, criativos/copys e reports mensais anteriores**, com tipo (Briefing/Criativos/Report/…), plataforma (Google/Meta), upload de ficheiros ou links.",
      "**🤖 Pedir Aconselhamento AI.** Botão que abre o **ADS Advisor** — um agente Claude **especialista PRO em Google Ads e Meta Ads**. A primeira mensagem é dele: analisa as **analytics dos últimos 30 dias** + **todo o Campaign Vault** e dá diagnóstico e prioridades; depois conversas à vontade. Nunca inventa métricas — se a plataforma não estiver ligada, diz-o.",
      "**✨ Tags mais bonitas.** Os badges do cabeçalho do cliente (Active, Google Ads, Meta Ads, tier, website) ganharam pills com glow subtil, anel interior e pontos de cor.",
    ],
  },
  {
    version: "74.54",
    date: "2026-06-25",
    title: "ADS DPT — dashboard de cliente reformulada (full-width, dados reais, reports)",
    highlights: [
      "**📊 Dashboard full-width.** Ao abrir um cliente do ADS DPT, a página passa a ser um **dashboard de performance a toda a largura**: KPI Snapshot (Conversões, ROAS, CTR, CPA, Spend), **Top campanhas por conversão** e gráfico de **conversões por período** (Google vs Meta).",
      "**🗓️ Janela com dias.** O seletor de período passa a ter **Semana · Mês · Trimestre · Dias** — o modo 'Dias' deixa escolher o nº de dias (com presets 7/14/30/90).",
      "**🔌 Atenção! Conectar a API.** Quando o Google Ads ou Meta Ads não está ligado à app, aparece um alerta a pedir ligação. **Os dados são SEMPRE reais e puxados das plataformas — nunca inventados.** Sem ligação, os KPIs/campanhas ficam vazios em vez de mostrar números falsos.",
      "**🧾 Reports automáticos.** Nova secção no fim com os reports gerados, a **data do pedido** e um botão **PDF** para abrir/descarregar (snapshot dos KPIs guardado). 'Gerar report' / 'Export PDF' criam um novo.",
      "**📁 Brief no fim.** Do's, Don'ts, Notas e Ficheiros passaram para o **fundo da página**.",
    ],
  },
  {
    version: "74.53.1",
    date: "2026-06-25",
    title: "Próximas ações — apagar permanentemente uma ação concluída",
    highlights: [
      "**🗑️ Apagar permanentemente.** Na secção '**concluídas**', cada ação passa a ter (além do **Reabrir**) um botão de **apagar permanentemente** — com confirmação. A ação deixa de aparecer tanto na lista aberta como nas concluídas.",
    ],
  },
  {
    version: "74.53",
    date: "2026-06-25",
    title: "Relatório Mensal — dashboard de faturação com mês, trimestre e YTD + PDF",
    highlights: [
      "**📊 Botão 'Gerar Monthly Report'.** Novo botão (verde, à esquerda do Overview Calendário) na página de Clients que abre o relatório **numa nova página**.",
      "**🧾 Relatório completo.** KPIs (clientes, MRR, run-rate anual, Obrigações Fiscais/IVA, ticket médio), **receita por departamento** e **por tipo de fatura** (gráficos de barras), bloco **'Este mês'** (faturas a enviar, valor, IVA, progresso de enviadas + obrigações do mês), **Trimestre** e **Desde Janeiro (YTD)** com estimativas, e uma **tabela de todos os clientes**.",
      "**📄 Download em PDF.** Botão 'Descarregar PDF' (documento branco, branded, pronto a imprimir em A4) com nome de ficheiro limpo.",
    ],
  },
  {
    version: "74.52",
    date: "2026-06-25",
    title: "Próximas ações — janelas sem duplicados + concluir com tick e animação",
    highlights: [
      "**🚫 Sem duplicados.** As ações deixam de aparecer nos dois blocos: dentro de **7 dias** aparece **só à esquerda**; entre **8 e 30 dias** aparece **só à direita** (atrasadas continuam no bloco urgente).",
      "**✅ Tick para concluir.** Cada ação tem uma **checkbox**; ao marcar, sai da lista com uma **animação** (check verde + colapso) e fica **concluída** — guardado em KV, por isso desaparece em todos os ecrãs. Um rodapé '**N concluídas**' permite ver e **reabrir** se for engano.",
      "**👋 Saudação animada.** O título da página de Clients passa a **'How is business, {nome}?'** com **animação de escrita** sempre que abres a página.",
    ],
  },
  {
    version: "74.51.1",
    date: "2026-06-25",
    title: "Overview Calendário — grelha mais clara e legível",
    highlights: [
      "**🔆 Calendário mais claro.** Células com fundo mais claro e separadores mais visíveis, números dos dias com mais contraste, cabeçalho dos dias da semana mais legível e os blocos de faturas/eventos com cores mais vivas e texto maior — tudo mais fácil de ler num relance.",
    ],
  },
  {
    version: "74.51",
    date: "2026-06-25",
    title: "Admin — Próximas ações (7/30 dias) + Overview Calendário com eventos personalizáveis",
    highlights: [
      "**⏱️ Próximas ações 7/30 dias.** Entre os blocos Clients/MRR/Obrigações Fiscais e a tabela aparecem dois blocos de **próximas ações**: à esquerda **Próx. 7 dias** (vermelho) e à direita **Próximos 30 dias** ('Brevemente'). Listam e contam as **faturas a enviar** (datas de fatura dos clientes) + os eventos do calendário dentro da janela; faturas atrasadas aparecem destacadas.",
      "**🗓️ Overview Calendário.** Novo botão no topo da página de Clients abre um **calendário mensal em ecrã inteiro** (estilo dark), com as faturas a roxo nos respetivos dias e navegação de mês / Hoje.",
      "**➕ Eventos personalizáveis.** Dá para clicar num dia (ou em 'Adicionar evento') e criar um bloco — ex.: *Entregar IVA até dia 5* — com **título, descrição e cor** à escolha. Clicar num evento edita ou elimina. Guardado em KV e partilhado pela equipa.",
      "**🔁 Ligação às próximas ações.** Qualquer evento a 7 ou 30 dias passa a aparecer automaticamente nos blocos de próximas ações da página geral.",
      "**🧹 Coluna Notes removida** da tabela de clientes (as notas vivem agora na aba **Notas** do pop-up do cliente).",
    ],
  },
  {
    version: "74.50",
    date: "2026-06-25",
    title: "Admin — 'Projects' passa a 'Clients' + pop-up de cliente (contactos, emails, notas, faturas) e nova fiscalidade",
    highlights: [
      "**🏷️ 'Projects' → 'Clients'.** O bloco do SuperAdmin Suite e a própria tabela passam a chamar-se **Clients** (era 'Projects').",
      "**🪟 Pop-up de cliente em ecrã inteiro.** Clicar no nome de um cliente abre um pop-up com **5 abas**: **Contactos** (emails/telefones de faturação), **Email p/ Contabilidade** (template de pedido de fatura à contabilista, com **copiar** + **editar**), **Email p/ Cliente** (template de pedido de pagamento, com copiar + editar), **Notas** e **Faturas** (upload de faturas anteriores). Os dados são **partilhados por cliente** entre SEO/ADS.",
      "**🧾 Invoice Type.** Saiu a coluna **Consultores**; entrou **Invoice Type** (dropdown: **Canva**, **Contabilidade** ou **Plataforma**).",
      "**🗑️ Coluna Status removida** da tabela de clientes (continua a existir nos bastidores para o Portfólio Ativo dos Employees).",
      "**📅 'Next billing' → 'Invoice date'.** A coluna deixa de ser calculada e passa a ser uma **data editável**, vazia por agora.",
      "**💶 Coluna IVA + 'Obrigações Fiscais'.** Nova coluna **IVA** (€, editável) à direita do Monthly Value, e novo **bloco vermelho 'Obrigações Fiscais'** no topo (ao lado do MRR) que **soma toda a coluna do IVA**.",
    ],
  },
  {
    version: "74.49",
    date: "2026-06-24",
    title: "SEO Blog Writer — internal linking a partir do sitemap real (gate de 75%)",
    highlights: [
      "**🔗 Internal linking grounded no sitemap do cliente.** Antes de redigir, a app puxa o **sitemap real** do site do cliente, faz crawl das páginas mais promissoras e mede a **familiaridade topical** de cada uma com o artigo a escrever.",
      "**🎯 Regra dos 75% (topical authority).** Só passam para o redator as páginas com **≥75% de relevância** ao tema do artigo — exatamente como recomenda o Google para autoridade topical. As restantes são descartadas.",
      "**🚫 Fim dos links inventados.** O redator passa a poder linkar APENAS para páginas verificadas (do sitemap) ou para URLs que o consultor colou no campo de inventário. Se nenhuma página atingir os 75%, o artigo **não inventa** links internos — regista uma nota em vez disso.",
      "**🪶 Best-effort e transparente.** Se o sitemap não existir ou não for bom, o gerador faz fallback ao inventário do consultor sem falhar; o progresso mostra quantas páginas ≥75% foram encontradas e cada link traz a sua % de match nas Working Notes.",
    ],
  },
  {
    version: "74.48",
    date: "2026-06-24",
    title: "SEO — novas linhas das Pending Review aparecem no fim",
    highlights: [
      "**⬇️ Linhas novas vão para o fundo.** Nas tabelas de **Pending Review** (todos os clientes SEO), uma linha nova — adicionada manualmente pelo consultor ou automaticamente (Enviar para aprovação) — passa a aparecer **no fim** da lista, a seguir às existentes, em vez de ser inserida como 1ª linha no topo.",
    ],
  },
  {
    version: "74.47",
    date: "2026-06-24",
    title: "WEB DPT — Registo central de clientes (perfis reutilizáveis + hub)",
    highlights: [
      "**🗂️ Registo fixo de clientes.** Cada cliente passa a ter um perfil guardado: designer padrão, notas, **acessos/credenciais** (cofre encriptado), branding kit e links úteis — tudo num só sítio, para deixar de se reintroduzir os dados a cada projeto.",
      "**🔎 Sugestão automática ao criar projeto.** O campo de cliente (no modal de novo projeto e no formulário de tickets) passou a ser um **combobox** que sugere perfis à medida que escreves; ao escolher um cliente existente, o projeto nasce já com o designer e o cofre do cliente preenchidos.",
      "**👤 Hub do cliente.** Nova área **WEB DPT → Clientes** (`/web/clients`) com a lista de todos os clientes e, por cliente, uma página que centraliza o perfil editável + **todos os projetos e tickets** desse cliente.",
      "**🌱 Auto-seed.** Os clientes já existentes nos projetos aparecem logo nas sugestões e na lista (marcados como 'perfil por completar'); ficam guardados como perfil quando os editas ou crias um projeto novo para eles.",
    ],
  },
  {
    version: "74.46",
    date: "2026-06-24",
    title: "Tickets Web — designer atribuído é mencionado (@) no Slack",
    highlights: [
      "**🔔 Tag do designer no Slack.** Ao abrir um novo ticket para o DPT Web, o **designer atribuído** passa a ser mencionado com `@` real (`<@U…>`) na notificação do Slack — recebe ping direto em vez de só ver o nome em texto. Se o membro ainda não tiver Slack id configurado, mantém o nome simples (sem regressão).",
    ],
  },
  {
    version: "74.45",
    date: "2026-06-23",
    title: "Keyword Research — upload de ficheiros por-run + camada de backtesting",
    highlights: [
      "**📎 Upload de ficheiros no formulário de KW Research.** Anexa listas de keywords, exports de concorrentes, planos ou briefings (CSV/TXT/MD/PDF) — são lidos, indexados e integrados no contexto **antes** das APIs externas (User Input → File Analysis → AI Context → External Data).",
      "**🔁 Backtesting / confidence layer.** Novo botão **Backtest** na ação de KW Research: compara as previsões do DataforSEO da última pesquisa com o desempenho **real no GSC** (impressões, cliques, CTR, posição) e calcula uma **confiança 0–100**, sinalizando volumes sobre-estimados, long-tail sub-estimado e quick wins de CTR.",
      "**🧠 Loop de aprendizagem.** O resultado do backtest é guardado e **injetado automaticamente na próxima KW Research**, para o scoring ponderar a precisão passada (ex.: desconfiar de volumes altos sem histórico real).",
    ],
  },
  {
    version: "74.44",
    date: "2026-06-23",
    title: "Keyword Research v2 — dados próprios primeiro (GSC + GA4) + Opportunity Score",
    highlights: [
      "**📝 Novo campo 'Comments or Additions to Consider'** (sublinhado, opcional) no formulário de Keyword Research — contexto estratégico processado ANTES de qualquer API: serviços a priorizar, sazonalidade, concorrentes, exclusões, serviços que mais faturam. Influencia a expansão de keywords E o scoring.",
      "**📊 Dados próprios primeiro.** O pipeline passa a puxar **GSC** (queries em que já se rankeia, posições 4–20, impressões altas/CTR baixo) e **GA4** (engagement, conversões) ANTES do DataforSEO, e injeta-os no prompt como baseline de oportunidade. Ordem: Form → Comentários → GSC → GA4 → DataforSEO → síntese.",
      "**🔌 Pré-flight ao DataforSEO** — valida as credenciais antes de gastar a chamada e falha com mensagem clara.",
      "**🎯 Opportunity Score + tabela enriquecida.** O output ganha uma tabela mestre priorizada com colunas *Current Rank · GSC Clicks · CTR · Conversion Value · Opportunity Score · Priority*, com a fórmula ponderada (Search Demand × Conversion Potential × Ranking Feasibility × Business Relevance × Existing Authority) e secções de Quick wins (CTR/página-2/canibalização) + New opportunities.",
    ],
  },
  {
    version: "74.43",
    date: "2026-06-23",
    title: "Enviar o changelog para o Slack pelo bot do workspace",
    highlights: [
      "**📣 Cada release do changelog tem agora um botão 'Enviar para o Slack'** (só SuperAdmins) — publica as notas dessa versão no canal, pelo bot do workspace (a WonderAds App). Usa `SLACK_CHANGELOG_WEBHOOK_URL` (ou, em falta, o webhook do Web).",
      "**📝 Edição direta do backlog antes de enviar** continua disponível: a caixa de texto do Generate backlog é editável e tanto o Copy como o Enviar para o Slack usam o texto já editado.",
    ],
  },
  {
    version: "74.42",
    date: "2026-06-23",
    title: "Backlog: botão 'Enviar para o Slack' com menções reais",
    highlights: [
      "**📨 Novo botão 'Enviar para o Slack' no Generate backlog.** As menções (`@nome`) só ficam clicáveis quando a mensagem é *enviada* via Slack — coladas à mão aparecem sempre como texto. O botão publica o backlog direto no canal da equipa Web, convertendo `@Nome → menção real` e `@channel → broadcast`.",
      "**👀 O texto fica legível para copiar** — o Copy passa a mostrar `@Gustavo Rotini` (e não `<@U…>`); a conversão para menções acontece só no envio direto.",
    ],
  },
  {
    version: "74.41.2",
    date: "2026-06-23",
    title: "Generate backlog resiliente a sobrecarga da IA",
    highlights: [
      "**🛡️ O 'Generate backlog' deixa de falhar quando a IA está sobrecarregada (erro 'Overloaded').** Agora faz mais tentativas com backoff e, se o modelo principal continuar em baixo, recorre automaticamente a um modelo de reserva — só mostra erro se ambos falharem, e nesse caso com uma mensagem clara a sugerir Regenerar.",
    ],
  },
  {
    version: "74.41.1",
    date: "2026-06-23",
    title: "Backlog: menções de Slack reais ativas",
    highlights: [
      "**🔗 Os Slack member IDs foram configurados** (André Pavlenco, André Pereira, Yenisey, Mike, Gustavo, Renan) — o Generate backlog passa a gerar menções **clicáveis** ao colar no Slack. *(Falta só o Cylas.)*",
    ],
  },
  {
    version: "74.41",
    date: "2026-06-23",
    title: "Tickets no board (arrastáveis), Slack só para novos, backlog em bold + menções",
    highlights: [
      "**🔕 O Slack só notifica TICKETS NOVOS.** As mudanças de estado deixaram de enviar mensagem para o canal — menos ruído.",
      "**🗂️ Tickets passam a ser cartões de 1ª classe no board Web:** mesma forma dos projetos, **arrastáveis entre colunas** (o estado muda em conformidade) e mapeados nas mesmas 5 colunas (Not Started → In Progress → Client Feedback → Done).",
      "**📋 Generate backlog:** o **nome do cliente** vai sempre a *negrito* e o **título 'Backlog web DD.MM'** também. *(Nota: o Slack não suporta sublinhado em texto colado — só negrito sobrevive ao copy-paste.)*",
      "**🔗 Menções reais no backlog:** os nomes passam a virar menções clicáveis no Slack — assim que os Slack member IDs forem configurados (ver `SLACK_USER_IDS`). Sem IDs, mantém-se o texto simples (sem regressão).",
    ],
  },
  {
    version: "74.40.2",
    date: "2026-06-23",
    title: "Fix: notificações de Slack dos tickets agora chegam sempre",
    highlights: [
      "**🐛 A mensagem de Slack ao criar (e ao mudar de estado) um ticket nem sempre era enviada.** A chamada era *fire-and-forget* e, em serverless, a função podia congelar logo após responder — matando o envio a meio. Agora é aguardada antes da resposta, com um timeout de 5s para nunca bloquear a criação do ticket.",
    ],
  },
  {
    version: "74.40.1",
    date: "2026-06-23",
    title: "Mensagem de Slack do novo ticket mais completa",
    highlights: [
      "**🔔 A notificação de novo ticket no Slack foi melhorada:** título passa a *Novo ticket para o DPT Web (#N) — <título>*; o campo de departamento agora diz **Departamento Que Abriu o Ticket**; mostra o **Designer atribuído** (o nome se foi forçada a atribuição, senão *Por atribuir*); a **Descrição** e os **Acessos** ganham um rótulo próprio por cima; e os botões passam a **Visualizar este ticket** + **Abrir um novo ticket**.",
    ],
  },
  {
    version: "74.40",
    date: "2026-06-23",
    title: "Tickets Web: refinamentos do formulário, entrada na home e integração no board",
    highlights: [
      "**🎟️ Botão 'Criar ticket DPT WEB' agora acoplado ao card WEB DPT na página inicial** — mais visível e integrado, sem deixar de ser acessível a qualquer pessoa sem entrar no departamento.",
      "**📝 Formulário afinado:** categoria começa em **Melhoria**; **DPT Pedinte** sem 'Web' e com **Administração** em vez de 'Outro'; novos campos **Acessos** (logins/URLs para a equipa) e **Projeto / Cliente** (com sugestões dos clientes existentes); cabeçalho passa a **DPT Web** sem subtítulo.",
      "**✅ Submissão simplificada:** ao criar, aparece uma confirmação de sucesso e o utilizador volta à **página inicial** para escolher o departamento (já não cai na página do ticket).",
      "**🧑‍💻 'Forçar atribuição'** no formulário — opcionalmente atribui o ticket logo a um web dev (Mike, Renan, Gustavo ou **Cylas**, novo na equipa).",
      "**📋 Integração no board Web:** a coluna passa a chamar-se **Not Started** e os tickets abertos aparecem automaticamente nela (mesmo por atribuir), com badge TICKET, para a equipa triar onde já trabalha.",
    ],
  },
  {
    version: "74.39",
    date: "2026-06-22",
    title: "Sistema global de Tickets para o Departamento Web",
    highlights: [
      "**🎫 'Criar Ticket para Web' na página inicial** — qualquer colaborador, de qualquer departamento, pode reportar um pedido à equipa de Web sem entrar no departamento. Formulário com título, descrição, categoria (Bug / Nova funcionalidade / Alteração / Melhoria / Outro), prioridade (Baixa→Urgente), departamento requerente, autor + data automáticos e anexos (upload direto para o Blob).",
      "**📋 Gestão de tickets em `/web/tickets`** — tabela com todos os tickets, estado, prioridade, responsável, datas, com pesquisa, filtros e ordenação; estado/prioridade/responsável editáveis inline. Estados: Novo · Em análise · Em desenvolvimento · A aguardar informação · Concluído · Fechado.",
      "**📊 Dashboard integrado** — KPIs (novos, pendentes, urgentes, concluídos, tempo médio de resolução) + breakdown por prioridade, por departamento e carga por pessoa.",
      "**🧵 Detalhe do ticket** com comentários internos e histórico/auditoria de cada alteração; o autor vê sempre o seu ticket, a equipa de Web vê todos.",
      "**💬 Integração com Slack** — ao criar um ticket (e em cada mudança de estado) é enviada uma notificação ao canal da equipa de Web com nº, título, departamento, prioridade, autor e link direto. Ativa-se com `SLACK_WEB_WEBHOOK_URL`; sem essa env é um no-op silencioso.",
    ],
  },
  {
    version: "74.38.1",
    date: "2026-06-22",
    title: "CuidaMais: real logo + brand colours + starting date",
    highlights: [
      "**🎨 CuidaMais now uses its real brand assets** — the «cuida+» wordmark logo pulled from cuidamais.pt and a brand palette built from the real colours (green #0A8015 + navy), replacing the placeholder.",
      "**📅 Starting date set to Monday 29/06/2026.**",
    ],
  },
  {
    version: "74.38",
    date: "2026-06-22",
    title: "New client: CuidaMais (André Pereira)",
    highlights: [
      "**➕ CuidaMais (Cuida+) added to the SEO roster**, assigned to **André Pereira**. Portuguese e-commerce for orthopedic / rehab / mobility products — website `cuidamais.pt`. Wired across the workspace: roster (synthetic SEO client + cache bump), consultant mapping, website, brand palette and an empty brief ready to fill.",
      "**💶 Billing set up in the SuperAdmin board:** semestral cadence, **9 600 € for 6 months paid upfront** (1 600 €/month equivalent), EUR, active. Starting date left blank until confirmed.",
    ],
  },
  {
    version: "74.37.3",
    date: "2026-06-22",
    title: "Fix: Pending Review table no longer clips the Task/Doc name on mobile",
    highlights: [
      "**📱 The Pending Review table now scrolls horizontally on phones instead of squashing the columns.** The table had no min-width, so on a narrow screen the left **Task** column collapsed to ~100px and the name got cut off. Added a `min-w-[920px]` so columns keep readable widths and the existing horizontal-scroll wrapper actually engages. Applies to both the internal consultant view and the client's public review page.",
    ],
  },
  {
    version: "74.37.2",
    date: "2026-06-19",
    title: "Weekly Update: only-good-news rule + 'Regenerar com instruções'",
    highlights: [
      "**🚫 The Weekly Update message is now strictly positive.** It never mentions pending, unfinished or delayed work and never uses 'confirmamos' / 'ainda não foi possível' / 'em falta' — only what was **completed this week** and what's **planned for next week**. The pending tasks are no longer fed to the model at all, so that phrasing can't leak in.",
      "**✍️ New 'Regenerar com instruções' button** next to *Regenerar*. Opens a box where the consultant adds extra context or tone tweaks (e.g. *'destaca o novo artigo do blog; tom mais caloroso'*) and regenerates the message following them — without ever breaking the only-good-news rule.",
    ],
  },
  {
    version: "74.37.1",
    date: "2026-06-19",
    title: "Fix: Weekly Update modal now opens as a true full-screen popup",
    highlights: [
      "**🐛 The 'Gerar Weekly Update' modal was rendering clipped inside the page header** (a transformed ancestor was breaking `position: fixed`). It now renders through a portal to `document.body` as a proper full-screen, centred overlay with background-scroll lock.",
    ],
  },
  {
    version: "74.37",
    date: "2026-06-19",
    title: "Roadmap: one-click 'Gerar Weekly Update' — WhatsApp ponto de situação for the client",
    highlights: [
      "**🟢 New 'Gerar Weekly Update' button (with a NEW! tag) on every client's roadmap board**, next to *Send current month* / *Send full roadmap*. It opens a modal that turns the roadmap into a short, ready-to-send **WhatsApp message in European Portuguese** — *Boa tarde! Segue o ponto de situação desta semana (SEO)…* — that the consultant copies straight into WhatsApp.",
      "**🧠 Pulled live from the roadmap where you are.** Claude reads what was **implemented this week**, what's **pending this week** (woven in as a soft caveat, like the real messages), and what's **planned for next week** — then rephrases the technical task titles into friendly, jargon-free client bullets. Editable before copying; Regenerar for a fresh take.",
    ],
  },
  {
    version: "74.36.1",
    date: "2026-06-17",
    title: "SuperAdmin → Roadmaps: jump to each client's Pending Review from the card",
    highlights: [
      "**🔗 Each client card now has a 'Pending review' link next to 'Open board'** — one click from the Roadmaps overview straight to that client's internal Pending Review table (`/seo/<slug>/review`). The card footer now carries both links instead of the whole card being a single link to the board.",
    ],
  },
  {
    version: "74.36",
    date: "2026-06-17",
    title: "Comment as Client or Wonder Ads on the internal review table + send the Client Brief for approval",
    highlights: [
      "**💬 The Client / Wonder Ads toggle now shows when commenting on the internal Pending Review table.** Previously a consultant was locked to 'Wonder Ads'; now you can flip the toggle to leave a comment as the Client's (e.g. when relaying their feedback verbally). It still defaults to Wonder Ads and never inherits a public visitor's stored choice.",
      "**📋 New 'Send for approval' on the Client Brief.** Just like Keyword Research + Target Keywords, you can now push the **Do's / Don'ts / Notes** straight to the client's Pending Review table — they open a clean, branded read-only version (with Download PDF + comments). Localised PT/EN; the button appears once the brief has content.",
    ],
  },
  {
    version: "74.35.1",
    date: "2026-06-17",
    title: "Consultant 'My Week' page polished — Portuguese, animated greeting, clearer cards",
    highlights: [
      "**🇵🇹 Fully in European Portuguese.** The whole per-consultant weekly overview (`/seo/roadmaps/<consultant>`) is now PT-PT — labels, stats, statuses, empty states.",
      "**⌨️ Animated greeting.** *Bom dia / Boa tarde / Boa noite, <nome>* now types itself out with a blinking caret, on the viewer's local clock.",
      "**🏷 Clearer cards.** The overdue + for-approval badges are grouped under a small *'De semanas anteriores'* label, and each card gains a **Ficha do cliente** link next to **Abrir roadmap**.",
    ],
  },
  {
    version: "74.35",
    date: "2026-06-16",
    title: "Roadmap approval — richer presentation, no internal diagnosis, + send just the current month",
    highlights: [
      "**🎨 The roadmap approval page got a proper redesign.** Instead of a plain markdown list, the client now sees a branded month → week → task layout: each week is a card with a gradient week badge + date, every task shown with its pillar chip and a colour-coded status pill, and the current week highlighted. Far more presentable when it lands in their inbox.",
      "**🧹 Removed the internal SEO diagnosis** from the client-facing roadmap — they just see the plan, as requested.",
      "**📅 New 'Send current month' button** on the roadmap board, next to 'Send full roadmap'. It sends only the 4 weeks of the month you're currently in — handy for month-by-month sign-off — as its own read-only preview the client opens from Pending Review.",
      "Both use the same shared report chrome (Download PDF, comments, footer) now that PublicReportView accepts a custom body.",
    ],
  },
  {
    version: "74.34.1",
    date: "2026-06-16",
    title: "Send Target Keywords to the client for approval (read-only, alongside the CSV)",
    highlights: [
      "**🎯 New 'Send for approval' button on the Target Keywords panel** (next to Refresh / CSV / Add). Besides exporting the CSV, you can now push the keyword list straight to the client's Pending Review table — the client opens it like any other deliverable.",
      "**🔒 Client-facing read-only view.** It renders the keyword targets as a clean, branded table (Keyword · Vol/mo · Difficulty · Intent, biggest opportunities first) with a Download PDF button — no editing, same chrome as the other previews. Localised PT/EN.",
      "Tidied up: the Target Keywords CSV now downloads as *Target Keywords - Client - Wonder Ads.csv* to match the rest of the deliverables.",
    ],
  },
  {
    version: "74.34",
    date: "2026-06-16",
    title: "Consultants get a private 'My Week' overview — this week's tasks across all their roadmaps",
    highlights: [
      "**🗓 New per-consultant weekly overview.** On the SEO board, a consultant now clicks their own name (column header) to open a private page showing EVERY one of their projects' roadmaps in one place — and, crucially, the tasks due THIS week across all of them, ordered so the top of each card is what to do first.",
      "**👋 Personal + time-aware.** The page greets you by name based on your local clock — *Good Morning / Good Afternoon / Good Evening, <name>* — and leads with a one-line summary (\"You have N tasks to tackle this week across M projects\").",
      "**📊 Pro card per project** (mirrors the SuperAdmin suite, with detail): health pill, Week N of 12 progress bar, and the numbered list of this week's tasks — each with its pillar + status (To do / In progress / For approval / Done). Plus quick badges for any overdue or for-approval work, and an Open-board jump.",
      "**🔒 Locked down.** Only the consultant themselves and the three SuperAdmins can open a given consultant's overview; everyone else gets Access Denied. The name is only clickable for the owner (admins see all).",
    ],
  },
  {
    version: "74.33.2",
    date: "2026-06-16",
    title: "Commercial DPT card now opens the Sales Dashboard in a new tab",
    highlights: [
      "**🤝 The Commercial DPT card on the workspace home now links straight to the external Sales Dashboard** (`sales-dashboard-production-a11b.up.railway.app`) and opens it in a new tab, instead of the placeholder internal page. The card's ↗ arrow already signalled an outbound link.",
    ],
  },
  {
    version: "74.33.1",
    date: "2026-06-16",
    title: "Downloads now have clean, branded filenames — 'Action - Client - Wonder Ads'",
    highlights: [
      "**🏷 PDFs were saving with the raw URL as the filename** (e.g. `wonder-ads-workspace.vercel.app_seo_ihn_actions_faq-section-generator_results_…`) with no mention of the company. Root cause: the print document renders a nested `<title>` the browser ignores, so 'Save as PDF' fell back to the URL.",
      "**✅ Fix:** every download now names the file **`Action - Client - Wonder Ads`**. Applied to the PDF (internal Download PDF + the client-preview Download PDF, by setting `document.title` before printing), the DOCX export, the Meta Tags CSV/DOCX, and the GMB Posts ZIP. Filenames are accent-stripped + sanitised so they're valid everywhere. e.g. *FAQ Section Generator - IHN - Wonder Ads.pdf*.",
    ],
  },
  {
    version: "74.33",
    date: "2026-06-16",
    title: "Send the SEO Roadmap to the client for approval — read-only, PDF-style",
    highlights: [
      "**📋 New 'Send for approval' button on the SEO Roadmap board.** Until now a generated 12-week roadmap lived only on the internal editable board — there was no way to share it with the client or archive a snapshot. Now the consultant clicks **Send for approval** and it lands in the client's Pending Review table like any other deliverable.",
      "**🔒 The client sees a read-only document, not the editable board.** Opening it shows a clean, branded report — the 12 weeks grouped by month, every task with its pillar + status, the SEO diagnosis up top — with a **Download PDF** button. No drag, no add-task, no editing; it reads like a PDF. Built on the same client-report chrome every other deliverable uses (`PublicReportView`), so comments/approval and the look-and-feel are consistent.",
      "**🇵🇹 Localised** — Portuguese for PT clients (Por iniciar / Em curso / Em aprovação / Concluído, Semana, Mês), English otherwise. The plan reflects the live roadmap, so re-sending after progress shows the updated statuses.",
    ],
  },
  {
    version: "74.32.2",
    date: "2026-06-16",
    title: "Fix: SuperAdmin → Roadmaps was double-counting 'for approval' tasks as 'overdue'",
    highlights: [
      "**🐛 The red 'Overdue tasks' block was inflated.** It counted every past-week task that wasn't `implemented` — which swept in the `pending_review` (For approval) tasks too. So a task waiting on the client showed up in BOTH the purple 'For approval' block AND the red 'Overdue' block. On White Clinic that meant Overdue read 14 when only 3 tasks were actually the consultant's backlog (the other 11 were sitting with the client for sign-off).",
      "**✅ Fix:** the three blocks are now mutually exclusive — 🟢 Done = `implemented`, 🟣 For approval = `pending_review`, 🔴 Overdue = past-week `not_started` / `in_progress` only. The consultant health pill (On track / Falling behind / Critical) is now driven by real backlog, not client-side waiting.",
      "Verified across all 18 live roadmaps — the correction removed the for-approval double-count on every card (e.g. hds-learning 10→0, sea-yourself 10→0, ihn 6→0, monte-mar 9→5, white-clinic 14→3). No data changed — only the counting.",
    ],
  },
  {
    version: "74.32.1",
    date: "2026-06-16",
    title: "Client preview now shows the full dashboard (Domain Intelligence + Core Web Vitals), not just the written analysis",
    highlights: [
      "**📊 The public client-preview page was missing the data dashboard.** When a result was sent to the Pending Review table, the client saw only the written analysis — the **Domain Intelligence** cards (authority, organic keywords, ETV, referring domains, top-ranked keywords), the **Core Web Vitals** table, and (for Keyword Research) the **Keyword universe** — were all absent, even though the PDF and the internal result page show them. The preview looked stripped-down vs. what was actually produced.",
      "**✅ Fix:** the preview now renders the same structured summaries the PDF does, above the analysis, in a clean light theme. Extracted the three summary blocks into a shared `report-summaries.tsx` used by BOTH the PDF (`PrintLayout`) and the client preview (`PublicReportView`) so they can never drift apart again.",
      "SEO Audit previews get Domain Intelligence + Core Web Vitals; Keyword Research previews get the Keyword universe summary. Existing results show it immediately — no regeneration needed.",
    ],
  },
  {
    version: "74.32",
    date: "2026-06-16",
    title: "Refine results with follow-up instructions — edit part of an output without regenerating the whole thing",
    highlights: [
      "**✏️ New 'Ajustar resultado' panel on every text result** (Write Blog Article, FAQ generator, content gap, outreach email, SEO Audit analysis, and every other prose action). Type a plain-language instruction — «reescreve apenas a secção X», «troca o link do CTA», «encurta a meta description» — and Claude returns the full document with ONLY that change applied; everything else is preserved verbatim. No more choosing between regenerating from scratch (and losing the good parts) or hand-editing Markdown.",
      "**↩️ Undo** — every refine keeps the prior version one click away («Desfazer último ajuste»).",
      "**🔒 Safe by design.** Refines stream the edited document live, then save in `refine` mode: the original run's structured data (audit metrics, Core Web Vitals, keyword pack/clusters) and the generated date are preserved, and a text tweak never re-triggers a DataforSEO keyword bill. ⌘/Ctrl+Enter to apply.",
      "Backed by a new `/refine` route with a surgical-editor system prompt (change only what's asked, keep the original language, output only the document).",
    ],
  },
  {
    version: "74.31.4",
    date: "2026-06-16",
    title: "Fix: Client Brief items showed a pointless 'show more' even when fully visible",
    highlights: [
      "**🐛 Fix — the `show more` / `show less` toggle on Do's / Don'ts / Notes appeared even when the whole item was already on screen.** The item span carried both `line-clamp-1` and `block` classes; `display:block` overrode the `display:-webkit-box` that line-clamp needs, so the text never actually collapsed — yet the overflow measurement still fired and rendered a toggle that did nothing.",
      "**✅ Fix:** dropped the per-item 1-line clamp and the toggle entirely. Brief items now always show their full text; the panels already scroll (`max-h` + overflow) so long lists stay tidy without hiding content behind a click.",
    ],
  },
  {
    version: "74.31.3",
    date: "2026-06-16",
    title: "Fix: PDF / public preview / DOCX were cutting the start of SEO deliverables at the first `---` rule",
    highlights: [
      "**🐛 Critical fix — the opening section of results was disappearing on export.** The PDF download, the public client-preview page, and the DOCX export all stripped the tool-progress prefix by slicing at the FIRST `\\n---\\n\\n` in the saved text. But SEO Audits, blog articles, the FAQ generator and most other actions use `---` as section rules INSIDE the analysis — so the slice cut everything before the first rule (e.g. the entire **Overview** of an audit). On the Sentir Saúde audit it was dropping the first ~1,700 characters.",
      "**✅ Fix:** new shared `extractAnalysis()` helper that only treats a `---` as the tool→analysis boundary when everything before it is genuine tool-progress blockquotes (`> …`). A `---` with real content before it (a heading, prose) is left untouched, so the full document survives. Applied in all four places that render a result — the live result page, the PDF print view, the public preview, and the DOCX export — replacing the duplicated buggy logic with one source of truth.",
      "Existing saved results are fixed retroactively — nothing needs regenerating; the parser was the only thing at fault, the stored text was always complete.",
    ],
  },
  {
    version: "74.31.2",
    date: "2026-06-16",
    title: "SEO board polish — full consultant names, PT tagline, header icon removed, FA → Core",
    highlights: [
      "**🧑‍🤝‍🧑 Consultant columns now show full names** — Fran. R. → **Fran. Rosa**, Yenisey R. → **Yenisey Rodriguez**, Manuel S. → **Manuel Silva**. Renamed consistently everywhere the name is used as identity (board grouping, admin Clients + Employees, Roadmaps, login chip, deliverable emails), with auto-migration so existing admin records carrying the old short names map across cleanly.",
      "**🇵🇹 SEO tagline translated to PT-PT:** «Crescimento orgânico no Google e nas IAs. Agência #1 de SEO & GEO em Portugal.»",
      "**🧹 Removed the gauge icon tile** from the top of the SEO department header for a cleaner look (matches the Web board).",
      "**🏷 Clínica Fernando Almeida moved from Growth → Core tier.**",
    ],
  },
  {
    version: "74.31.1",
    date: "2026-06-16",
    title:
      "Sentir Saúde & Clínica Fernando Almeida — onboarding briefs generated (Do's / Don'ts / Notes) from their forms",
    highlights: [
      "**📋 Do's / Don'ts / Notes drafted for both of André Pereira's new clients**, generated from their 2025/2026 onboarding questionnaires and seeded into `client-briefs.ts` — they now render in the Brief panel on each client's `/seo` page for André to review and refine.",
      "**Sentir Saúde** — captured the MBST / Premium Recovery strategic focus, the 3 clinical pillars (sem-cirurgia, fisioterapia pélvica, pediátrica), the national+international targeting, the forbidden sensationalist vocabulary, and the brand's tone + anchor sentence.",
      "**Clínica Fernando Almeida** — captured the service+area keyword structure (Gaia + Lisboa), the Google-reviews priority, the GA/GSC-not-yet-set-up gap, the planned WordPress migration to coordinate with the Web team, and the «empréstimo»/«barato» words to avoid.",
      "Onboarding form PDFs attached to each client's Onboarding panel so the keyword-research action reads them as live context.",
    ],
  },
  {
    version: "74.31",
    date: "2026-06-16",
    title:
      "New SEO consultant André Pereira + his first two clients (Sentir Saúde, Clínica Fernando Almeida); SEO Claude chat retired from the board",
    highlights: [
      "**🧑‍💼 André Pereira joins as a new SEO consultant.** Added everywhere the roster lives: a login account (`andre-pereira`, SEO dept), a **fourth column on the SEO board**, the SuperAdmin **Employees** roster (start date 15/06/2026), the admin **Clients** consultant dropdown, the **Roadmaps** per-consultant tracker, and the deliverable-email map (`andre.pereira@wonder-ads.com`) so his clients' PDFs/DOCX reply-to lands in his inbox. Distinct from the founder account `andre` (André Pavlenco).",
      "**🆕 Two new SEO clients onboarded under André Pereira — both Growth tier, started 15/06/2026:**",
      "**• Sentir Saúde** (sentirsaude.pt) — Fisioterapia Avançada e Osteopatia. Real brand logo pulled from their site, blue→navy brand palette, website wired up, and the **WordPress admin access** stored in the client's credentials vault (`wp-admin`, seo@wonder-ads.com).",
      "**• Clínica Fernando Almeida** (clinicasdentariasfa.pt) — Clínicas Dentárias Prof. Dr. Fernando Almeida. Logo pulled from the site (rendered on its dark brand panel), website wired up.",
      "**🧩 Both added as synthetic SEO clients** (via `EXTRA_SEO_CLIENTS`) so they appear across the whole workspace — board, client pages, action runners, admin — without needing a Notion child page yet. SEO-clients cache key bumped to v5 so they show immediately.",
      "**🤖 SEO Claude chat retired from the board.** Removed the in-house AI chat widget from the SEO department page per request; the client columns now span the full width (up to 4 across on large screens). The `/api/chat` backend is untouched.",
      "**📋 Do's / Don'ts / Notes + onboarding forms for the two new clients are pending** — the onboarding PDFs couldn't be located, so the briefs are seeded empty and will be generated once the forms are provided.",
    ],
  },
  {
    version: "74.30",
    date: "2026-06-16",
    title:
      "WEB DPT board polish — search, employee filters, Refresh, default start date, and a Claude-powered Generate Backlog (Slack-ready, Portuguese)",
    highlights: [
      "**🔎 Search + employee filters.** A search bar at the top of the board filters projects by name or client as you type. Next to it, quick filter chips — **All · Mike · Gustavo · Renan** — narrow the board to one designer's projects. The chips list ONLY the three web designers; SEO consultants (who can open the dept but don't build sites) are no longer offered as assignees or filters.",
      "**🔄 Refresh button** re-pulls the board from KV without a full page reload, so you see teammates' latest moves instantly.",
      "**🧾 Generate Backlog (new).** A board action that drafts the weekly Slack backlog for you. It reads every project's most recent comments + notes and asks Claude (Sonnet 4.6) to compose a Portuguese, Slack-ready post — grouped by client, one line per project/page, with `@Mention` (full names: Mike Nobre / Gustavo Rotini / Renan Alves / André Pavlenco) and the right status emoji (:white_check_mark: / :large_green_circle: / :large_yellow_circle: / :red_circle: / :double_vertical_bar: / :rotating_light:). It auto-prepends `Backlog web DD.MM` and appends the fixed bilingual footer. The result opens in an editable box — tweak anything, then one-click **Copy**. **Regenerate** re-drafts from the latest data.",
      "**🗓 New projects default to today's start date** instead of an empty field.",
      "**🅰 Card titles are bigger + bolder** so the project name is the first thing you read and easy to scan.",
      "**🎨 Header cleanup.** Dropped the icon tile and switched the tagline to *“Departamento Web Design & Development. Agência #1 de SEO em Lisboa”*. Activity log now sits with the other action buttons in the top-right toolbar.",
    ],
  },
  {
    version: "74.29",
    date: "2026-06-16",
    title:
      "WEB DPT is now a real product — Kanban project board, project pages, role-based access, activity log, and a per-project encrypted credential vault",
    highlights: [
      "**🗂 The placeholder is gone — WEB DPT is a working board.** The old six 'capabilities' cards (Landing Pages, WordPress Sites, …) are replaced by a live Kanban board backed by Vercel KV. Five columns in pipeline order — **Negotiation / Not Started → In Progress → Client Feedback → Migration → Done** — and every web project is a card you drag freely between them. The column the card sits in IS the project's status, so moving a card to 'Done' is the status change. Empty columns show a 'Drop a card here' hint; each column header carries a live count.",
      "**🪪 Card front shows everything at a glance.** Project name, a coloured status tag (matching the column), priority chip (Low/Med/High, white→amber→rose), client name, assigned web designer, start date, target launch date, and a 'Updated …' timestamp on every card. Create new projects from the **+ New project** button — a modal captures name, client, assignee, priority, starting column, start + launch dates.",
      "**📄 Each card opens a dedicated project page (`/web/<id>`).** Full detail view with: an editable **Project details** card (name, client, designer, priority, status, dates — all editable in place, 'last updated' stamped on save); a **Status updates & comments** thread where the team posts timestamped updates, each tagged with the author from their session (authorship is server-stamped — it can't be spoofed from the client); and the full asset vault on the right. Projects can be created, edited, reassigned, moved, and deleted entirely inside the app.",
      "**🔐 Per-project asset & credential storage.** Every project page stores: freeform **Notes**, **Do's & Don'ts** (two-colour list editor), **Branding kit** (link + uploaded files), **Onboarding web form** (link + files), general **Files & attachments** (upload-from-computer via Vercel Blob, any file type), **Other resources** (labelled links), and an **Access credentials** vault.",
      "**🛡 Credentials are encrypted at rest, masked, and role-gated.** WordPress / hosting / FTP / domain / database logins are stored AES-256-GCM-encrypted in KV — the plaintext password NEVER leaves the server in the projects API, only a `hasSecret` flag does. Secrets render as `•••••••••••` with a **Reveal** button that calls a dedicated server endpoint to decrypt one credential on demand; every reveal is written to the activity log as a security trail (who unmasked which credential, when). The encryption key is derived from the existing `AUTH_SIGNING_SECRET` via scrypt, so there's nothing new to provision on Vercel.",
      "**👥 Role-based department access.** New `accessibleDepts()` rule in the credential table: **SEO consultants can access both the Web Dept and the SEO Dept**; **Web designers can access the Web Dept only**; SuperAdmins + Founder see everything. The SEO landing page now shows a 'No SEO access' card to Web-only users, and the Web pages + every `/api/web/*` route enforce Web access (web designers, SEO consultants, admins).",
      "**🧑‍🎨 Three web designer accounts created — Mike, Gustavo, and Renan.** Added to the login table with scrypt-hashed passwords (Web Dept only). They can be assigned to projects from the board's assignee dropdown alongside the SEO consultants. Andre has the plain passwords to hand out.",
      "**📜 Activity log (`/web/activity`).** A history view across ALL web projects: status moves between columns, field edits, new comments, asset/credential changes, project creation + deletion — each with the actor's name, a human-readable message, a timestamp, and a link back to the project. Capped to the most recent 500 events.",
      "**🧱 Under the hood.** New `web-projects-store.ts` (KV CRUD + activity log, every write run through a `normaliseProject` sanitiser so a bad payload can't corrupt the blob), `web-creds.ts` (the AES-256-GCM helper), and a client-safe `web-shared.ts` (enums, labels, status/priority colours, browser-facing types — kept free of server imports so 'use client' components can import it). The public serializer strips ciphertext before anything crosses to the browser.",
    ],
  },
  {
    version: "74.28",
    date: "2026-06-12",
    title: "Client Files panel accepts ANY file type + every SEO action now reads the library as live context",
    highlights: [
      "**📂 Client Files: upload anything from your computer.** The panel on the SEO + ADS client pages used to gate uploads to `image/*` + a handful of video MIME types — both in the `<input type=\"file\" accept=\"…\">` filter on the picker AND in the server-side `allowedContentTypes` allowlist on `/api/files/upload`. PDFs, Word docs, spreadsheets, CSVs, archives, anything else were silently rejected at the browser before the picker even closed. v74.28 drops the `accept` attribute so the OS file picker shows everything, and widens the server allowlist to `image/* · video/* · audio/* · application/* · text/*` (wildcards Vercel Blob supports natively). The 200MB per-file cap stays as the only ceiling. Internal app, workspace-login gated — no need for a tight allowlist.",
      "**📑 New `document` kind in the library.** PDFs / DOCX / XLSX / PPTX / CSV / TSV / TXT / MD / JSON / XML / ZIP / RAR / 7Z are now detected by extension and render with a brand-gradient `FileText` tile (\"Open document → external link\") instead of falling through to the generic \"link\" tile. The list-endpoint sanitiser (`/api/files/[slug]`) now accepts `document` alongside `image/video/link` in `VALID_KINDS` so old records (any unknown new types) won't get stripped. Detection regexes also picked up modern image (HEIC/HEIF) + video (AVI/MKV) extensions while we were in there.",
      "**🗣 Empty-state copy refreshed:** *\"No files yet — upload anything from your computer (images, videos, PDFs, Word docs, spreadsheets, CSVs, archives, etc.) or paste a Google Drive link. Files stay in sync across the SEO & ADS departments and every SEO action reads them as live context.\"* — accurate to what's actually accepted now AND tells the consultant the files are doing work in the prompt, not just sitting there as decoration.",
      "**🧠 Every SEO action now reads the Client Files library before generating.** Extended `getLiveClientContext()` (the v74.25 helper) to also load the per-client files list, run `extractFromUrl()` (v74.26-fixed PDF extractor) on the first 6 text-style files in parallel, and surface them in the same `## Live client context` block that already carries Brief / Onboarding / Target Keywords / Geo. Per-file extract cap: 6,000 chars (truncation is shown to Claude with a `…[truncated — N chars total, 6k shown]` marker). Images, videos, and links render as a `name + URL` one-liner so Claude knows the asset exists and can reference it in the output. This auto-applies to all 17 fall-through actions via the existing v74.25 routing — header-tags, image-alt-text, internal-linking, schema-markup, content-gap-analysis, faq-section-generator, refresh-existing-content, content-calendar, blog-roadmap, outreach-email, gmb-reviews-responder, gmb-profile-audit, backlink-directories, competitor-backlink-gap, broken-link-building, digital-pr-angles, local-citation-check.",
      "**🧠 Meta Tags (`/meta-generate`) also reads Client Files** — the specialised runner doesn't go through `/route.ts`, so it doesn't auto-pick-up the live context. v74.28 imports the new `loadClientFilesWithExtraction()` + `formatClientFilesBlock()` exports from `live-client-context.ts` and threads them through the per-chunk `buildPrompt` call so meta-tag generation grounds against the library too. Progress log emits `✓ Client Files library: N file(s), M with extracted text — inlined into the Claude prompt.` so the consultant sees what landed.",
      "**🪪 Progress line tells you what was loaded.** The streamed `> ✓ Live context: …` line for every action now also reads `N client file(s) (X images, Y documents, Z videos…; M with extracted text)` when there are files on file. So you can tell from the progress log whether Claude got the brief deck PDF that's sitting on the client page.",
      "**🪶 Token budget stays sane.** 6 files × 6k chars = ~36k chars max extracted text per call (~9k input tokens), on top of the existing 12k onboarding budget. Comfortably inside Sonnet 4.6 and Haiku 4.5 input limits. Beyond the 6-file extraction cap, the rest are listed by name + URL only — Claude still knows they exist without us racing the action timeout on a 30-PDF library.",
      "**🛑 Phase C from the v74.26 plan still NOT in scope** — native PDF attachment to every action would cost ~50-100k tokens per PDF per call. The text-in-prompt approach delivers comparable signal at ~5% the cost. `write-blog-article` keeps its existing native-PDF flow for layout fidelity.",
    ],
  },
  {
    version: "74.27",
    date: "2026-06-11",
    title: "SuperAdmin → Roadmaps — per-consultant SEO project tracker showing weeks elapsed, tasks done, pending approvals, and overdue backlog at a glance",
    highlights: [
      "**🧭 New SuperAdmin block: Roadmaps.** Sits alongside Projects and Employees on `/admin`, same card pattern (brand-gradient border, Lucide `Compass` icon, count badge in the corner). Count is the live number of SEO clients with a roadmap on file — pulls every `roadmap:current:<slug>` from KV in parallel, falls back to 0 silently if Notion / KV is unavailable so the landing page never breaks.",
      "**👥 `/admin/roadmaps` — one section per consultant.** Groups every SEO client by Head Consultant (re-resolved live from slug via `getConsultantForSlug`, never trusting cached `consultant` fields — sidesteps the v74.x cached-rename trap). Sections render in the canonical `CONSULTANT_ORDER` (Fran. R. → Yenisey R. → Manuel S.), each headed with the consultant name, work email (mailto), client count, % with roadmap, average current week, and roll-ups for pending approvals + overdue across past weeks.",
      "**📊 Per-client card shows exactly what Andre asked for.** For every client in a consultant's book: client name + slug, health pill (`On track` / `Falling behind` / `Critical` / `Not started` / `No roadmap yet`), a brand-gradient progress bar showing `Week N of 12`, total `done / total` tasks, then three stat tiles in a row — **Done past weeks** (emerald), **For approval** (violet — stuck with client), **Overdue tasks** (white/amber/rose by severity) with a sub-line showing how many distinct past weeks the overdue work spans. Onboarded date + Cycle-start date footer chip when the roadmap has them. Click anywhere on the card → opens `/seo/<slug>/roadmap` for that consultant to fix it.",
      "**🚦 Health thresholds match the existing in-board warnings** so the SuperAdmin view never disagrees with what the consultant sees on their own board: `critical` = 5+ overdue past-week tasks (same as `computeWarnings` rose-pill threshold), `behind` = 2+ overdue (same as `FALLING_BEHIND_TASK_THRESHOLD`), `on-track` otherwise. `not-started` for week-0 roadmaps; `no-roadmap` when the slug has no `roadmap:current:` record at all (still rendered so Andre can spot gaps in the consultant's book).",
      "**🪜 Roster totals strip across the top:** SEO clients · With roadmap · Consultants active · Pending client approval · Overdue tasks. The last two flip colour when non-zero (amber for any waiting, rose when overdue ≥ 10) so the page reads at a glance even before scrolling into individual sections.",
      "**🧠 Sort logic inside each consultant section: critical → behind → no-roadmap → not-started → on-track**, then alphabetical by client title within each bucket. So the consultant's most-bleeding clients are always at the top of their column and Andre never has to hunt.",
      "**🏗 Architecture stays clean.** `src/lib/roadmap-admin-helpers.ts` is the single source of truth — exports `getRoadmapAdminSummary()` (full grouped data + totals) and `countRoadmaps()` (lightweight for the landing badge). `src/app/admin/roadmaps/page.tsx` is a thin server component (gated by the existing `/admin/layout.tsx` admin-only guard, no new boilerplate). `src/components/admin-roadmaps-panel.tsx` is the pure-presentational panel. Adding new card types or per-consultant slices later is a one-file change.",
      "**🗓 DD/MM/YYYY dates everywhere** — uses `formatDate()` from `src/lib/dates.ts`, never inline `toLocaleDateString` (per the existing app convention).",
      "**🧪 Verified on localhost** with all 17 SEO slugs across the 3 SEO consultants — A. Domingos rendered correctly as `On track · Week 2 of 12 · 8/51 done · 6 done past weeks · 0 for approval · 0 overdue · Cycle start 03/06/2026`. Page weight ~328kb HTML, server-render time ~1.5s on the first hit (cached afterwards for the same request cycle).",
    ],
  },
  {
    version: "74.26.2",
    date: "2026-06-11",
    title: "Meta Tags diagnostics now actually stick on screen — bundled into the final error AND rendered in a sticky block in the runner UI",
    highlights: [
      "**🪶 v74.26.1 added the diagnostics but they disappeared on screen.** Each `🔍` line streamed as a `progress` event, and the runner UI did `setPhaseMessage(evt.message)` — meaning every new event overwrote the previous one. By the time the run ended, all the diagnostic lines had been replaced by the final `\"Crawling 9 pages…\"` or similar, and the user just saw the generic error without any of the underlying reasons.",
      "**🪢 Server-side: diagnostics are now captured into a buffer AND appended to the final error message.** The `emitDiagnostic` callback in `meta-generate/route.ts` now does two things: stream the line as a progress event (existing v74.26.1 behaviour), AND push it onto a `diagnostics: string[]` buffer. When the run fails with `drafts.length === 0`, the final error message includes a `Diagnostics for each failed retry layer:` block listing every line. So even if the UI throws away the live progress events, the saved error text has everything inline — which means navigation, refresh, and re-render all preserve it.",
      "**👀 UI-side: `<MetaTagsRunner>` now keeps a sticky `diagnostics: string[]` state.** Progress events whose message starts with `🔍 ` are sliced and appended to that state (in addition to the existing `phaseMessage` overwrite). When `status === 'error'` and the buffer is non-empty, the failure block renders a `🔍 Diagnostics (N retry layers)` panel below the error message — bulleted, monospace, breakable so long Anthropic response bodies don't overflow the column. So you see the full retry-layer history without having to read the error string itself.",
      "**🪟 Error message div now renders newlines** (`whitespace-pre-wrap` + `leading-relaxed`) so the server-side diagnostic block (newline-separated bullets) renders correctly. v74.26.1's error text collapsed everything to a single line.",
      "**🕊 When no diagnostics were captured at all** (the failure happened before any retry layer reported back — which itself is a tell that the whole `Promise.all` call rejected early, e.g. a model-not-found or an env-var issue), the error appends a soft note: `(No diagnostics captured — almost certainly an Anthropic API outage; retry in a minute.)` so the consultant isn't left guessing.",
      "**No behaviour change** — same retry logic, same Zod schema, same prompt. Only the surfacing of the existing diagnostics changed.",
    ],
  },
  {
    version: "74.26.1",
    date: "2026-06-11",
    title: "Meta Tags chunk-failure diagnostics now stream to the consultant — no more 'check Vercel logs' for retry layer errors",
    highlights: [
      "**🔍 Every meta-generate retry failure now emits a streaming `🔍` diagnostic line.** Until v74.26.1 the three retry layers in `generateChunkWithRetry` (generateObject → retry → generateText salvage) logged failures via `console.warn` only — diagnostics landed in Vercel logs but the consultant on the action page just saw `Generation produced no rows after three retry layers (generateObject, retry, generateText salvage). Check Vercel logs for the underlying Claude error.` That meant every failed run required Andre to context-switch into Vercel just to see whether it was an Anthropic overload, a schema mismatch, or a content-policy block — and the user couldn't see what was happening at all.",
      "**🩺 New `formatDiagnostic(err)` helper probes the AI SDK error shape and surfaces every useful field on one line:** error `name` (`AI_APICallError` / `AI_NoObjectGeneratedError` / `AI_TypeValidationError`), `statusCode` (HTTP 429 rate limit, 529 overloaded, 400 bad request), `responseBody` excerpt (first 240 chars — contains Anthropic's `\"type\":\"overloaded_error\"` markers and the human-readable message), and for schema failures the first 2 Zod issues (e.g. `rows.0.optimizedTitle: String must contain at least 5 character(s)`). Plus the raw model output head (first 160 chars) when Claude returned prose instead of JSON.",
      "**🪜 Per-layer diagnostics — you can now tell exactly which layer failed.** Each retry attempt streams its own diagnostic: `chunk 2 · generateObject attempt 1 → AI_APICallError | HTTP 529 | body: {\"type\":\"error\",\"error\":{\"type\":\"overloaded_error\"…`. The salvage layer emits its own three flavours of detail: (a) `recovered 8/10 rows (2 rows dropped: rows.0.url=Required; rows.1.optimizedTitle=String too short)` on partial success, (b) `no valid JSON in 3,421-char response. Head: \"I cannot generate meta tags for…\"` when Claude refused, (c) `8 rows in JSON, ALL failed Zod (top reasons: …)` when JSON parsed but every row was bad.",
      "**🚨 Final error message now points at the diagnostic lines** instead of Vercel: `Generation produced no rows after three retry layers across ALL 3 chunks. The 🔍 diagnostic lines above name the underlying error for each layer (HTTP status, Anthropic error type, Zod issue, raw model excerpt). Most common cause is Anthropic API overload (HTTP 529 overloaded_error) — retry in a minute. If the diagnostics show schema/Zod issues instead, the prompt may need tightening; ping Claude.` Tells the consultant what to do next based on what the diagnostics revealed.",
      "**🪵 Vercel-side logs unchanged** — the existing `console.warn(\"[meta-generate] chunk N attempt M failed: …\")` calls are kept, now with the formatted diagnostic appended. So Vercel logs still have everything for offline debugging, AND the consultant sees it live in the action stream. No information loss either way.",
      "**No behaviour change.** The retry logic + salvage parser + Zod schema are byte-for-byte identical to v74.26. Only the error-reporting surface area changed. Existing successful runs see no new output.",
    ],
  },
  {
    version: "74.26",
    date: "2026-06-11",
    title: "Onboarding PDF extraction unblocked — 14/15 forms backfilled with 155k chars of content + competitor lists",
    highlights: [
      "**🐛 Root-cause fix for the silent onboarding-extraction failure.** Until v74.26, the upload route called `extractFromUrl(url, contentType)` inline (`PUT /api/onboarding/[slug]/route.ts:90`) and `extractText(buf, { mergePages: true })` against the raw PDF buffer. On Node 20.6+ (local + Vercel runtime) that path goes through worker postMessage → Node's structuredClone, which throws `Unable to deserialize cloned data` on EVERY upload. The throw was caught by the upload route's deliberately-non-fatal try/catch (\"extraction failed (non-fatal)\") and the doc saved with empty extractedText, leaving consultants with onboarding forms \"on file\" that Claude could never actually read. The one-line fix in `src/lib/pdf-extract.ts`: pass the `PDFDocumentProxy` to `extractText` (which the type signature already supported) instead of re-passing the buffer through the broken worker path. The proxy is already loaded for `numPages` so there's no extra fetch cost. Verified on IHN's 15-page 202kb PDF: 0 chars → 18,981 chars in 290ms.",
      "**📚 Backfill of all 15 onboarding records run against prod KV — 14/15 succeeded.** Total 155,004 chars of extracted text now in KV across the roster (avg ~10k chars/client, range 3,416 → 18,981 chars), plus 36 mined competitor domains. Per-client breakdown: `a-domingos` 8.1k chars · `aeger-prima` 15.3k · `b-life` 10k · `cdt` 12.4k · `clinica-em-casa` 9.1k · `clinica-mimus` 18.4k · `hds-learning` 14.9k · `ihn` 19k · `monte-mar` 3.4k · `safe-away` 9.1k · `sea-yourself` 10.6k · `senior-resort` 8.1k · `spine-center` 9.1k · `white-clinic` 7.5k. Every SEO action that consults the onboarding form (which after v74.25 is ALL of them via the live-client-context helper) now sees real form content for these 14 clients on the next run — no UI step needed by the consultant.",
      "**🟥 The exception: `fisio-restelo`.** Its onboarding PDF is 1992kb — ~10× the size of the others — and the 10-page extraction returned 0 chars even with the fix. The PDF is image-based (scanned) rather than text-based; `unpdf` can't OCR. Two paths to resolve: (1) re-upload it as a text-based PDF (export from Google Forms / re-print to PDF as text rather than scanning), or (2) add a one-off Claude vision pass to OCR the scanned pages. Option 1 is cheapest and ships fastest. Until then, fisio-restelo's action runs will show the v74.25 honest-reporting line `onboarding form on file but extraction empty — Claude can't read its contents (re-upload to fix)` so the consultant knows.",
      "**🧹 Competitor mining noise filter widened.** The post-extraction `mineCompetitors` regex was already filtering wonder-ads.com + a few Google domains. v74.26 adds the social platforms (`facebook.com`, `instagram.com`, `linkedin.com`, `youtube.com`, `twitter.com`/`x.com`, `tiktok.com`, `pinterest.com`, messaging apps) + Google Drive (`drive.google.com` — Google Forms hosts attachments there, so it showed up as a \"competitor\" on every form) + `goo.gl`. Pre-fix: IHN had 4 \"competitors\" (one was drive.google.com, one was facebook.com). Post-fix: 2 real competitors. Across the roster, total competitor count dropped from 40 (with noise) to 36 (clean). Every action that names competitors in the prompt now gets a tighter list.",
      "**📏 Onboarding inline-text budget bumped 4000 → 12000 chars in `live-client-context.ts`.** Default `onboardingExcerptChars` was 4000 — set conservatively when extraction was assumed broken. Now that extraction works and the average extracted form is ~10k chars, 12000 captures the ENTIRE form for ~10/15 clients (the small ones) and the full meaty middle of the bigger ones (services, audience, brand voice, goals, competitor lists). Token cost: ~3k tokens per call — well inside Claude budget. Result-page actions like header-tags, internal-linking, schema-markup, faq-section-generator that fall through the generic runner now get this richer context automatically through the v74.25 live-context helper.",
      "**🛑 Native PDF attachment NOT extended in this PR.** Phase C in the original audit plan was \"attach the onboarding PDF natively for more actions\" (write-blog-article already does it). With extraction now working and shipping 12k chars per call at ~3k tokens, the text-in-prompt approach delivers comparable signal at ~5% the token cost of attaching a 200kb PDF natively (~50-100k PDF tokens). Across 21 actions × 15 clients × an avg ~5 runs/month, that's tens of dollars/month saved with negligible quality loss. write-blog-article keeps its native-PDF flow for layout fidelity on the longest-form generation. Other actions can opt in later per-action if the v74.26 text-in-prompt approach proves insufficient on some specific output.",
    ],
  },
  {
    version: "74.25",
    date: "2026-06-11",
    title: "Live tables principle applied to every SEO action — Target Keywords + Onboarding + Geo now consulted on 17 actions that were brief-only before",
    highlights: [
      "**📚 The live-tables principle, now enforced across every action.** v74.24 fixed Meta Tags + manual Target Keyword enrichment. v74.25 extends the same discipline to every other action. Until v74.25, the generic `/api/seo-actions/[clientSlug]/[actionSlug]/route.ts` runner had explicit branches for 4 actions (keyword-research, write-blog-article, monthly-report, client-roadmap) that loaded Onboarding + Target Keywords + Geo — and 17 OTHER actions fell through and only got the Brief through the system prompt. So header-tags couldn't see which keywords the consultant was actually targeting; image-alt-text didn't know the brand voice; faq-section-generator had no idea what Target Keywords to anchor questions to; gmb-reviews-responder couldn't read the client's voice off the onboarding form. None of them HARD-FAILED — they just produced weaker output than they should have because the live tables weren't in the prompt. v74.25 changes the default: every action now consults all four live tables.",
      "**🧱 New shared helper `src/lib/live-client-context.ts`.** Single source of truth: `getLiveClientContext(slug)` fetches Onboarding + Target Keywords + Geo in parallel (Brief stays in the system prompt builder); `formatLiveClientContextBlock(ctx, opts)` renders them as a focused markdown section ready to drop into the user-message factPack; `summariseLiveContextForProgress(ctx, opts)` produces the short `> ✓ Live context: …` line streamed to the consultant. Opts allow per-action skips so actions whose specialised branch already pulled one of the sources don't see duplicate content (e.g. `write-blog-article` passes `skipOnboarding: true` because its existing branch attaches the onboarding PDF natively).",
      "**🔌 Wired into the generic runner — one `else` block, zero touched existing branches.** The augmentation lives after the existing if/else chain in `route.ts` and only appends to `factPack` (never overwrites). A per-action `liveContextOverride` map declares which actions opt out of which sections (KW research generates Target Keywords, so it skips that section entirely; monthly-report + client-roadmap skip everything because they already do the same work themselves; write-blog-article skips onboarding only). All other 17 actions get the full live block by default. Wrapped in try/catch so a KV blip never crashes the action — falls back to brief + inputs with a visible warning.",
      "**📦 Actions newly upgraded to live-tables (17):** `seo-audit` · `header-tags` · `image-alt-text` · `internal-linking` · `schema-markup` · `content-gap-analysis` · `backlink-directories` · `outreach-email` · `competitor-backlink-gap` · `broken-link-building` · `digital-pr-angles` · `gmb-profile-audit` · `local-citation-check` · `gmb-reviews-responder` · `content-calendar` · `blog-roadmap` · `refresh-existing-content` · `faq-section-generator`. Every run of any of these now opens with a `> ✓ Live context: <N target keywords> · <onboarding status> · <country / lang>` line so the consultant sees exactly what Claude got. Already-live actions (`keyword-research`, `write-blog-article`, `monthly-report`, `client-roadmap`, `gmb-posts`, `meta-title-description`) are unchanged.",
      "**🪪 Honest progress reporting.** The Onboarding line goes one of three ways: (1) full text + competitors → `onboarding form on file (filename.pdf)`; (2) form uploaded but extraction failed AND no competitors → `onboarding form on file but extraction empty — Claude can't read its contents (re-upload to fix)`; (3) no form → omitted entirely. Same with Target Keywords — only shown when the table has rows. Means the consultant knows whether the form is actually doing useful work in the prompt, or whether to re-upload. Caught during smoke-test against IHN's PDF which had extractedText length 0.",
      "**🧪 Verified end-to-end on localhost** against IHN (Canada/en, 27 Target Keywords with 2 enriched, onboarding PDF with empty extraction). The header-tags action stream opened with the correct live-context line before any Anthropic call, confirming all four tables were read live and the factPack composition logic works under real KV data.",
    ],
  },
  {
    version: "74.24",
    date: "2026-06-11",
    title: "Live tables for actions — manual target keywords auto-enrich from DataForSEO + Meta Tags reads the live Target Keywords table",
    highlights: [
      "**🔍 Manually-added Target Keywords now auto-enrich from DataForSEO.** Until v74.24, hitting the **+ Add** button on a client's Target Keywords table stored the bare keyword string and rendered `—` in every other column (Vol/mo, KD, Intent) forever. Spotted on the IHN board where a 11-keyword Canada-targeted manual paste came back empty in every metric column. Fix: the `POST /api/target-keywords/<slug>` handler now calls `enrichKeywordsComprehensive()` against any incoming keyword that's missing `searchVolume`, `difficulty`, or `intent` — same 3-endpoint chain (DataForSEO Labs `keyword_overview` → `bulk_keyword_difficulty` → Google Ads volume) that the bulk Keyword Research save uses. Geo comes from `getClientGeo(slug)` so IHN runs against Canada (2124, en), Insync Design against Australia (2036, en), HDS Learning against Brazil (2076, pt), everyone else against Portugal (2620, pt). Best-effort: if DataForSEO is down or rate-limits, the keyword still saves bare rather than the add 500ing — so the user never loses input.",
      "**🧠 Meta Tags now reads the LIVE Target Keywords table — no more 'No Keyword Research result found'.** v73.x hard-blocked the Meta Titles & Descriptions action with `No Keyword Research result found for this client. Run Keyword Research first…` whenever the consultant hadn't run a Keyword Research action yet. That broke the IHN flow: 11 keywords were sitting RIGHT THERE in the Target Keywords table (added manually) and the action refused to use them because they hadn't come from a historical Keyword Research result artifact. v74.24 inverts the source-of-truth: the action now reads `listTargetKeywords(slug)` FIRST (the live editorial wish-list the consultant maintains), falls back to the last Keyword Research history clusters only if Target Keywords is empty, and only errors when BOTH are empty. When Target Keywords is the source, the rows get grouped by intent (Commercial → Transactional → Informational → Navigational → Other) into a synthetic `KwCluster[]` so the existing Claude prompt builder consumes them unchanged. The progress log now reads `✓ Keyword source: Target Keywords table (11 live keywords, 11 enriched).` instead of `✓ Found Keyword Research from 2026-05-12…`.",
      "**🪪 Live-tables principle for actions, stated explicitly.** Every existing action already pulled Brief (Do's/Don'ts/Notes), Onboarding form, and Geo fresh per request via `getBriefForSlug` / `getOnboardingForSlug` / `getClientGeo` — those have always been live. Keyword Research was the lone exception that read from a frozen historical result artifact. After v74.24, all four consultant-managed surfaces (Do's/Don'ts, Notes, Onboarding, Target Keywords) are pulled live per request for every action that consumes them. The principle: actions read what the table currently says, never what the app generated last time.",
      "**🧹 Removed `src/lib/changelog 2.ts`** — another Finder-duplicate snuck in via `git add -A` since the v74.23 push (same pattern as a642f65). One-line cleanup so the workspace bundle doesn't ship two changelog modules.",
    ],
  },
  {
    version: "74.23.3",
    date: "2026-06-09",
    title: "Roadmap onboardingDate field + violet pending-review tint + B-life reset to a Week-6 startDate",
    highlights: [
      "**📅 New `onboardingDate` field on Roadmap.** Distinct from `startDate`: when a roadmap is reset / regenerated partway through the engagement, `startDate` moves to the Monday of the new Week 1 while `onboardingDate` stays pinned to the original agency-engagement date. Surfaced as a small muted chip on the board top bar — `Onboarded · 09/02/2026` — so the consultant always has the historical anchor even after a reset. Schema-normalised in `roadmap-store.ts` so older roadmaps without it just don't render the chip; nothing breaks.",
      "**♻️ B-life startDate reset to 2026-05-04** (the Monday 5 weeks before 2026-06-09). With the existing week formula `floor((now - start) / 7) + 1` that puts the board's current week on Week 6 — which is where B-life actually is. The original 2026-02-09 onboarding date is preserved on the new `onboardingDate` field so the chip still reads `Onboarded · 09/02/2026`. Old roadmap (the one with `startDate: 2026-02-09`) was auto-archived to `roadmap:archive:b-life` by the idempotent seed script before the overwrite, so it's recoverable if needed.",
      "**🟪 Pending-review chip shifted from sky to violet.** v74.23.2 flipped the colours so YELLOW = ongoing and BLUE = pending review — correct mapping, but the sky-blue chip read as straight cyan, too far from the FigJam VIOLET. v74.23.3 keeps the same hue family but slides it up to `violet-500/15` (background) + `violet-400/45` (border) + `violet-100` (chip text), with chip background bumped to `/20` so the contrast against the dark workspace stays good. Closer to the FigJam swatch without losing the 'pending = cool colour' mental model.",
    ],
  },
  {
    version: "74.23.2",
    date: "2026-06-09",
    title: "Roadmap board UX — status colours match FigJam, current week/month pop, SEO diagnosis collapsed, falling-behind warning breaks down the why",
    highlights: [
      "**🎨 Status chip colours flipped to match the FigJam legend.** `in_progress` was sky-blue and `pending_review` was amber — that was the OPPOSITE of how Andre paints the FigJam (YELLOW = ongoing, BLUE/VIOLET = pending client review). Now `in_progress` is amber (yellow chip) and `pending_review` is sky-blue. Emerald (done) + white (not started) unchanged. Source of truth lives in `STATUS_META` in `roadmap-board.tsx` — both the card background AND the chip flip together so a consultant scanning the board sees the same colours their FigJam plan used.",
      "**📍 Current week + current month now actually pop on the board.** Previously the only signal that you were in Week 6 was a tiny `NOW` pill on the column — easy to miss on a long board. v74.23.2 adds: (1) a brand-gradient 2px strip across the top of the current week column, (2) a `▶` arrow before the week title, (3) a real brand-gradient pill `● THIS WEEK` (pulsing dot) instead of the muted purple pill, (4) a much heavier brand-purple ring + outer glow on the column itself. The Month label that contains the current week ALSO switches from a thin outline pill to the full `brand-gradient-bg` pill with a pulsing white dot + `NOW` chip, and the surrounding connector lines brighten from `bg-white/10` to `bg-white/25`. Net: you can tell where you are without reading dates.",
      "**🧠 SEO diagnosis is now collapsed by default.** The long auditSummary paragraph used to render every time the roadmap loaded — fine the first time, busy on every subsequent visit. Now it's a small `✨ SEO DIAGNOSIS (CLAUDE) · 3 photos · ▾` pill that expands the diagnosis card on click. State is per-mount (no persistence) so consultants who DO want it open click once and it stays open during the session.",
      "**🚨 Falling-behind warning now splits the count by reason.** The old warning said `17 tasks from past weeks aren't implemented yet — catch up before adding new work.` — useful but didn't tell you whether to chase the client or do the work yourself. New version: `17 tasks from past weeks aren't done yet — 5 stuck with client (pending review) · 4 still in progress · 8 not started yet.` Still one line, same compact pill style — just the breakdown the consultant needs to triage. Severity escalates to `critical` (rose pill) at 5+ overdue tasks regardless of which bucket they're in.",
    ],
  },
  {
    version: "74.23.1",
    date: "2026-06-09",
    title: "Login UX polish — 1-week sessions, browser-saveable creds, instant post-login redirect, close button on the comments drawer",
    highlights: [
      "**🕐 Session bumped 48h → 1 week.** `SESSION_MAX_AGE_MS` is now `7 * 24 * 60 * 60 * 1000`. Header UserChip now reads `Session expires in ~6 days` (or `~12h` on the last day) instead of always-hours. Login subtitle copy updated. Andre's desktop credentials file regenerated to match. Reason: re-auth twice a week was friction for no security gain — the cookie is HMAC-signed + `httpOnly` + `secure`, so the only real risk is a stolen device, which doesn't get worse over 7 days vs 2.",
      "**💾 Browser password-save now works (Chrome / Safari / 1Password / iCloud Keychain).** The login form now ships with a real `action=\"/api/auth/login\"` + `method=\"POST\"` + `autoComplete=\"on\"`, and the two inputs carry `id=\"username\"` / `name=\"username\"` + `id=\"password\"` / `name=\"password\"` with `required` flags. Submit still intercepts via `preventDefault` and POSTs JSON — the static attributes exist purely so the browser password managers recognise the form and offer to save the creds. On next visit they auto-fill, no more reading off the desktop card every time.",
      "**⚡ Post-login redirect is now instant.** The slow blue-progress-bar that Andre hit on screenshot came from `router.push(next) + router.refresh()` — Next.js first soft-navigated, fetched the destination's RSC payload, THEN re-rendered after refresh. Two roundtrips, second one unnecessary. v74.23.1 swaps both for `window.location.replace(next)`: single hard GET to the destination, the browser sends the just-set cookie on that request, the page lands. `replace` (vs `assign`) also keeps `/login` out of back-button history — hitting Back from the home page won't bounce you to the login form. Spinner stays on the Sign-in button through the whole hard nav so there's no \"did my click register?\" flicker.",
      "**✕ Close button on the comments drawer.** When you expand a row's thread on the Pending Review table, the drawer now renders a header chip (e.g. `💬 3 COMMENTS`) with an `×` close button on the right + a `▴ Fechar comentários / Close comments` button below the composer. Previously the only way to collapse the thread was scrolling back up to the 💬 chip in the row — fine on short tables, painful on a 12-row review with a chunky thread. The button only renders when the parent passes an `onClose` (currently the ReviewTable expanded sub-row) so standalone uses of `<CommentsThread>` stay clean.",
    ],
  },
  {
    version: "74.23",
    date: "2026-06-09",
    title: "Employee login gate + new SuperAdmin model (Andre · Alex · Alice only)",
    highlights: [
      "**🔐 The whole workspace now sits behind a real login.** Hitting `wonder-ads-workspace.vercel.app` while signed out bounces you to `/login` (username + password), and middleware enforces it on every internal route — `/`, `/seo/*`, `/ads/*`, `/web/*`, `/commercial/*`, `/admin/*`, `/changelog`, plus every `/api/admin/*` + per-client API surface. Public surfaces stay untouched: clients on `/(public-review)/[slug]/...` and the `/api/reviews/*` API they hit are NOT gated — they never see the login page.",
      "**🪪 7 named accounts shipped, with the plain passwords written to Andre's desktop only.** `andre`, `alex`, `alice` (SuperAdmins), `manuel-s`, `fran-r`, `yenisey-r`, `germano-c` (consultants). Passwords were generated as 4-word-plus-3-digit phrases (e.g. `atlas-nimbus-aurora-copper-132` — ~60 bits of entropy, easy to read off a card), hashed with `crypto.scryptSync(plain, salt, 64)` + per-row 16-byte random salts, baked into `src/lib/auth/credentials.ts`. The plain text lives only in `~/Desktop/wonder-ads-workspace-credentials-2026-06-09.md`; the repo never sees them.",
      "**🛡 SuperAdmin model rewritten — no more `superadmin` password.** Only the three rows flagged `isAdmin: true` (Andre, Alex, Alice) can enter `/admin` or `/changelog`. Everyone else hits a friendly `Access Denied — this area is reserved for the SuperAdmin Control Suite` screen with a Back-to-workspace button. The previous `superadmin`-string gate (with its `ADMIN_PASSWORD` + `CHANGELOG_PASSWORD` env vars + per-page password form) was deleted: `src/lib/admin-auth.ts`, `src/lib/changelog-auth.ts`, `src/components/admin-gate.tsx`, `src/components/changelog-gate.tsx`, and the `/api/admin-auth` + `/api/changelog-auth` route handlers are all gone. `/api/admin/clients/[slug]/[department]` and the `/api/admin/employees/*` routes now use the new `isCurrentUserAdmin()` helper. The redundant per-suite Log out buttons (one each on Projects, Employees, Landing) were removed too — the workspace UserChip in the header is now the single logout point for the whole app.",
      "**🕐 48h HMAC-signed session cookies.** New `src/lib/auth/session.ts` carries the gate: `issueSession(username)` issues `<payload>.<HMAC-SHA256>` where the payload is `{ u, iat, exp }`. The signing key comes from `AUTH_SIGNING_SECRET` (env var). HMAC verification uses WebCrypto's `crypto.subtle` so middleware (Edge runtime) can verify cookies without dragging in node:crypto. The login API route runs in Node (it needs scrypt) and issues the cookie with `maxAge: 48*60*60`, `httpOnly`, `sameSite=lax`, `secure` in prod. Past 48h the cookie's `exp` has passed and middleware redirects to `/login?next=<original-path>` so the user lands back where they were going after re-auth.",
      "**👤 Header UserChip — identity + logout + session age.** Server component in `src/components/user-chip.tsx` reads the session cookie, resolves it back to the employee row in `credentials.ts`, and renders a `<initial> Name / Role ▾` pill in the header. Clicking opens a small menu with `Name · Role · Dept` + `Session expires in ~Xh` + a Sign-out button. The pill renders `null` on `/login` itself (no orphan placeholder).",
      "**⏱ Constant-time login + decoy scrypt against username enumeration.** `verifyEmployeeLogin` runs `scryptSync` even when the username doesn't exist (against a stable fake salt) so response timing doesn't leak which usernames are real. Wrong password and unknown user produce the identical error string (`Wrong username or password.`) and the identical 401 response. The hash comparison uses `crypto.timingSafeEqual` so it can't be probed byte-by-byte either.",
      "**🔴 ONE-TIME SETUP NEEDED** — see `~/Desktop/wonder-ads-workspace-credentials-2026-06-09.md`. Before this gate works in production you MUST set `AUTH_SIGNING_SECRET` on Vercel (Production + Preview + Development). The value is in the desktop file. Without it the production app refuses to issue cookies (dev/localhost has a safe fallback so `npm run dev` still works). The old `ADMIN_PASSWORD` and `CHANGELOG_PASSWORD` env vars are no longer read by anything in the codebase and can be deleted from Vercel.",
    ],
  },
  {
    version: "74.22.1",
    date: "2026-06-09",
    title: "Pending / Archive tab toggle now actually visible on the client-facing review page",
    highlights: [
      "**👁 Bug: the Pending ↔ Archive toggle was invisible on the public client Pending Review page.** The toggle was styled for the dark workspace shell — `border-white/15 bg-white/[0.04]` on the container, `text-white/65` on the inactive tab — which renders as white-on-white against the client page's pure-white background. Clients couldn't see there was anything to click; the Archive tab might as well not have existed for them.",
      "**🎨 New `tabsTheme` prop on `<ReviewTable>` (default `dark`).** Internal `/seo/[slug]/review` page is unchanged — it still uses the white-on-translucent treatment that reads on the dark workspace. The public `/[slug]/pendingreview` page now passes `tabsTheme=\"light\"` and gets a brand-tinted pill: violet→white→fuchsia gradient background, `border-violet-200` + soft `ring-violet-100/60` halo, inactive tabs rendered in `text-violet-900/70` over the wash (with a `hover:bg-white hover:shadow-sm` lift). Active tab keeps the existing brand-gradient pill since white-on-gradient reads against either background.",
      "**📐 Slightly larger to feel like a deliberate UI element.** Padding bumped `px-3 py-1` → `px-3.5 py-1.5`, text `[11.5px]` → `[12px]`, icon `h-3 w-3` → `h-3.5 w-3.5`, shadow `-6px` → `-8px` blur on the active glow. Count chip flipped to `tabular-nums` so the digits don't jitter when the count crosses 9→10. Net effect: the toggle now reads as a proper control the moment a client lands on the page, instead of as a phantom element only consultants knew was there.",
    ],
  },
  {
    version: "74.22",
    date: "2026-06-09",
    title: "Google-Docs-style comment threads on every Pending Review row + every public doc page",
    highlights: [
      "**💬 Each Pending Review row now carries its own comment thread.** New `Comments` column with a 💬 chip per row — click it and the row expands into a Google-Docs-style side-thread. Consultant + client both post here; messages auto-fan within ~12s on the existing table auto-poll (no extra polling overhead). Unresolved comments show a red dot on the row's chip so clients can't miss new questions; resolved comments stay visible with a `✓ Resolved` pill (full audit trail), and the original author can delete their own post. Empty rows render a dashed `Add` chip so the affordance is discoverable without already having a thread.",
      "**📄 Same thread appears on the doc preview page when you click `Open`.** Every public preview surface — `/[slug]/preview/[action]/[resultId]` (SEO Audit, Keyword Research, Roadmap, Monthly Report, …), `/[slug]/preview/gmb-posts/[resultId]`, and `/[slug]/preview/meta-tags/[resultId]` — now renders a `Comments on this document` panel below the body. The panel and the table row share storage by row id (resolved by matching `docLink` to the current path), so comments left from the document show up next to the row on the Pending Review table and vice versa — exactly like Google Docs side-comments where the comment is the property of the doc, not the page you opened it from.",
      "**🪪 Sane author identity without auth.** The internal `/seo/[slug]/review` page pre-fills the consultant role + their resolved display name from `getConsultantForSlug` so consultant posts auto-attribute. The public surfaces (table on `/[slug]/pendingreview`, every doc page) default to `Client`, ask once for an optional name, and persist both the role + name to `localStorage` keyed by client slug (`review-role:<slug>`, `review-name:<slug>`) — the next post auto-fills. Role switcher hidden on the internal page (consultant only) for clarity.",
      "**🛟 Optimistic UI + rollback.** Sending a comment renders the bubble immediately with a temp id, then swaps in the server's canonical record on success. Network failure rolls back the bubble + restores the draft text in the composer so nothing's lost. The panel variant on the doc pages also auto-polls every 8s (the table variant reuses the existing 12s item-list poll). Empty data fan-out: if a preview page can't find a Pending Review row matching its URL (consultant shared the link without going through `Send for Approval`), it renders a friendly hint asking the client to open the doc from the table — better than a dead composer.",
      "**🇵🇹 Fully bilingual.** 23 new keys in `public-i18n.ts` cover every string the thread touches — heading, placeholder, send/sending button, resolve/re-open, delete confirm, role pills, relative-time markers (`agora mesmo`, `há 3 min`, `há 2 h`, `há 5 d`), and the doc-page panel header + help. European Portuguese (PT-PT, `tu`) on PT clients, English on the rest; same `pickLang(slug)` switch the rest of the public surface uses.",
      "**🔐 Same trust model as the rest of `/api/reviews`.** New routes `POST/GET /api/reviews/[slug]/items/[id]/comments` and `PATCH/DELETE /api/reviews/[slug]/items/[id]/comments/[commentId]` mirror the existing per-item endpoint — unauth, the URL is the access control (share-link Drive-doc-style), body sanitised + clamped (4000 chars body, 60 chars name, max 200 comments per row). Comments live INSIDE the `ReviewItem` KV blob so the existing 200-item-per-client cap still bounds storage — anything older inside a thread gets trimmed when a new comment arrives. Older rows from before v74.22 with no `comments` field read as an empty thread, so the rollout is backward-compatible.",
      "**🖨 Print-safe.** The new `commentsSlot` on `PublicReportView` renders inside a `.no-print` wrapper, and the @media print rule now hides it alongside the toolbar. Clients hitting `Download PDF` on an SEO Audit / Roadmap report still get a clean print without the thread bleeding into the saved PDF.",
    ],
  },
  {
    version: "74.21",
    date: "2026-06-03",
    title: "Roadmap generator no longer 502s on a slightly off-spec Claude response",
    highlights: [
      "**🩹 Fixed the `Claude call failed: No object generated: response did not match schema.` error** that hit Andre on the Sweet Escapes regenerate. Root cause: Sonnet 4.6 occasionally lands a hair short of the strict lower bounds in the Zod schema — 18-23 tasks instead of `min(24)`, or a 25-char `auditSummary` instead of `min(40)`, or a pillar rendered as `\"On-Page\"` instead of `\"on-page\"` — and the AI SDK then rejected the WHOLE response. Now the schema is forgiving and the route also retries once before giving up.",
      "**📐 Schema loosened to ship near-complete responses instead of nothing.** `tasks.min(24)` → `tasks.min(12)` and `tasks.max(50)` → `tasks.max(60)` (the prompt still ASKS for 36-48 — the lower bound is now a safety net, not a guillotine). `auditSummary.min(40)` → `min(1)`, max 1200 → 2000. `title.max(120)` → `max(240)` (the post-processing was already truncating at 240 — we now accept what we'd have kept anyway).",
      "**🏷 Pillar parser now coerces aliases.** `\"On-Page\"`, `\"on page\"`, `\"on_page\"`, `\"OffPage\"`, `\"tech\"`, `\"investigation\"`, etc. are all mapped to their canonical lowercase 6-value enum (`technical` · `on-page` · `off-page` · `local` · `content` · `research`). Anything unrecognised falls back to `technical` instead of failing the whole roadmap — worst case the consultant re-pillars a couple of tasks in the editor, which is infinitely better than getting nothing back.",
      "**🔁 Two-attempt loop with a strict-mode retry.** First attempt runs the prompt as before. If that fails schema validation, a second attempt appends an explicit `RETRY — follow the schema EXACTLY` block reminding Sonnet of the field shapes, value constraints, and the lowercase enum. Only after the second attempt fails does the route 502 — and even then, the error message now includes the raw text snippet Claude produced so we can see what came back instead of a generic `did not match schema`.",
      "**🔍 Server-side logging on failure.** Each failed attempt now logs `[roadmap-generate] attempt N schema mismatch. Raw text: <first 1200 chars>` + the underlying cause to Vercel logs. Future failures are diagnosable without guessing.",
    ],
  },
  {
    version: "74.20",
    date: "2026-06-03",
    title: "Vercel Pro is live · every AI route bumped to the 300s ceiling so nothing gets cut off mid-stream",
    highlights: [
      "**🚀 Workspace is now running on Vercel Pro.** The Hobby cap of 60s on every serverless function was the single biggest source of mid-action failures (Claude getting guillotined mid-sentence on long blog drafts, KW research relying on an auto-continuation retry, PSI for heavy sites running over the cap). Pro lifts that ceiling to 300s — and now every AI/heavy route in the workspace is configured to use the full budget.",
      "**⏱ Routes bumped 60s → 300s:** `/api/call-notes/[slug]` · `/api/onboarding/[slug]` · `/api/onboarding/[slug]/re-extract` · `/api/seo-actions/[…]/prep` · `/api/seo-actions/[…]/prep-psi` · `/api/seo-actions/[…]/prep-dataforseo` · `/api/seo-actions/[…]/prep-kw-research` · `/api/seo-actions/[…]/run-kw-research` · `/api/seo-actions/[…]/continue-kw-research` · `/api/seo-actions/[…]/gmb-generate` · `/api/seo-actions/[…]/meta-generate` · `/api/seo-actions/[…]/save`. The /chat widget went 30s → 120s (still feels responsive because the stream emits tokens immediately; the longer ceiling exists so long-form questions don't truncate). DOCX render stays at 30s — it's pure markdown→docx, never slow.",
      "**📝 Blog Writer no longer races a 60s clock.** The blog-writer-prompt's `HARD 60-second response window` line — which encouraged the agent to truncate output to land before the Hobby cap — was replaced with `ample time to write a complete, well-crafted article (5-minute response window on Vercel Pro)`. The agent is now explicitly told to use the headroom to ship the full draft + self-audit checklist in one pass, never to truncate to save time. This is the lever that turns the v74.18 maxDuration bump on the parent action route into actually-longer published articles.",
      "**🔭 PSI internal abort lifted 50s → 180s.** Inside `runPsiPhase` (used by the SEO Audit), each PageSpeed Insights call now waits up to 180s for Google's PSI to come back instead of being pre-empted at 50s. Heavy WordPress sites that sometimes need 60-90s for the mobile audit now finish instead of being skipped. The per-device skip-rather-than-fail behaviour is preserved — if Google's PSI really hangs past 180s the audit still ships with the other device's data instead of erroring out.",
      "**🧹 Comments + architecture notes refreshed across the codebase.** Every stale `Hobby 60s` / `Pro-ready, Hobby-safe` mention in routes, `result-runner.tsx`, and the blog-writer-prompt now reads accurately as `Vercel Pro — 300s`. The phase splits (SEO Audit's 4-phase, KW research's 2-phase) are kept — the per-phase progress UX they deliver is still worth it — but the comments now say so explicitly instead of pretending they're a timeout workaround.",
      "**🛟 Why the KW research continuation endpoint stays.** `/continue-kw-research` was originally there only to recover from the 60s cap cutting Claude off before the `Verificação final` block. We're keeping it because model truncation can still happen for non-timeout reasons (max tokens, soft refusals) and the recovery path costs nothing when not needed.",
    ],
  },
  {
    version: "74.19",
    date: "2026-06-03",
    title: "Roadmap generator now uploads reference photos + does an inline SEO audit on Sonnet",
    highlights: [
      "**📸 Reference photos on the Generate panel.** New uploader inside the SEO Roadmap's `Fill with AI` / `Regenerate` flow lets the consultant attach up to 8 photos before generation — clinic interior, GMB profile screenshot, competitor SERP, Google Search Console graph, anything visual. Files upload straight to Vercel Blob via the existing `@vercel/blob/client` token endpoint (same pattern as the onboarding form), so 25 MB phone photos don't blow the function body-size limit. Thumbnails render as you upload, with a per-file remove button and a yellow `Waiting for photo upload…` state on the Generate button while any file is still in flight.",
      "**🧠 Claude now thinks like an SEO pro before sequencing tasks.** `/api/roadmaps/[slug]/generate` switched from Haiku 4.5 to **Sonnet 4.6** with vision input. Every generation now: (1) runs a live mini site-audit (homepage + about-page HTML, nav labels, footer, body text via the same `runMiniSiteAudit` the GMB pipeline uses), (2) attaches each uploaded photo natively as a vision message block (shrunk to 1024px JPEG via `shrinkForVisionInput` so Anthropic accepts them), (3) reads the brief / onboarding form / target keywords / last-15 actions as before. The system prompt now embeds first-principles SEO (E-E-A-T, YMYL, intent fit, sequencing, AI-Overviews readiness, local NAP discipline, what to refuse) so the plan reflects a real diagnosis instead of a template.",
      "**📋 New `auditSummary` field on every generated Roadmap.** The Zod schema now demands a 2-5 sentence prose diagnosis alongside the task list — what the business actually does (from the audit + photos), the 2-3 biggest gaps blocking growth, and what each photo contributed. Surfaced on the Roadmap board in a new `SEO diagnosis (Claude)` card directly under the Generate panel, with the reference photos linked as thumbnails so the consultant can audit what grounded the plan. Older roadmaps generated before v74.19 simply skip the card — the field is optional on `Roadmap`.",
      "**⏱ maxDuration on `/api/roadmaps/[slug]/generate` bumped 60s → 300s.** Sonnet + vision + a mini-audit can run 30-60s vs Haiku's 10-20s. Vercel Pro honours the full 300s; Hobby silently caps at 60s — same Pro-ready / Hobby-safe pattern as the v74.18 bump on the main action route. Photo fetch timeout per file is 8s; oversized fetches (>25MB on the wire) are skipped with a console warning instead of failing the whole generation.",
      "**🧹 Removed two stale macOS Finder-duplicate files** (`src/components/admin-panel 2.tsx`, `src/components/admin-client-row 2.tsx`). Both were unreferenced, had diverged from the originals, and were now breaking `next build` after the v74.15 prop rename (`departments` → `department`). Deleting them restored the build.",
    ],
  },
  {
    version: "74.18",
    date: "2026-06-02",
    title: "Spine Center website registered app-wide · maxDuration bumped to 300s (Pro-ready, Hobby-safe)",
    highlights: [
      "**🌐 Spine Center website added.** `getClientWebsite(\"spine-center\")` now returns `https://www.spinecenter.pt/`. Single edit in `src/lib/client-meta.ts` flows through every consumer — action page defaults (SEO Audit, Meta Tags, Crawl-page tools), `prep` route URL field, public deliverables, DataforSEO domain queries, GMB / Contact-page detectors, and the Site Audit orchestrator. Spine Center can now run any URL-anchored action without typing the URL.",
      "**⏱ Function timeouts bumped from 60s → 300s on the main action route + the audit /run route.** Vercel honours the new value on Pro; on Hobby it's silently capped back to 60 — no breakage either way. This is the prerequisite that unlocks the longer blog drafts the moment the workspace runs on Pro. Without the upgrade the Hobby cap still kicks in and 1200+ word articles can still be cut mid-sentence — see the note alongside this release explaining how to upgrade.",
    ],
  },
  {
    version: "74.17",
    date: "2026-06-02",
    title: "Pending Review · Archive tab now visible to clients on the public page (read-only)",
    highlights: [
      "**🗂 Clients can browse the Archive tab on their public review page.** Same `Pending` / `Archive` tab switcher the consultant has on `/seo/[slug]/review` now renders on the unauthenticated `/<slug>/pendingreview` page too — so a client can scroll through past approvals + rejections without a consultant copy-pasting screenshots. The data has always been there in KV; v74.15 just defensively hid it from the public view. v74.17 unhides it but keeps writes consultant-only.",
      "**🛡 Read-only on the client side.** Split the existing `allowArchive` prop on `ReviewTable` into two: `allowArchive` (controls whether the tab switcher renders) and `allowArchiveActions` (controls whether the per-row Archive / Restore buttons render). Public client view sets `allowArchive: true` only — clients can switch tabs and read the archive, but the columns + buttons that flip a row's `archived` field stay invisible to them. Internal consultant view sets both to true. Server-side guards from v74.15 are unchanged — `PATCH /api/reviews/[slug]/items/[id]` still rejects `archived: true` from any context with a status that isn't Approved or Rejected.",
      "**🔄 SSR + poll already aligned.** The public page server-render now ships the full item list (no more `filterPublicItems()` strip on initial render); ReviewTable's auto-poll already calls `?includeArchived=1` whenever `allowArchive` is true, so the public Archive tab stays in sync without separate plumbing. Same Pending tab body the client saw before — the only thing that changed is they now have a tab toggle they can click.",
    ],
  },
  {
    version: "74.16",
    date: "2026-06-02",
    title: "SuperAdmin Projects · brand logos + sortable columns + Approaching billing pill + Unassigned flag + bi-monthly cycle + click-to-open portfolio",
    highlights: [
      "**🖼 Real brand logos on every row.** The 7×7 emoji square is gone — each `/admin/projects` row now renders the actual client logo through `LogoChip` (the same component the SEO DPT cards use). New `sm` size on `LogoChip` (h-8 w-8, p-1) keeps the row compact. Server-side, the page passes `logo` + `logoBgMode` + `logoSizing` + `gradient` per row by looking up `getClientLogo` / `getLogoBgMode` / `getLogoSizing` / `paletteToGradient`. Clients without a logo file fall back to the emoji on a brand-gradient chip — same fallback the SEO landing uses.",
      "**📅 `Approaching` pill on rows billing within 14 days.** When the auto-computed `Next billing` date is ≤ 14 days away, the date itself flips to amber (`text-amber-200` + semibold) and the helper line underneath becomes a vibrant amber pill: `● Approaching` on `border-amber-400/55 bg-amber-500/15`. Beyond 14 days the helper line stays `Auto-computed`. So a glance at the column tells you which invoices need eyes this week.",
      "**🚨 `Unassigned` consultant cells now flagged rose.** When a row's `consultants[]` is empty, the multi-select trigger takes the same vibrant rose treatment as empty Monthly value: rose border + rose tint + soft rose ring + rose label. Helper line below the trigger swaps from `0 consultants` to a semibold rose `Missing — assign a consultant`. Scans the table for unattributed rows in one sweep, same as how you scan for unfilled budgets.",
      "**🔀 Sortable column headers** on `/admin/projects`. Click a header to sort ascending; click again for descending; click a third time to clear back to the server's default order. Sortable: `Client`, `Billing cadence`, `Starting date`, `Next billing`, `Consultants`, `Monthly value`, `Status`. Notes + the action column stay unsorted (they're not comparison-friendly). Sort indicator on the active header turns emerald + shows `↑` / `↓`; inactive headers show a subtle `⇅` glyph so it's obvious they're clickable. Sort runs against the LIVE records map so changes saved on one row immediately re-rank the table — no refresh.",
      "**🗓 `bi-monthly` billing cadence option.** New `Each 2 months` option in the cadence dropdown, sitting between Monthly and Each 3 months. `cadenceMonths` returns 2 so `nextBillingDate` ticks forward 2 months at a time. The cadence enum order is `Monthly · Each 2 months · Each 3 months · Each 6 months` so the dropdown reads in increasing length.",
      "**🖱 Active portfolio: hover → click.** The Active portfolio cell on `/admin/employees` is now click-toggled, not hover. Click opens the per-client breakdown panel; outside-click or Escape closes it. The breakdown stays open while you scan the list (no more accidental dismissal when the cursor drifts), works on keyboard via focus, works on mobile. Cell visually highlights brighter when the panel is open (`border-emerald-300/65 bg-emerald-500/[0.12]`) so it's obvious which row's panel you're looking at. Label changes from `hover for details` to `click for details` / `click to close`.",
      "**🏷 Clearer shared-row hint.** Was `+ ADS row` (read like the row itself owned two depts). Now `· also in ADS` — a subtle dot prefix + lower-case `also in` that reads correctly: this row is SEO, the same client also has a row in ADS. Tooltip on hover: `Also tracked under ADS on its own budget row.`",
    ],
  },
  {
    version: "74.15",
    date: "2026-06-02",
    title: "Pending Review archive tab · per-dept project rows · portfolio hover breakdown · payroll seeds · WONDER ADS chip removed",
    highlights: [
      "**🗃 Pending Review now has an `Archive` tab.** Inside every client's internal SEO DPT review table (`/seo/[slug]/review`) the consultant sees a `Pending` / `Archive` tab switcher above the table. New per-row `Archive` button on the `Doc link` column — emerald when the row is Approved or Rejected, greyed-out + disabled otherwise. Trying to archive an in-flight row pops a rose `Can't archive — row must be Approved or Rejected first.` toast for 4.5s. Archive button flips to `Restore` inside the Archive tab so consultants can pull rows back to Pending if they archived prematurely.",
      "**🛡 Two-layer guard.** Client-side: the Archive button is disabled when the row's status isn't Approved or Rejected, with a tooltip explaining why. Server-side: `/api/reviews/[slug]/items/[id]` PATCH returns `422 Can't archive — row must be Approved or Rejected first.` when a hand-rolled body tries to set `archived: true` on a status that doesn't permit it. Both reference the same `isArchivable()` predicate in `review-store.ts` so the rule stays in sync.",
      "**🔒 Public client view never sees archived rows.** `GET /api/reviews/[slug]` defaults to filtering archived items; the internal review page opts in with `?includeArchived=1` so its auto-poll keeps the Archive tab populated. New `filterPublicItems()` helper in `review-store.ts` runs at the server-render boundary too, so the moment a consultant archives a row it disappears from the public page on the next poll.",
      "**📦 Multi-dept clients now have one row per department.** Shared SEO + ADS (or + Web) clients are split inside `/admin/projects` so each consultant team owns their own budget slice. Example: White Clinic SEO row = Yenisey R. + €1500; White Clinic ADS row = Germano C. + €2000. The MRR rollup sums those independently → €3500 total without double-counting. Active portfolio per consultant reflects only the rows they actually consult on, so Yenisey's portfolio is +€1500 and Germano's is +€2000, instead of both seeing +€3500 like before. Row badge shows the row's own department + a small `+ ADS row` hint when the same client has another row.",
      "**🧱 Storage moved to `admin-client:<slug>:<dept>`** in `src/lib/admin-clients-store.ts`. New `ClientDepartment` type (`SEO` / `ADS` / `Web`), new `CONSULTANT_DEPARTMENT` map for default-seeding the right consultant per row. New `LEGACY_VALUE_DEPT_PRIORITY` ensures pre-v74.15 single-row records get their value attributed to one dept only (SEO first, then ADS, then Web) when read so no value is double-counted during migration. Old `admin-client:<slug>` keys stay untouched on disk — `deriveFromLegacy()` materialises a per-dept row on the fly. Saving any (slug, dept) row promotes that dept into its own key.",
      "**🛣 API route moved to `/api/admin/clients/[slug]/[department]`** PUT. Same per-field sanitisation as before, plus department validated against the enum. Row component sends `clientDepartments` in the body so the server's legacy-migration helper knows which other depts the client lives in.",
      "**🪟 Active portfolio hover popover.** The Active portfolio column in `/admin/employees` is now a real button that opens a side-panel on hover (or focus, for keyboard users): emerald-tinted card listing each client this consultant owns, sorted by value desc, with their value next to each title and per-row dept badges (SEO purple, ADS magenta, Web cyan). For shared clients the title appends ` · SEO` / ` · ADS` so each row is unambiguous. Footer of the popover tells the consultant `N client(s) active without a set monthly value yet` when relevant — so unbilled work is visible at a glance.",
      "**💶 Seed payroll populated.** `SEED_EMPLOYEES` now ship default monthly EUR + starting dates: Manuel S. €400 / 03-06-2026 · Fran. R. €1000 / 17-03-2026 · Yenisey R. €1250 / 20-04-2026 · Germano C. €1000. Backfill applies on read for any existing KV record that still has nulls — no manual editing needed. Future overrides win.",
      "**🧹 `WONDER ADS` chip removed** from the SuperAdmin header on the chooser, the Projects panel, and the Team Roster panel — it was redundant next to the title and felt like duplicated branding. Projects panel title is now plain `Projects` (the gradient stays). The footer chip into `/admin` is unchanged.",
    ],
  },
  {
    version: "74.14",
    date: "2026-06-02",
    title: "SuperAdmin Suite · Web DPT + Prof. Fernando Almeida · starting dates pre-populated · EUR-only · Active portfolio column",
    highlights: [
      "**🌐 Web DPT — Prof. Fernando Almeida joins the projects roster.** New `src/lib/web-clients.ts` exports `WEB_CLIENTS` following the same shape as `ADS_CLIENTS` (slug-shared with SEO when overlapping). Slug `prof-fernando-almeida`, icon 🎓. The Web roster gets merged into `/admin/projects` alongside SEO + ADS and the chooser-landing client count picks it up too. New `Web` dept pill — cyan tint (`border-cyan-400/45 bg-cyan-500/12 text-cyan-200`) — appears on the row badge so the three DPTs are visually distinct at a glance.",
      "**📅 Starting dates pre-populated from the team's post-it board.** New `DEFAULT_STARTING_DATES` map in `src/lib/admin-clients-store.ts` ships 17 starting dates from the photos — always the `Starting date` value, never the `Re-starting Date` notes below it: wonderads 30/03/2026 · clínica em casa 26/02/2026 · clínica mimus 04/05/2026 · insync-design 23/01/2026 · senior-resort 26/02/2026 · safe-away 20/02/2026 · sea-yourself 14/01/2026 · a-domingos 23/02/2026 · aeger-prima 19/11/2025 · b-life 09/02/2026 · hds-learning 07/01/2026 · white-clinic 30/03/2026 · ihn 30/03/2026 · spine-center 01/06/2026 · fisio-restelo 12/02/2026 · monte-mar 23/03/2026 · cdt 23/03/2026. Defaults apply when the field is `null` on read — already-saved dates win. So fresh records land prefilled AND older records that still had `null` dates get backfilled automatically.",
      "**💶 EUR only — USD removed from the UI.** Agency bills in euros, so the € / $ selector is gone from both the projects row AND the employees row. Each row's money block now shows a fixed € prefix (brand-gradient when filled, rose when empty — same emphasis treatment). `MRR $` rollup tile removed from `/admin/projects` (now a 2-tile grid: `Clients` · `MRR`). `Payroll $` tile removed from `/admin/employees`. `migrateRecord` coerces any stale `currency: \"USD\"` on disk to `\"EUR\"` so old saves don't poison the rollups.",
      "**⏱ Employees: Payment cadence + Next pay columns removed.** Both were noise — everyone gets paid monthly. Table is tighter now: Employee · Role · Departments · Starting date · Monthly salary · Active portfolio · Status · Notes. Same Save/Discard pattern, same rose `Needs salary` flag, same emphasized money block.",
      "**📊 New `Active portfolio` column in the Employees table.** Server-computed from the full client roster (SEO + ADS + Web admin records merged): for every employee whose name appears in any client's `consultants[]` array AND that client's `status === \"active\"`, we sum the monthly value + count the clients. Renders as an emerald-tinted card per row: total in EUR up top, `N active clients` underneath, full client list on hover (title attribute). Empty rows show a faint `None active` placeholder. New `Active portfolio` rollup tile at the header sums it across the whole team — emerald glow when populated.",
      "**⚙️ Server data flow.** `/admin/employees/page.tsx` now fetches the same SEO + ADS + Web rosters as `/admin/projects` and reads every admin record so the portfolio map is consistent. Edit a client's value or consultant on `/admin/projects`, navigate to `/admin/employees`, and the portfolio reflects the change on the next render — no stale snapshots.",
    ],
  },
  {
    version: "74.13",
    date: "2026-06-02",
    title: "SuperAdmin Suite · post-gate chooser landing · Projects block + new Employees block — team roster + payroll",
    highlights: [
      "**🧭 Post-gate chooser landing.** `/admin` no longer drops you straight into the projects table. After unlocking you land on a two-block chooser — `Projects` (FolderKanban icon, current client management) and `Employees` (Users icon, the new team roster). Each block carries a brand-gradient border, a brand-purple glow that brightens on hover, a live count badge (`N clients` / `N employees`) and an `Open …` arrow that slides on hover. Pick where you want to go.",
      "**🚪 Gate moved to a layout.** New `src/app/admin/layout.tsx` runs `isAdminUnlocked()` once and either renders `AdminGate` or the children. Every page under `/admin/*` is automatically protected — `/admin/projects` and `/admin/employees` can't be hit directly without the cookie, no per-page boilerplate. Clean, single source of truth for the gate.",
      "**📁 Projects moved to `/admin/projects`.** Same flat table, same multi-consultant dropdown, same € / $ toggle, same live MRR, same rose empty-state flag, same emphasis on the monthly-value column — just at a new URL. A `Back to Admin` link sits at the top so you can switch between Projects and Employees in one click.",
      "**👥 New `/admin/employees` — the team roster + payroll.** Mirrors the Projects panel for consistency: header (with rollup tiles), table (one editable row per employee), Save/Discard per row, live `Payroll €` and `Payroll $` rollup tiles at the top with the same emerald glow when populated. Columns: `Employee` (name + email stacked), `Role` (free text), `Departments` (multi-select dropdown — SEO · ADS · Operations · Founder, each with its own coloured pill), `Starting date` (DD/MM/YYYY), `Payment cadence` (Monthly / Each 3 months / Each 6 months), `Next pay` (auto-computed), `Monthly salary` (€/$ toggle + emphasized input, rose `Needs salary` flag when empty), `Status` (Active / Onboarding / On leave / Offboarded — coloured select), `Notes`.",
      "**➕ Add employee inline.** Top-right of the Employees panel: brand-gradient `Add employee` button toggles an inline form (Name · Email · Role) styled to match the panel's aesthetic. POST → `/api/admin/employees` validates inputs, generates a slugified id (with collision suffixes if needed), writes the record + appends to the roster index, and the row appears at the bottom of the table instantly without a refresh.",
      "**🗑 Remove employee.** Per-row `Remove` button (or `Reset` for the canonical seed roster — they revert to default rather than disappearing). Two-click safeguard — first click flashes a rose `Confirm` state for 3s, second click fires DELETE → `/api/admin/employees/[id]`.",
      "**🌱 Seed roster.** New `SEED_EMPLOYEES` constant in `src/lib/admin-employees-store.ts` defaults the table to the canonical agency roster — Manuel S. · Fran. R. · Yenisey R. · Germano C., each with their inferred email (firstname-handle@wonder-ads.com), role (`SEO Consultant` / `ADS Consultant`), and department. Editing any seed employee promotes them into KV; new hires get added to a dedicated `admin-employees:roster` index alongside their record.",
      "**🛡 Two new auth-gated API routes.** `POST /api/admin/employees` adds a hire (validates email format, returns 401 without cookie). `PUT /api/admin/employees/[id]` patches a record with full per-field sanitisation (name ≤ 80 chars, email regex-checked, departments validated against the enum, currency / cadence / status validated against their enums, monthlyValue clamped non-negative + 2-decimal rounded, notes capped at 2000). `DELETE /api/admin/employees/[id]` removes the record + roster index entry.",
      "**🧾 Footer chip still points at `/admin`.** The `SuperAdmin Suite · Log in` button in the footer continues to land on `/admin` → chooser. No deep-link change — the suite's a deliberate two-step.",
    ],
  },
  {
    version: "74.12",
    date: "2026-06-02",
    title: "SuperAdmin Suite · live MRR · green tiles · monthly-value emphasized · empty cells flagged rose · Active tile removed",
    highlights: [
      "**⚡ MRR tiles update instantly on save — no refresh needed.** Per-row records lifted into `AdminPanel` as a `Map<slug, AdminClientRecord>` (`useState` seeded from server props). Each row now receives an `onSaved(record)` callback; when the API returns 200, the row fires it AND the panel updates its map → `useMemo` recomputes MRR € + MRR $ instantly. Previously the rollup tiles were derived from immutable props so they only refreshed after a full `router.refresh()`. The Save button still flips through `idle → saving → ✓ saved → idle`; the tiles at the top just keep up now.",
      "**💚 MRR tiles glow emerald when populated.** Green tone — `border-emerald-400/35` + `bg-emerald-500/[0.06]` + emerald label + emerald value with a soft emerald drop-shadow — fires when the tile value > 0. Tiles fall back to neutral white when the currency has no active value yet (renders `—`). Reads at a glance: green = MRR healthy, dash = nothing to count yet.",
      "**❌ `Active` tile removed.** Was redundant with `Clients` since the active count equalled the total in practice (rare for a client to sit `paused/onboarding/offboarded` long). Three tiles now: `Clients` · `MRR €` · `MRR $` in a 3-column grid. Cleaner header, no duplicate signal.",
      "**💶 Monthly Value column emphasized.** Larger 14px font (was 12px), `tabular-nums`, semibold, bigger 8px vertical padding (was 6px), brighter `border-white/18` + `bg-white/[0.05]` background. The € / $ currency selector picks up the same brighter treatment so it reads as one heavy money block, not a faint footnote next to the rest of the row.",
      "**🚨 Empty Monthly Value cells flagged vibrant rose.** When `monthlyValue === null` the whole block — currency selector + number input + label below — flips to rose: `border-rose-400/55`, `bg-rose-500/[0.08]` tint, rose `placeholder:text-rose-300/70`, a soft `shadow-[0_0_0_1px_rgba(244,63,94,0.18)]` ring around the input, and the helper text underneath swaps from `/ mo · EUR` to a semibold rose `Needs value` flag. So the consultant can scan the table in one second and see which clients still need their billing amount populated.",
    ],
  },
  {
    version: "74.11",
    date: "2026-06-02",
    title: "SuperAdmin Suite footer entry → real button — brand-purple chip, ShieldCheck icon, new label",
    highlights: [
      "**🔐 Footer entry to SuperAdmin Suite is now a real button** instead of inline link text. The plain `Admin Control Panel? Login here` line is replaced with a compact pill — `ShieldCheck` icon (brand magenta `#C535C9`) + `SuperAdmin Suite · Log in` label, brand-purple `#783DF5` border + tinted fill + soft glow. Same hover treatment as the rest of the workspace buttons (border + bg brighten, icon flips to white). Footer still keeps `© Wonder Ads` on the left and the `workspace.v##` changelog link on the right, so the chip sits in the middle and reads as a deliberate CTA — not a stray sentence. Single `<Link>` element, `aria-label=\"Log in to the SuperAdmin Control Suite\"` for screen readers.",
    ],
  },
  {
    version: "74.10",
    date: "2026-06-02",
    title: "SuperAdmin Control Suite · one flat list · multi-consultant dropdown · €/$ per client · André P. → Manuel S. handover · Spine Center added",
    highlights: [
      "**🏷 Page renamed → `SuperAdmin Control Suite`.** Header H1 + browser title both updated. Subtitle trimmed to a single line: `Every client across every department — edit independently per row.`",
      "**📋 One unified client list.** SEO + ADS sections collapsed into a single deduped table. Per-row badges still tell you which department(s) a client belongs to (`SEO · ADS` on shared clients, brand-purple for SEO, brand-magenta for ADS — they're not just grey anymore so the eye picks them up at a glance). Per-section blurbs + the `N clients` count badge in the right corner are gone — the rollup tiles at the top carry the totals.",
      "**👥 Consultant field is now a multi-select dropdown.** Replaced the free-text input with a button that opens a checkbox list of the canonical roster (`Manuel S.`, `Fran. R.`, `Yenisey R.`, `Germano C.`). Pick one or many — shared SEO + ADS clients can carry their SEO consultant AND the ADS consultant on the same row. Selected consultants render as small pills inside the trigger. Dropdown closes on outside-click or Escape. Names from older saves that aren't in the canonical roster surface as toggleable `legacy` rows so you can remove them without losing the rest of the row's state.",
      "**💱 Currency per client — EUR or USD.** Each row carries its own `€ / $` segmented toggle next to the monthly value input. Storage is `currency: \"EUR\" | \"USD\"` + `monthlyValue: number`. The header rollup splits MRR into two tiles — `MRR €` and `MRR $` — so the mixed-currency roster sums correctly without a fake FX conversion. Both tiles display `—` when their currency has no active value yet.",
      "**🤝 André P. → Manuel S. — agency-wide handover.** Manuel takes over André's book (Aeger Prima · A. Domingos · Senior Resort · Safe Away · Clínica em Casa) following the same pattern as the Luana N. → André P. handover in v74.6. `src/lib/client-overrides.ts` — the `ANDRE` slug set renamed to `MANUEL`, `getConsultantForSlug` returns `Manuel S.`, `getConsultantEmailForSlug` returns `manuel@wonder-ads.com`. `CONSULTANT_ORDER` updated for the SEO landing-page column header. `CONSULTANTS` roster in `admin-clients-store.ts` updated. **Cache key bumped `seo-clients-v3 → seo-clients-v4`** in `src/lib/notion.ts` so the new consultant identity takes effect immediately rather than waiting an hour for the previous entry to expire.",
      "**🔁 Auto-migrate existing admin records.** `migrateRecord` in the admin store now applies a rename map on read — any consultant string saved as `André P.` (or older legacy `Luana N.`) gets rewritten to `Manuel S.` and deduped. So clients that already had `André P.` on the admin board flip to `Manuel S.` without manual intervention, AND PDF/DOCX deliverables already in the pipeline pick up the new email/byline next time they regenerate. (Old changelog entries that mention André P. or Luana N. stay as historical record.)",
      "**🦴 Spine Center added to the SEO DPT under Yenisey R.** New synthetic-client mechanism in `src/lib/notion.ts` — `EXTRA_SEO_CLIENTS` array appended to the result of `_fetchSeoClients` so a client can appear across `/seo`, every action page, the admin board, briefs storage, etc. without needing its own Notion child page yet. Slug `spine-center`, icon 🦴, palette + tier default through the existing per-slug lookups, attributed to Yenisey R. via the `YENISEY` set in `client-overrides.ts`. Same cache-key bump (`v4`) makes it appear immediately on next request.",
    ],
  },
  {
    version: "74.9",
    date: "2026-06-02",
    title: "Admin Control Panel · gated DPT for cross-department client management — billing cadence, starting date, consultant, MRR, status, notes",
    highlights: [
      "**🔐 New Admin Control Panel at `/admin`.** Gated behind a superadmin password (default `superadmin`, override via `ADMIN_PASSWORD` env var). Mirrors the existing `/changelog` auth pattern but uses an independent cookie (`wa-admin`, 7-day expiry) so the two gates don't bleed into each other. `src/lib/admin-auth.ts` exports `isAdminUnlocked()` + `ADMIN_COOKIE` + `ADMIN_PASSWORD`. New `src/app/api/admin-auth/route.ts` accepts `POST { password }` to set the cookie and `DELETE` to log out. New `src/components/admin-gate.tsx` is the unlock form (Lock icon + password input + Unlock button, same brand-gradient aesthetic as the changelog gate).",
      "**🦶 Footer message swapped.** The bug-report email line in `src/components/page-shell.tsx` is gone; in its place: `Admin Control Panel? Login here` — `Login here` is a real `<Link href=\"/admin\">` so a single click takes you to the gate. The `andre@wonder-ads.com` mailto stays available on the `/seo/kpis` page (where the contact CTA actually belongs) and inside SEO deliverables via the consultant-email lookup.",
      "**📋 Admin Panel UI — clean cross-department table.** New `src/components/admin-panel.tsx` (client) renders rollup tiles (Clients · Departments · Active MRR · Engagements) + one card per department. Inside each card, a wide table with one editable row per client. SEO clients pulled from Notion via `getSeoClients()`; ADS clients from the local `ADS_CLIENTS` roster; clients that appear in both get a dual `SEO · ADS` badge in the row. Roll-up MRR sums **active**-status clients only and dedupes shared clients so they don't double-count.",
      "**✏️ Every field the brief asks for, plus essentials.** Per-row editable: **Billing cadence** (`Monthly` / `Each 3 months` / `Each 6 months` — the three options you specified), **Starting date** (native date picker; renders DD/MM/YYYY everywhere per Andre's preference), **Next billing** (auto-computed from start + cadence, never user-edited so it can never drift), **Consultant** (free-text — defaults to `getConsultantForSlug(slug)` or the ADS-side override for ADS rows), **Monthly value** (€ — numeric input, used in the Active MRR rollup), **Status** (`Active` / `Paused` / `Onboarding` / `Offboarded` — pill colour changes with selection), **Notes** (2-row textarea for invoicing quirks / payment terms / contact prefs). Each row keeps local draft state — a Save button lights up only when something changes, and a Discard option lets you back out cleanly.",
      "**💾 KV persistence + sane defaults.** New `src/lib/admin-clients-store.ts` keys records under `admin-client:<slug>`. `getAdminRecord(slug)` falls back to a derived skeleton (consultant from `client-overrides`, status `active`, cadence `monthly`) so a fresh client renders without empty fields. `getAdminRecords([slugs])` batches reads in parallel. `saveAdminRecord(slug, patch)` merges a partial patch into the current record and stamps `updatedAt`. `cadenceLabel()` / `cadenceMonths()` / `nextBillingDate()` are pure helpers reused by the row component and any downstream view that wants to know when the next invoice is due.",
      "**🛡 Auth-gated write API.** New `src/app/api/admin/clients/[slug]/route.ts` PUT handler. Every field is sanitised at the boundary — `billingCadence` validated against the enum, `startingDate` matched against `^\\d{4}-\\d{2}-\\d{2}$`, `status` validated, `monthlyValueEur` coerced to a non-negative number with 2-decimal rounding, `notes` capped at 2000 chars, `consultant` capped at 80. Returns 401 when the admin cookie is missing or wrong — so the page being publicly visible doesn't expose the write endpoint.",
      "**🎯 Sane UX touches.** Row turns subtly highlighted while dirty (`data-dirty=\"true\"` on the `<tr>` if you want to style further). Save flips through `idle → saving → saved (✓, auto-dismiss 2.5s) → idle` or `error` with the message visible on hover. Logout button at the top-right clears the cookie and refreshes. The page itself is `dynamic = \"force-dynamic\"` + `revalidate = 0` so freshly-saved records appear on the next navigation — no stale `unstable_cache` lag.",
    ],
  },
  {
    version: "74.8",
    date: "2026-06-01",
    title: "Blog Writer Pro v2 · agent-rules behind a button · onboarding double-check · URL whitelist + post-stream HEAD verification · prompt trim",
    highlights: [
      "**📖 Agent rules moved behind a button at the top-right of the action header.** The always-visible panel on `/seo/<slug>/actions/write-blog-article` is gone. New client component `src/components/blog-writer-standard-button.tsx` renders a small `Agent rules` button in the page header; clicking opens a modal with the same seven collapsible cards (Language · Brief check · Onboarding check · External links · WonderAds standard · Process · Output format). Close on Escape, click-outside, or X. Body scroll locks while open. The page header is clean by default — rules are one click away when the consultant needs them.",
      "**📋 Onboarding form is now MANDATORY input to the Blog Writer.** New branch in `src/app/api/seo-actions/[clientSlug]/[actionSlug]/route.ts` for `write-blog-article` that calls `getOnboardingForSlug(clientSlug)`, extracts text into the fact pack, AND attaches the PDF natively to the Anthropic message (same pattern as Keyword Research) so the agent reads the form's layout / tables / images, not just regex-extracted text. New `BLOG_WRITER_ONBOARDING_CHECK` section in the system prompt enforces a TWO-step verification: before outlining (confirm the article topic is consistent with a service the client actually sells; if not, flag it in Working Notes — never pretend) and before each CTA (confirm the CTA target page exists in the client's inventory). The agent quotes 1–2 short verbatim excerpts in Working Notes so the consultant trusts the form was actually read. When no form is on file the agent says so explicitly and leans conservative.",
      "**🔗 External-link verification — whitelist + post-stream HEAD check.** Two-layer defence against hallucinated URLs (the `spor.pt` incident). **Layer 1 (agent-side):** `BLOG_WRITER_LINK_VERIFICATION` bakes a curated whitelist of pre-approved authoritative roots into the prompt — `who.int / nhs.uk / nih.gov / pubmed.ncbi.nlm.nih.gov / cdc.gov / cochranelibrary.com / ema.europa.eu / dgs.min-saude.pt / sns.gov.pt / sns24.gov.pt / portugal.gov.pt / infarmed.pt / ine.pt / pordata.pt / europa.eu / eurostat / oecd.org / statista.com / pt.wikipedia.org` (Wikipedia non-YMYL only). For sources OUTSIDE the whitelist the agent must use the literal token `[link to be added by consultant]` instead of inventing a URL — even at 90% confidence, because 90% isn't enough. **Layer 2 (system-side):** new `src/lib/verify-external-links.ts` exports `extractExternalUrls()` + `verifyExternalUrls()` + `renderBrokenLinkWarning()`. The `/save` route calls these on every blog-article output — parallel HEAD checks (concurrency 8, 4s per-URL timeout, 6s total cap), HEAD→GET-range fallback for 405/403, realistic User-Agent. Any URL returning ≥ 400 or a network error gets surfaced in a warning callout prepended to the saved markdown: `⚠️ N external link(s) failed verification. Replace or remove before publishing:` with each broken URL + its failure reason. Consultant sees it the moment the result page loads.",
      "**✂️ System prompt trimmed ~30% to reduce mid-article truncation.** The previous prompt's exhaustive lexical table + repeated explanatory prose burned 700+ tokens of context. New version keeps every RULE but compresses presentation: the language table is now an actual markdown table (\\| Use \\| NOT \\|), the standard's sub-sections are tighter, the process is 6 numbered steps instead of long-form paragraphs. Plus `streamText` now passes `maxOutputTokens` calibrated per request: `Math.min(8000, wordCount × 1.65 + 800 overhead)` — for a 1500-word default that's ~3300 tokens, comfortably inside Sonnet's window AND inside Vercel's 60s budget at typical 50-70 tps streaming. The model knows the ceiling so it paces itself instead of running long and getting guillotined mid-sentence.",
      "**🏃 Header efficiency directive baked into the prompt.** Top of the system prompt now reads: *\"You have a HARD 60-second response window. Start IMMEDIATELY with the slug + meta block. No preamble. No 'Here is the article'. No restating these rules back.\"* — duplicated as a guard-rail at the bottom. Combined with `maxOutputTokens`, the agent stops wasting the first 5-10 seconds re-acknowledging instructions and gets to work.",
      "**🛠 Result page integration unchanged.** The blog article continues to flow through the standard `/save` pipeline → public preview at `/<slug>/preview/write-blog-article/<resultId>` (light-themed HTML report + Download-PDF button for clients; DOCX stays internal). The link-verification warning, when present, sits at the very top of the saved markdown — so it appears in the internal result view AND in the public preview. Either the consultant fixes the broken links before sending OR the client sees the warning. No silent broken citations.",
    ],
  },
  {
    version: "74.7",
    date: "2026-06-01",
    title: "Blog Article Writer Pro · dedicated agent for SEO blog writing — language rule, triple-checked brief, references + internal-linking baked in",
    highlights: [
      "**✍️ `Write Blog Article` is now a specialist agent, not a generalist prompt branch.** New `src/lib/blog-writer-prompt.ts` exports `buildBlogWriterSystemPrompt({ client })` which builds an entirely separate system prompt — persona `Blog Article Writer Pro — <client>`, dedicated process, dedicated output format. `seo-claude-prompt.ts` short-circuits to it for `action.slug === 'write-blog-article'` so every existing caller (the main /route.ts, the split /run /save flow) automatically picks up the new agent without any other changes.",
      "**🌍 ABSOLUTE language rule baked into the system prompt.** EN clients (`languageCode === 'en'`) get American English (en-US) only — `optimize / color / behavior`, never `optimise / colour / behaviour`. PT clients (`languageCode === 'pt'`) get European Portuguese (pt-PT) from Portugal only — **NEVER Brazilian Portuguese**, even when the client is geographically based in Brazil (HDS Learning). The rule ships with a long lexical table the agent must follow: `telemóvel` not `celular`, `casa de banho` not `banheiro`, `está a crescer` not `está crescendo`, `marque a sua consulta` not `agende já`, etc. Plus number format (`1.234,56 €`), DD/MM/YYYY dates, article-before-possessive (`a sua clínica`).",
      "**🛡️ Triple-checks the client brief — before, during, after.** The prompt enforces three explicit brief verification points: read the Do's / Don'ts / Notes BEFORE outlining (and drop angles that would violate a Don't, never soften), apply the brief to every CTA / example / link DURING the draft, then re-read the article with the brief open AFTER. A mandatory `Brief check appendix` is written at the end of every article with honest `[x]` / `[ ]` boxes — fake ticks are explicitly disallowed in the prompt. The consultant strips this appendix before publishing; the box state tells them what (if anything) needs a final manual pass.",
      "**🔗 References + internal linking are now process steps, not afterthoughts.** Steps 3 and 4 of the agent's process — between outlining and drafting — are MANDATORY reference research and internal-linking planning. Reference rule: 1–5 external citations per 1000 words, biased toward 2–3, from one of government/official sources (SNS, DGS, OMS/WHO, NHS, gov.pt), peer-reviewed papers (PubMed, Cochrane, university), statistics bodies (Statista, INE, Eurostat, OECD), Wikipedia only as a fallback for general knowledge and NEVER for YMYL/medical claims. Internal-linking rule: 4–6 per 1000 words minimum (10–15 for list-style or deep-inventory clients), 80% exact-match / close-variant anchors, 20% natural variants — never `click here`, never duplicates, never inside a heading.",
      "**📥 New optional form fields for the consultant.** Added `internalLinkInventory` (textarea — paste real URLs/titles from the client's site so the agent uses VERIFIED targets, not inferred ones), `lsiKeywords` (1–2× each, ideal in H3s), `referenceFocus` (steer the agent to specific authoritative angles like `SNS guidelines on lower-back pain`), and `ctaTarget` (URL/slug for the bottom-funnel CTA). The brief panel on the action page now reads `What Blog Article Writer Pro knows about <client>` for this action so it's clear which agent is loaded.",
      "**📖 Best practices are accessible IN the app.** New `src/components/blog-writer-standard-panel.tsx` (server component) renders five collapsible cards above the form: 🌍 Language rule · 🛡️ Brief triple-check · 📝 WonderAds writing standard (keywords, headings, intro, lists, linking, meta, CTAs, images, tone) · 🧭 Process (5 mandatory steps) · 📤 Output format + brief-check appendix. Each card uses `MarkdownView` and renders the EXACT same constants the agent receives in its system prompt — `BLOG_WRITER_LANGUAGE_RULE`, `BLOG_WRITER_BRIEF_CHECK`, `BLOG_WRITER_STANDARD`, `BLOG_WRITER_PROCESS`, `BLOG_WRITER_OUTPUT_FORMAT` exported from the same file. **One source of truth.** Edit `lib/blog-writer-prompt.ts` and the agent + the panel update together — they cannot drift.",
      "**📚 Distilled from the two WonderAds source documents.** The writing standard is a synthesis of the in-house **Guia da Redação** + **Guia de Criação de Conteúdos** Google Docs Andre pinned (read via the Google Drive MCP at build-time). Specific WonderAds-isms preserved: never title the final heading `Conclusion / Conclusão`, every heading followed by ≥50 words of body before any list/image (text/image ratio + readability), CTAs use a `help / care` register for clinics (`Recupere a sua qualidade de vida`, not `COMPRE JÁ`), images must have kebab-case filenames with the primary keyword baked in (`fisioterapia-lombar-almada.jpg`), and YMYL clients get hard limits on claims (no cures, no guaranteed outcomes, every clinical claim cited).",
      "**📦 Pending Review wiring.** When the article finishes generating, the prominent `Send for Approval` button uses category `Content` (was `Other`), so blog articles land in the right column of the public Pending Review table. Doc link continues to flow through the generic `/<slug>/preview/write-blog-article/<resultId>` HTML report (light-themed, Download-PDF button) — clients never see the DOCX. The article output itself includes a `Working Notes` block (intent + angle + external references with URLs + internal-link plan + CTA targets) and the `Brief check appendix` — both wrapped in `>` markdown blockquotes so they visually separate from the publishable copy, and both labelled as `consultant: REMOVE before publishing`.",
      "**🎯 Output structure — publication-ready Markdown.** Top of file: `Suggested URL slug` + `Meta title (NN chars)` + `Meta description (NN chars)` + declared `Language: en-US | pt-PT` + word-count target + reading level. Then H1 → intro (one of three explicit styles — rich-snippet definition / direct journalistic / storytelling — chosen on intent), at least two H2s each leading with a direct answer to the implied PAA question, real markdown lists (never inline `(1)…(2)…(3)…` paragraph runs), one FAQ section (4–6 Q&As, 40–90 words each), one conclusive-but-specific final H2 that segues into the bottom-funnel CTA. Mid-funnel CTA placed after a problem is established; bottom-funnel CTA at the end. Both CTAs point at client-owned destinations (never external).",
      "**🚫 Guard-rails the agent obeys.** No greeting, no preamble, no `In today's digital landscape` openers (banned AI-tells listed explicitly in the prompt). Never recommend a service the client doesn't sell. Never promise a cure. Never link to a competitor. Never invent statistics — if no credible source is found, rewrite the claim or drop it. If the requested topic itself conflicts with a Don't and the agent can't honourably write it, the agent STOPS at step 1 and outputs only a short block explaining which Don't blocks the angle + the rewrite the consultant should consider. No silent compromise.",
    ],
  },
  {
    version: "74.6",
    date: "2026-05-28",
    title: "Audit buttons auto-appear · Luana N. → André P. handover · clients see PDF in browser, not DOCX",
    highlights: [
      "**⏱ Send-for-Approval + Download buttons no longer require a page refresh.** When an SEO Audit (or any text-action) finished generating, the prominent buttons at the top of the result page stayed hidden until you manually hit refresh — they're rendered from the server-side `getHistoryEntry` lookup, which doesn't re-run automatically when the client-side runner finishes streaming. Wired `router.refresh()` into `src/components/result-runner.tsx` immediately after the `/save` call succeeds (mirroring what `meta-tags-runner.tsx` already does), and into `src/components/gmb-posts-runner.tsx` after the GMB result lands. Server component re-evaluates → buttons appear instantly, runner's streamed output stays in client state (useState ignores prop changes after mount, so the markdown view doesn't blink).",
      "**🤝 Luana N. → André P. handover across the SEO DPT.** `src/lib/client-overrides.ts` — the `LUANA` slug set renamed to `ANDRE`, same client roster (Aeger Prima · A. Domingos · Senior Resort · Safe Away · Clínica em Casa) but `getConsultantForSlug` now returns `André P.` and `getConsultantEmailForSlug` returns `andre@wonder-ads.com`. `CONSULTANT_ORDER` column header updated. **Cache key bumped `seo-clients-v2` → `seo-clients-v3`** in `src/lib/notion.ts` so any in-flight `unstable_cache` entries holding the old `Luana N.` value get evicted on the next request — without the bump, the SEO landing page columns could keep showing `Luana N.` for up to an hour after deploy. PDF/DOCX deliverables, in-app cards, footer email line — all flip together. (Old changelog entries that mention Luana N. stay as historical record.)",
      "**📄 Clients now open a branded HTML report in their browser instead of being handed a .docx download.** Previously the `Open` button in the public Pending Review table linked straight to `/api/seo-actions/.../docx` — mobile clients got a useless save dialog, desktop clients had to launch Word just to read an audit. New generic public preview at `/(public-review)/[slug]/preview/[action]/[resultId]/page.tsx` reads the action-history entry and renders it via a new `PublicReportView` client component: WonderAds brand header + client logo + light-themed markdown (its own scoped CSS class so it can't clash with the internal dark-themed `seo-markdown`) + footer with the consultant's email + a prominent `Download PDF` button that fires `window.print()`. Print CSS hides the toolbar so the saved PDF is clean.",
      "**🔁 Generic catch-all route covers everything text-based.** SEO Audit, Keyword Research, Client Roadmap, Monthly Report, all the Off-Page SEO actions, etc. — every action without a bespoke preview now flows through `/preview/[action]/[resultId]`. The static `gmb-posts` and `meta-tags` segments still take precedence (Next.js routes static-before-dynamic), so those keep their existing custom previews. PT clients get the European-Portuguese footer + Download button label automatically via `pickLang(slug)`.",
      "**🔒 DOCX stays internal-only.** Consultants still see the `Download DOCX` button on every result page for editing-before-sending. The `/api/seo-actions/.../docx` endpoint is unchanged — only the `SendToReviewButton`'s `docLink` for non-gmb / non-meta-tags actions stopped pointing at it. The internal Pending Review table renders the same row data, so consultants viewing their internal table get the new HTML report too (consistent UX, one source of truth).",
      "**🪲 Existing Pending Review rows still point at the old DOCX URL.** New entries from now on use the preview URL automatically. If you want to migrate the A. Domingos test row in the screenshot, either delete + re-send the audit (loses the `Changes Requested` status) or edit the `Doc Link` cell in the public table directly (paste `/a-domingos/preview/seo-audit/<resultId>` resolved against `wonder-ads-workspace.vercel.app`).",
    ],
  },
  {
    version: "74.5",
    date: "2026-05-21",
    title: "Brief line-clamp actually works · modal is now properly opaque · cleaner bucket picker",
    highlights: [
      "**🪲 `show more` / `show less` did nothing — fixed.** v74.4 tried to clamp brief items with Tailwind's arbitrary-property syntax (`[display:-webkit-box] [-webkit-line-clamp:3]`) — turns out Tailwind v4 doesn't compile that combination of arbitrary properties + vendor prefixes reliably, so the clamp CSS was never produced and the buttons toggled state with nothing to show for it. Switched to Tailwind's built-in `line-clamp-1` core utility. Brief items now show **one line** by default (per your spec), with the `show more` / `show less` toggle working correctly via a ResizeObserver that only renders the toggle when there's actually hidden text to reveal.",
      "**🌑 Modal overlap fix — backdrop bumped + competing gradient borders removed.** Previously the modal had `bg-black/85` backdrop + a `brand-gradient-border` on the modal frame. The page's own brand-gradient-border panels (Client Brief, Client Files, Target Keywords) were leaking through the 15% transparent backdrop AND visually merging with the modal's identical gradient — felt like the modal and the page were overlapping. Two changes:",
      "  - Backdrop now `bg-black/95 backdrop-blur-xl` — the page is essentially invisible behind the modal.",
      "  - Modal frame uses a neutral `border-white/10` + soft purple drop shadow instead of the brand gradient. No more border-on-border clash; the modal reads as a clean floating panel.",
      "**🎨 Bucket picker tightened.** Dropped the `Move to` label per request — edit mode now shows just the three pill toggles (`DO'S · DON'TS · NOTES`) in the card header. The active bucket re-themes the card colour live so the destination is always visually obvious without needing the label.",
    ],
  },
  {
    version: "74.4",
    date: "2026-05-21",
    title: "Call Notes modal layout fix · bucket switcher in edit · brief panels stop growing",
    highlights: [
      "**🪜 Modal layout fix.** v74.3's deck had everything inside one big scroll container — when the card grew (long reasoning, edit mode with bigger textarea) the action buttons could slip below the visible area and the keyboard hint floated awkwardly under the buttons with no breathing room. New structure: **fixed header · scrollable body (source + progress + card) · FIXED FOOTER (action buttons + keyboard hint)**. The action bar is now always within reach regardless of how tall the card grows. Modal max-height bumped 70vh → 85vh.",
      "**🎯 Edit-before-approve now also lets you change the bucket.** New `Move to: DO'S | DON'TS | NOTES` segmented picker appears in the card header during edit. Clicking a different bucket re-themes the card colour LIVE (green / red / violet) so you can see what you're committing to. The bucket override flows through `accept(s, { text, bucket })` so the new bucket is honoured both in the brief PUT and in the in-memory suggestion list.",
      "**📏 Brief panels stop growing with long items.** Two changes inside the Do's / Don'ts / Notes panels:",
      "  - Each item display now line-clamps to 3 lines (CSS `-webkit-line-clamp: 3`). A `ResizeObserver` checks whether the unclamped text actually overflows — only renders the `show more` / `show less` link when there's something to reveal. No useless toggles on short items.",
      "  - The `<ul>` containing items now has `max-h-[42vh] overflow-y-auto pr-1` — a brief with 20 items scrolls inside its panel instead of pushing Target Keywords / SEO Actions off-screen.",
      "**✏️ Brief item editor now multi-line.** EditableRow's single-line `<input>` was cramming long items into a one-line field — the most common reason brief items got harder to read after a Call Notes session. Now uses a `<textarea>` with auto-sized rows (2-6 based on content). Cmd/Ctrl+Enter saves, Esc cancels.",
      "**🧹 Cleaner edit textarea visual.** Removed the nested border that made the edit textarea look like a card-within-a-card. Now uses a single ring (1px static → 2px amber focus) on a soft white/[0.04] background. Reads as part of the card, not a stranger inside it.",
    ],
  },
  {
    version: "74.2",
    date: "2026-05-21",
    title: "Call Notes goes link-only — paste a Fathom URL, no copy-paste needed",
    highlights: [
      "**🔗 Add Call Notes now accepts a Fathom share URL directly.** Paste `https://fathom.video/share/…` → app calls Fathom's official REST API (`GET /external/v1/meetings?include_transcript=true&include_summary=true` with `Authorization: Bearer FATHOM_API_KEY`) → paginates up to ~250 most-recent meetings → matches by `share_url` → pulls AI summary + action items + transcript → hands the lot to the existing Claude analyzer. No copy-paste needed.",
      "**🪜 Three-stage architecture in `src/lib/fathom-api.ts`.** Designed so today's single-team-key setup graduates cleanly:",
      "  - **Stage 1 (now)** — single `FATHOM_API_KEY` Vercel env var, generated from one team Fathom account. Whole team uses it.",
      "  - **Stage 2 (later)** — per-user key cached in localStorage if you ever onboard consultants who don't share a Fathom workspace.",
      "  - **Stage 3 (much later)** — full OAuth per-user when you sell this or scale past ~5 consultants.",
      "  Swapping stages = changing where `apiKey` is read in one file. Routes + UI stay the same.",
      "**🪲 Graceful fallback when something goes wrong.** If `FATHOM_API_KEY` is missing, the URL is malformed, Fathom rejects the key, or the recording isn't in the first ~250 meetings, the modal surfaces a clear error AND auto-reveals the paste-AI-summary fallback. No dead-ends — consultant can always finish the task.",
      "**🛡️ Real error codes through the stack.** `FathomApiError` exposes `code: \"no_key\" | \"not_found\" | \"upstream\" | \"rate_limited\" | \"bad_url\"` + correct HTTP status (503 / 404 / 502 / 429 / 400). UI inspects `code` from the response and reacts (auto-show paste view on `no_key`, etc.).",
      "**🧠 AI summary > raw transcript.** When the AI summary is available (which is almost always for Fathom meetings), the analyzer gets that first — it's pre-filtered for signal. Action items + transcript follow as additional context. Truncates at 59k chars (under the 60k analyzer ceiling) if the transcript is huge.",
      "**📋 Source title shown on suggestion view.** After processing, the modal shows `Source: <meeting title> · fetched from Fathom` above the suggestion cards so consultants know what they're reviewing.",
      "**⚡ Rate limit headroom.** Fathom's 60 calls/min limit is global per key; processing a call hits 1-5 calls (paginating to find the match). Plenty of margin for a busy day.",
    ],
  },
  {
    version: "74.1",
    date: "2026-05-21",
    title: "Add Call Notes — paste a Fathom call, get filtered Do/Don't/Note suggestions",
    highlights: [
      "**📞 New `Add Call Notes` button on every Client Brief header.** Workflow: paste the Fathom AI summary (or full transcript) → Claude filters out the scheduling / chit-chat / pleasantries and surfaces only items that actually belong in the brief → each suggestion appears as a card with Accept / Decline buttons → accepted items land in the right bucket (Do's / Don'ts / Notes) instantly via the existing brief-save endpoint.",
      "**🧠 Smart bucketing per language.** Claude reads the call in the client's language (PT-PT for Mimus, EN for IHN-CA, etc.) and writes each suggestion in the same language. Bucket rules baked into the system prompt:",
      "  - `dos` — preferences / positive behaviours / strategies the client likes",
      "  - `donts` — explicit prohibitions / past mistakes / brand-voice violations",
      "  - `notes` — neutral context (audience, business model, stakeholders, constraints)",
      "  Filters out tasks (\"will send logo Tuesday\") — those go in the roadmap, not the brief.",
      "**🎯 De-dupes against the existing brief.** Sends the current Do's / Don'ts / Notes to Claude as context so it skips anything already covered (semantically, not just lexically).",
      "**💡 Each suggestion has a `Why this matters` line.** Helps consultants decide accept/decline at a glance. Optional verbatim source quote shown as a blockquote when Claude could anchor the item to a specific line.",
      "**ℹ️ Fathom transcript fetching note.** Fathom doesn't expose share-page transcripts to unauthenticated HTTP fetches — confirmed 404/500 on every endpoint, the SPA renders via authenticated XHR. So the modal asks for the AI summary text directly (paste from the right-panel of the Fathom share page). Adds ~5s vs auto-fetching but ships today without needing Fathom API setup. Field for the share URL is still there for reference/audit.",
      "**🪜 Capped at 10 suggestions max.** Quality over quantity — Claude picks the highest-value items if more were found. Zero suggestions is a valid output if the call was purely status-update content.",
    ],
  },
  {
    version: "74.0",
    date: "2026-05-21",
    title: "Public client pages now localise to European Portuguese for PT clients",
    highlights: [
      "**🇵🇹 European Portuguese (pt-PT) on every client-facing public page** — switches automatically based on `getClientGeo(slug).languageCode`. PT clients (Mimus, Sea Yourself, IHN-PT, etc.) see the page in Portuguese; EN clients (IHN-CA, Insync Design, etc.) stay in English. Three pages localised:",
      "  - **Meta Tags preview** (`/<slug>/preview/meta-tags/<resultId>`) — badge, stats line, intro paragraph, footer.",
      "  - **GMB Posts preview** (`/<slug>/preview/gmb-posts/<resultId>`) — badge, stats line, intro paragraph, footer.",
      "  - **Pending Review table** (`/<slug>/pendingreview`) — badge, intro line, footer.",
      "**🧰 New `src/lib/public-i18n.ts` helper.** Centralised phrase catalog with `t(lang, key, vars)` lookup + `{name}` token substitution. Adding a fourth public page is a single import + a few `t()` calls. Adding Spanish / French later = adding a column to each phrase. Designed to scale — no per-component string trees.",
      "**🇵🇹 PT-PT correctness baked in.** Uses Portugal vocabulary (`elaborado`, `palavras-chave`, `aprovações`) and `tu`/2nd-person, NOT Brazilian Portuguese (`elaborado em` not `criado em`, `pesquisa de palavras-chave` not `pesquisa de palavras-chave` 😉, no `você`). Footer tagline becomes `Agência de Crescimento em Saúde & Bem-estar · #1 SEO em Portugal`.",
    ],
  },
  {
    version: "73.5",
    date: "2026-05-21",
    title: "Send-on-top button stack, fresh-generation bug fixed, Pending Review page feels instant",
    highlights: [
      "**⬆️ Send-for-Approval moved to top of the right-side button stack.** Previously it floated on the LEFT while CSV/DOCX downloads stacked on the right — split focus. Now a single right-aligned column: `Send for Approval` (brand-gradient, on top), `Download CSV`, `Download DOCX`. Same restack applies to GMB Posts + every other action's result page for consistency.",
      "**🪲 New meta-tags generation was showing the PREVIOUS result's data — fixed.** v73.4's \"fall back to latest meta-tags result if exact ID misses\" optimisation mis-fired on fresh runs: when the page loaded BEFORE generation completed, it'd serve the previous result's 25 rows instead of waiting. Two-part fix:",
      "  - Removed the fallback. The page now does a strict exact-ID lookup. Since v73.4 already made `meta-generate` honour `body.resultId` from the URL, new runs are self-consistent end-to-end without needing the fallback.",
      "  - Runner now calls `router.refresh()` after generation completes, so the server-rendered Send/Download buttons at the top of the page re-evaluate against the freshly-saved result automatically — no manual page refresh needed.",
      "**⚡ Pending Review page felt instant now (was ~10s on cold function).** Three changes:",
      "  - New `loading.tsx` at `/seo/[slug]/review` — Next.js shows an instant skeleton (logo placeholder + title + 4 table-row shimmers) the moment the consultant clicks the chip, instead of 10s of blank screen while SSR runs.",
      "  - Parallelised the two awaited fetches (`getClientBySlug` + `listReviewItems`) with `Promise.all` — saves one round-trip on warm requests.",
      "  - Link prefetch on the chip already warms the shell when consultants hover; the loading boundary closes the visual gap on actual click.",
      "  Cold function spin-up + Notion warm-up still cost ~3-5s in absolute terms, but consultants now see the page chrome immediately and the table fills in as data lands. Perceived perf is the win.",
    ],
  },
  {
    version: "73.4",
    date: "2026-05-21",
    title: "Slimmer header, meta-tags result page now sendable + downloadable instantly",
    highlights: [
      "**📐 Client page header trimmed.** Removed the per-task chips that piled up next to the client name (`Keyword Cluster Mapping…`, `Blog Article #1…`, `GMB Post Week 1…`). Header now shows just the two essentials:",
      "  - `Week N of 12 · X tasks pending` (current week summary)",
      "  - Green or red past-weeks status chip",
      "  Individual tasks live on the roadmap board itself — clicking the summary chip jumps you there.",
      "**🪲 Meta-tags result page was stuck on \"Download available soon\" — fixed.** Root cause: `meta-generate` was generating its own meta-tags-store ID server-side and ignoring the URL `resultId` that the runner sent in the request body. Result: the page-level `getMetaTagsResult(slug, urlResultId)` lookup always returned null → buttons stayed disabled, even after generation completed. Two-part fix:",
      "  - `meta-generate` now honours the `resultId` from the request body and uses it as the store key. New runs are self-consistent.",
      "  - Page falls back to `listMetaTagsResults(slug)` and grabs the latest if the exact ID misses — covers pre-v73.4 runs so existing results work too.",
      "**📤 Send-for-Approval + downloads light up the instant generation finishes.** Same logic as GMB Posts:",
      "  - Prominent brand-gradient `Send for Approval` button at the top of the result page, points the doc link at the public preview at `/<slug>/preview/meta-tags/<resultId>` (client-friendly, no internal app leaks).",
      "  - `Download CSV` — one row per page, drops into Yoast / Webflow / WordPress bulk edit.",
      "  - `Download DOCX` — Word doc with the current vs optimised table for review packets.",
    ],
  },
  {
    version: "73.3",
    date: "2026-05-21",
    title: "Meta tags: lenient schema + generateText salvage layer (definitive fix for \"schema\" error)",
    highlights: [
      "**🪲 Meta tags: \"No object generated: response did not match schema\" — properly fixed this time.** v73.2's chunking didn't help because the underlying problem wasn't truncation — it was the Zod schema rejecting Claude's natural output (e.g. a 32-char title for a thin `/privacy` page would fail `min(40)`, killing the whole chunk's 10 rows). New three-layer defense:",
      "  - **Layer 1 — Lenient schema.** Title accepts 5-120 chars, meta accepts 20-300 chars. System prompt still pushes for 40-60 / 120-160 — we just let imperfect outputs through instead of nuking the chunk.",
      "  - **Layer 2 — Same call, one retry.** Existing chunked + retried `generateObject` from v73.2 still runs.",
      "  - **Layer 3 — `generateText` + manual JSON parse salvage.** If both `generateObject` attempts fail, the chunk falls back to free-form generateText with an explicit JSON-only instruction, then we hand-parse the response (`extractJsonObject` with balanced-brace scan + `\\`\\`\\`json` fence stripping), and re-validate each row with Zod. Bad rows get dropped; good rows land. One bad row no longer takes the chunk down.",
      "  Net result: Claude has to literally return zero parseable rows for the run to fail — and even then we get a real underlying-error log instead of a schema mismatch.",
      "**📏 Length issues auto-flagged in `issues[]`.** Since the schema is lenient now, the consultant might see a 32-char optimized title. Server-side post-processing (`annotateRowLengths`) auto-appends to `issues[]`:",
      "  - `optimized title under 40 chars (N) — manually lengthen` when title floor missed",
      "  - `optimized title over 60 chars (N) — risk of SERP truncation` when title ceiling exceeded",
      "  - Same for meta (under 120 / over 160).",
      "  So the consultant gets the row + a clear flag of what needs touching up. Nothing silent.",
      "**🔍 Better failure logging.** Chunk-level `logChunkError` now records `name: message` + cause (when AI SDK exposes it) so the Vercel logs make it obvious whether failures are a rate limit, content policy block, or actual schema issue. v73.2's log just said `failed:` — now it says `[meta-generate] chunk 2 attempt 2 failed: AI_NoObjectGeneratedError: response did not match schema | cause: title length must be ≥40`.",
      "**🧪 Tested live on Mimus + one other client before shipping.** No more \"try Quick depth\" suggestion — Standard now works at Mimus's full 25-page depth.",
    ],
  },
  {
    version: "73.2",
    date: "2026-05-21",
    title: "SEO DPT icon, roadmap past-week status chip, Accesses vault, chunked meta-generation (fixes \"schema\" error)",
    highlights: [
      "**🪲 Meta tags: \"No object generated: response did not match schema\" — fixed.** Root cause: full-batch generateObject for 25+ pages was either truncating mid-JSON (output token ceiling) or returning one bad row that invalidated the whole array. Now chunked: pages split into batches of 10, each batch hits Claude in PARALLEL via Promise.all, with one automatic retry per chunk on failure. Three wins:",
      "  - Each chunk's output fits comfortably under the schema validator's ceiling (no truncation).",
      "  - A transient blip on one chunk no longer kills the whole run.",
      "  - **Partial success**: 4/5 chunks succeeding now returns 40 rows instead of 0. The result UI surfaces a `⚠ N of M chunks failed` warning when this happens.",
      "**🎨 SEO DPT header gets its icon.** Passed `Icon={Gauge}` to `DepartmentHeader` so the SEO landing page gets the same brand-gradient icon block ADS DPT etc. have. The plain dark header looked weak; gradient icon gives it visual anchor without bloating the layout.",
      "**🟢🔴 Roadmap strip past-week status chip.** Next to the current-week summary, a colored chip now appears from Week 2 onwards:",
      "  - `Weeks 1–N all done` in emerald when zero past-week tasks remain non-implemented",
      "  - `Weeks 1–N have X tasks pending` in rose when something slipped",
      "  Computed live from the roadmap board state — consultant sees at a glance whether the project is on-track.",
      "  Current-week summary also reworded to `Week N of 12 · X tasks pending` (only counts non-implemented tasks; implemented ones don't need attention).",
      "**🔐 New Accesses section on every client page.** Per-client credentials vault (host login, WordPress backend, cPanel, Mailchimp, GA4 / GSC owner — whatever the client hands us). Each entry: label, login URL, username, password, notes. Passwords MASKED by default with click-to-reveal + copy buttons. Add / edit / delete inline. Stored in KV (private to the workspace; KV encrypted at rest by Vercel/Upstash). New `Accesses` entry in the in-page section nav.",
    ],
  },
  {
    version: "73.1",
    date: "2026-05-20",
    title: "Meta Tags: full sitemap coverage, top-agency-grade title rules, batch-only approval",
    highlights: [
      "**🪲 Sitemap was being capped at 12 URLs regardless of depth — fixed.** `discoverSitemap(url, { maxUrls })` defaults to 12; v73.0 wasn't passing the consultant's depth (25 / 50) so Mimus only ever got 12 pages. Now `maxUrls = depth.maxPages` is passed through. Mimus scan at Standard goes from 12 → 25+ pages.",
      "**🕸️ Homepage-link fallback.** When the sitemap returns fewer URLs than the requested depth, the action now crawls the homepage's internal `<a href>` links and adds same-origin URLs to the pool until depth is hit. Catches all the doctor / service / location pages clinic sitemaps tend to omit. Skips assets (jpg/pdf/zip/css/js), external domains, anchors, mailto, tel, javascript:.",
      "**📏 Title floor 40 chars (HARD).** Schema `min(40)` — generateObject now rejects + retries any title under 40 chars. System prompt also lists the lengthening playbook: add geo modifier / specialty / role / brand with ` | ` separator. The v73.0 \"Susana | Dentista MIMUS\" (23 chars) result is now impossible.",
      "**📐 Top-agency-grade best practices baked into the system prompt.** Rewritten around what Backlinko / Ahrefs / Moz / Search Engine Journal treat as table stakes for 2026 Google SERPs:",
      "  - Title 50-60 (never <40, never >65) · primary keyword in first 60% of title · brand at the end with ` | ` separator · one focus per page · every title unique · Title Case in EN / sentence case in PT-ES-FR · no ALL CAPS · no year-stamping unless the page is year-specific.",
      "  - Meta 120-160 (never <120, never >170) · lead with value prop (first 90 chars = mobile cutoff) · primary keyword exactly once · soft CTA verb at the end localised per language · NEVER repeat the title.",
      "  - YMYL safety: no medical claims, no outcome promises, no `garantido / sem dor / 100%`. Brief Do's / Don'ts override best practice.",
      "**📝 Shorter blurb on the action card.** `Crawls every page, audits the current title + meta, drafts SERP-optimised rewrites from the latest Keyword Research.` — one line, scannable.",
      "**🔘 Generate button reads what it does.** New `ActionDef.generateButtonLabel` override; meta-tags shows `Scan current Meta Tags and SEO Optimize` instead of the generic `Generate`. Reusable for any future action that benefits from a more descriptive label.",
      "**🧹 Removed per-row Approval buttons.** Only the prominent batch `Send for Approval` button at the top stays — clients review the entire batch together on the public preview, no scattered approvals.",
      "**📊 TITLE_IDEAL_MIN bumped 30 → 40** on the char-count indicators so the UI matches the new floor (anything under 40 shows amber `short`).",
    ],
  },
  {
    version: "73.0",
    date: "2026-05-20",
    title: "Meta Titles & Descriptions — site-wide crawler + Keyword-Research-grounded rewrites + approval loop",
    highlights: [
      "**🪧 New site-wide Meta Tags optimiser.** Replaces the old single-page action with a real workflow: enter the client URL, pick depth (Quick = 10 / Standard = 25 / Deep = 50), and the action discovers the sitemap, crawls every page, pulls the current `<title>` + `<meta description>`, then drafts SERP-optimised rewrites grounded in the **latest Keyword Research** for that client. Optional focus keywords on the form for last-minute steer.",
      "**🔒 Blocks until Keyword Research exists.** Hard error: `No Keyword Research result found for this client. Run Keyword Research first, then come back — meta-tag optimisation depends on the keyword clusters to assign primary keywords per URL.` Forces the right sequence; prevents weak \"brief-only\" output.",
      "**🎯 Claude auto-assigns primary + secondary keywords per URL.** Per-row generation reads the full KW cluster map + the URL + current title + H1 and picks the best-fit primary keyword (with 1-3 supporting keywords from related clusters). One-line reasoning shown next to each row. URLs with no commercial intent (/privacy, /terms, /404) explicitly get `primaryKeyword: null` so they don't get keyword-stuffed.",
      "**📋 Per-page Current vs Optimised side-by-side.** Result page renders each URL as a card: clickable URL header + a 2-column body with `Current title / Optimised title` and `Current meta / Optimised meta`. Char-count indicators under each editable field colour-coded against the SERP sweet spots (title 30–60 / meta 120–160 — green inside, amber short, rose over). Optimised columns are inline-editable; debounced auto-save.",
      "**🪪 Issues + secondary-keyword chips per row.** Claude flags problems on the CURRENT tags (`missing meta`, `title too long`, `brand-only title`, `no primary keyword`, etc.) as amber chips. Secondary keywords show as white chips. Header stats: how many pages were missing metas / had over-long titles — surfaces the quality of the existing site at a glance.",
      "**🚀 Send for Approval — both batch and per-row.** Prominent `Send for Approval` button at the top of the result page (sends the whole batch). Compact `Approval` button per URL row (sends just that page). All point at the new public preview at `/<slug>/preview/meta-tags/<resultId>`.",
      "**🪟 Public preview — same locked-down pattern as GMB Posts.** Read-only side-by-side table at `/(public-review)/[slug]/preview/meta-tags/[resultId]`. No PageShell, no app chrome, noindex/nofollow, SEO DPT badge, consultant email in footer. Client cannot edit, cannot navigate up into other clients.",
      "**📦 CSV + DOCX exports.** Top-of-page download buttons.",
      "  - **CSV** for the dev team: one row per page with URL, current title, optimised title, char counts, current meta, optimised meta, primary keyword, secondary keywords, reasoning, issues. Drops straight into Yoast / Webflow / WordPress bulk-edit tools.",
      "  - **DOCX** for the client packet: heading per URL, side-by-side current vs optimised tables for both title and meta, primary keyword + reasoning per page.",
      "**🧱 Plumbing.** New `src/lib/meta-tags-store.ts` (types + KV CRUD), 4 new API routes (`/meta-generate` NDJSON stream, `/meta-result` GET, `/meta-update` PATCH per row, `/meta-export` CSV+DOCX). New `<MetaTagsTable>` + `<MetaTagsRunner>` components. Action def updated with new `titleTemplate`, depth-select, focus-keywords textarea. Pre-fill of website URL from client metadata.",
    ],
  },
  {
    version: "72.2",
    date: "2026-05-20",
    title: "Pending Review: \"Approval\" button label, public preview routes (no app leak), auto-fill approval date",
    highlights: [
      "**🔒 Doc link no longer leaks the internal app.** Critical fix. The v72.1 link to `/seo/<slug>/actions/gmb-posts/results/<id>` let clients climb up the URL into `/seo` (every other client's data) by hitting the back button. Replaced with two safe targets:",
      "  - **GMB Posts** → new public preview at `/(public-review)/<slug>/preview/gmb-posts/<resultId>` — read-only post cards, no editing, no PageShell, no navigation back into the workspace. Same minimal layout the pending-review table uses (noindex/nofollow, consultant-email footer, SEO DPT badge).",
      "  - **SEO Audit / Keyword Research / Roadmap / Monthly Report** → doc link points at the existing public DOCX endpoint (`/api/seo-actions/.../docx`) — a file download, not a navigable page.",
      "**🏷️ \"Send\" button renamed to \"Approval\" everywhere.** Compact variant on GMB cards: `Approval` (sent state: `Sent`). Prominent variant at top of result pages: `Send for Approval` → `Sent for approval`. Same Send icon kept for visual consistency with the existing chip.",
      "**🗓️ Approval date auto-fills server-side.** PATCH route detects when status flips to `Approved` AND the row's `approvalDate` is null, and sets it to today's date (`YYYY-MM-DD`). Doesn't clear on status flip away from Approved (preserves the original approval timestamp). Internal staff can still manually override.",
      "**🔐 Approval-date column read-only on client side.** New `readonlyApprovalDate` prop on `ReviewTable`. Public page passes true; date renders as DD/MM/YYYY static text. Internal staff view keeps the date input editable. Clients literally cannot type wrong dates — the only interaction they have is the status pill, and the date fills automatically the moment they hit Approved.",
    ],
  },
  {
    version: "72.1",
    date: "2026-05-20",
    title: "Pending Review polish: red For-Approval, refresh + auto-poll, per-post Send button, public-side trim",
    highlights: [
      "**🚨 \"For Approval\" pill is now loud red with a leading `!`.** Light blue blended into the background — clients scanned past it. Now it pops vs the calmer Approved / Changes-Requested states (red bg, dark-red text, `! For Approval` label). Used on both public + internal.",
      "**🔄 Refresh button + auto-poll on both sides.** The table now polls `/api/reviews/[slug]` every 12 seconds while the tab is visible, merging remote changes into local state without clobbering whatever the user is currently typing. Dedicated `Refresh` button in the footer for impatient moments + `Last synced 3s ago` indicator. Polling pauses on hidden tabs (KV-quota-friendly) and re-fires immediately on visibility-regain.",
      "**🚀 Per-post `Send` button on every GMB post card.** Next to Image / Copy / Edit you now have a compact `Send` button that ships THIS specific post (not the whole batch) to the client's pending review table. Task label = `GMB {Type}: {first 100 chars of caption}`. Solves the v72.0 gap where the consultant could only send the batch.",
      "**✨ Top-of-page Send-to-Review button promoted to brand-gradient prominent variant.** New `prominent` variant on `SendToReviewButton` — same brand gradient + shadow as Download PDF / Download batch — so the universal Send button at the top of every result page is impossible to miss. The v72.0 muted variant was getting missed by consultants.",
      "**📋 Public-side trim:**",
      "  - `SEO DPT` badge added next to `Pending Review` so clients understand which department this list belongs to (relevant once ADS DPT ships their own).",
      "  - Footer line gained `· #1 SEO Provider in Portugal` after the agency tagline.",
      "  - `Reply to the email this link came from — we'll get back to you within one business day` replaced with the actual assigned consultant's name + email (mailto link). Pulls from `getConsultantEmailForSlug(slug)` — same mapping the PDF cover uses.",
      "  - `Publishing date` column hidden on the client view (it's an internal scheduling field, not something the client needs to set). Still editable on the staff side.",
      "**🔗 `Open` button on the doc-link cell.** Replaced the tiny grey ↗ arrow with a brand-gradient `Open` button next to the URL field — clients can't miss the doc link anymore.",
    ],
  },
  {
    version: "72.0",
    date: "2026-05-20",
    title: "Pending Review tables — public per-client, internal mirror, Send-to-Review on every result",
    highlights: [
      "**📋 Public Pending Review page per client.** New URL `/<slug>/pendingreview` (e.g. `/sea-yourself/pendingreview`) — no auth, no app chrome, no back-button into the workspace. Clients land on a clean Wonder-Ads-branded page with their clinic name + an editable table they can flip status / dates / doc links / notes on inline. Auto-save on every change.",
      "**🪑 Editable columns + colored pills.** Task (text) · Status (colored pill: For Approval / Approved / Rejected / Changes Requested) · Category (colored pill across all SEO pillars + GMB + KW + Roadmap) · Approval date · Publishing date · Doc link (clickable). Per-row save state shown bottom-right (`All changes saved · N items` / `Saving 1 change…`).",
      "**🚀 Send to Pending Review on every result page.** New `SendToReviewButton` component drops on every action result (SEO Audit, Keyword Research, GMB Posts, Client Roadmap, etc.). One click → appends a row to that client's review table, pre-filled with the action label, the right category, and a doc link back to the result page. Consultants stop copy-pasting URLs.",
      "**🪞 Internal mirror at `/seo/<slug>/review`.** Same data, same component, but inside PageShell + with `Copy public link`, `Open public link in new tab`, manual add-row, and per-row delete. Consultants see exactly what the client sees and edit the same store.",
      "**📌 Header chip on every client page.** `Pending Review · 3 for approval` chip in the client header, links to the internal mirror. Amber pill when there are items awaiting client approval; emerald pill when everything is approved.",
      "**🔒 Privacy guardrails.** Public layout uses its own `(public-review)` route group with NO PageShell — meaning no department picker, no client list, no internal navigation leaks. Public page also has `robots: noindex, nofollow` metadata so the link never gets indexed by Google. Reserved slugs (`seo`, `ads`, `api`, etc.) return 404 to prevent accidental catch-all matches.",
      "**🧱 Plumbing.** `review-store.ts` (types + KV CRUD + status/category pill colors + payload sanitisation for the public PUT endpoint). New API: `GET/POST /api/reviews/[slug]` and `PATCH/DELETE /api/reviews/[slug]/items/[id]`. Components: `ReviewTable` (shared), `CopyPublicLinkButton`, `AddReviewItemButton`, `SendToReviewButton`, `PendingReviewChip`.",
    ],
  },
  {
    version: "71.13",
    date: "2026-05-20",
    title: "GMB Posts form: per-post-type fields, segmented control, custom title",
    highlights: [
      "**🏷️ Form title is now per-action.** `ActionDef.titleTemplate` lets each action override the default `New generation for {client}`. GMB Posts opens with **`Create GMB Posts for {client}`**. Other actions keep the default.",
      "**🎛️ Image source as a 2-way segmented control.** The dropdown that buried the choice is replaced by a button group — both options (`Use client's photos` / `AI-generate (Gemini)`) are visible at all times, current pick rendered in the brand gradient. New generic `segmented` field type added to `ActionField`; reusable for any future 2–4 option binary choice.",
      "**📋 Structured per-post-type fields.** New `showWhen` conditional rendering on `ActionField`. Selecting a post type now surfaces the fields you'd expect on Google's own GMB post form:",
      "  - **Offer:** offer headline · discount (amount or %) · valid-from + valid-until dates · terms",
      "  - **Event:** event title · start date · end date · time · location · details",
      "  - **Product:** product/service name · price · highlights",
      "  - **Update:** free-form spotlight textarea (the previous behaviour)",
      "Server-side `buildHardFactsBlock(postType, inputs)` collapses these into a clean markdown bullet list and inserts it into the Claude prompt's `## Hard facts that MUST appear literally` block, so captions can include the actual discount %, the actual event dates, the actual price — verbatim — instead of guessing.",
      "**📅 New `date` field type** that renders as `<input type=\"date\">` with dark color scheme. Powers the Offer + Event date pickers.",
      "**🧠 Required validation respects visibility.** Hidden conditional fields don't trip the `Fill in the required fields to generate` warning — the form only enforces what's actually on screen.",
    ],
  },
  {
    version: "71.12",
    date: "2026-05-20",
    title: "Filter HEIC/AVIF/TIFF out of the image pool",
    highlights: [
      "**🧹 Pool now only sees vision-safe mime types.** iPhone HEIC photos were getting picked, sharp's HEIC support is platform-dependent on Vercel, and Anthropic's vision endpoint rejects HEIC outright. New `SAFE_IMAGE_MIMES` whitelist in drive-fetcher: `jpeg / png / gif / webp` only. HEIC + AVIF + TIFF are silently skipped during folder traversal so the random sampler can't pick something that would later fail downstream. Same formats Claude vision accepts AND browsers render natively for the actual GMB post image.",
    ],
  },
  {
    version: "71.11",
    date: "2026-05-20",
    title: "Shrink client photos before Claude vision (was Payload Too Large)",
    highlights: [
      "**🪛 Client-files mode now downscales before sending to Claude.** Full-res clinic photos (4-8MB JPEGs) were tripping Anthropic's vision payload ceiling (~5MB). New `shrinkForVisionInput` (uses the `sharp` already in the project via Next.js Image) resizes to 1024px longest edge + JPEG quality 80 with mozjpeg encoding — typical output is 150-400KB. The ORIGINAL bytes still go to Vercel Blob as the post image; the thumbnail is only for vision comprehension.",
      "**🛟 EXIF rotation honoured.** Portrait-orientation phone photos no longer end up sideways in the caption-writing step (sharp's `.rotate()` applies the EXIF orientation flag before resizing).",
      "**🪂 Sharp shrink failure has a fallback.** If `sharp` chokes on a weird format / corrupt file, we fall back to the original bytes and let Claude's own size check fail loud with a parseable error.",
    ],
  },
  {
    version: "71.10",
    date: "2026-05-20",
    title: "Drive 403 — surface the actual Google error string",
    highlights: [
      "**🪪 Drive folder 403 now surfaces the message body.** Previously we just reported `HTTP 403`; the actual Google error string (`Insufficient Permission` vs `File not found` vs `The user does not have sufficient permissions for this file`) was only in Vercel logs. Now we parse the JSON `error.message` from the response body and append it to the consultant-facing diagnostic so you can immediately tell whether it's a scope-not-authorized issue or a sharing issue.",
    ],
  },
  {
    version: "71.9",
    date: "2026-05-20",
    title: "Drive listing now distinguishes 'empty folder' from 'auth-denied folder'",
    highlights: [
      "**🪵 listDriveFolderContents returns HTTP status.** Previously, sub-folders that 403'd silently returned an empty array — so the route couldn't tell whether the folder was empty or unreadable. Now we return `{ items, errorStatus }` and the traversal aggregates per-folder outcomes.",
      "**🔍 listImageRefsInDriveFolder reports actionable errors.** When the recursive traversal finishes with no images, the error string now says either `Drive folder traversal completed (N folders checked, depth ≤ 2) but no image files found` OR `N sub-folder(s) couldn't be listed (auth issue). Sample errors: SubFolderX → HTTP 403. Share the folder DIRECTLY with seo@wonder-ads.com (Viewer) — 'Anyone with the link' doesn't always grant Drive API listing access for nested folders.`",
      "**🪪 Vercel logs gained a per-traversal info line.** `[drive-fetcher] traversal of FOLDER_ID: 4 folders checked, 0 with errors, 3 contained images, 47 total image refs` — every Drive listing now leaves a one-line summary in the function logs.",
    ],
  },
  {
    version: "71.8",
    date: "2026-05-20",
    title: "Empty image-pool error now lists per-entry reasons",
    highlights: [
      "**🔎 No more inscrutable \"No images available\" toast.** When client-files mode finds zero images, the error event now lists every Client Files entry with its specific failure reason underneath: `✕ Fotos — Drive auth failed: …` or `✕ Fotos — Drive folder has no image files…`. Consultants can act immediately instead of guessing.",
    ],
  },
  {
    version: "71.7",
    date: "2026-05-20",
    title: "GMB Posts: 'Use client's photos' mode — pick a real photo + Claude vision caption (no Gemini)",
    highlights: [
      "**🪧 New first question on the form: 'Where should the image come from?'** Two choices: `Use client's photos` (default) — app picks a random photo from the client's Client Files library and Claude writes a vision-grounded caption that describes/uses what's actually in the photo; OR `AI-generate (Gemini)` — the previous flow with Gemini drafting an image from brand references. Same result page, same edit / download / status flow afterwards.",
      "**📸 No more wrong-logo problem in client-files mode.** Because the image IS the client's real photo (not a Gemini render), brand-fidelity is perfect by definition. No logo invention, no scuba-school-instead-of-clinic risk on the image side. Faster too (no image-gen API call) and free of Gemini billing.",
      "**🎲 Random sampling across the whole library.** App walks every uploaded photo + every image inside every Drive folder link (recursive depth 2, same auth as v71.5) and builds a single flat pool. Fisher-Yates shuffle picks N — gives consultants visual variety across runs without exposing a picker UI.",
      "**🧠 Claude Haiku vision per image.** For each picked image, the route sends a per-post call with the image bytes attached as a vision content part. Claude SEES the photo and writes a caption that contextualises what's visible (people, setting, mood) rather than generic templated copy. Same Zod schema (caption / CTA / target keywords / reasoning), minus the now-unneeded `imagePrompt`.",
      "**🛟 Pool-size guardrail.** If the library has fewer images than the requested post count, the route generates as many posts as photos available and surfaces a warning at the top of the result (`Only 2 image(s) available — generating 2 posts instead of the requested 3`). No silent under-delivery.",
      "**📦 Drive images get re-hosted on Vercel Blob.** Picked Drive photos are downloaded once + saved to Blob using the same path scheme Gemini-generated images use, so the result-page card has a stable public URL and per-image Download / batch ZIP both keep working unchanged.",
      "**🪪 Image-source chip on every card.** Bottom-left of the image: `📸 Client photo` (emerald) or `✨ AI image` (brand purple). Hover for the source filename (`Client photo from Sessão 9 → IMG_5612.jpg`).",
      "**🪛 Plumbing.** drive-fetcher gained `listImageRefsInDriveFolder` (list-only) + `downloadDriveImageById` (download-only) on top of the existing all-in-one helper, so the route can list once, sample N, download N. GmbPost gained `imageSource` + `imageOrigin` fields (optional for back-compat with v71.0-71.6 entries).",
    ],
  },
  {
    version: "71.6",
    date: "2026-05-20",
    title: "Drive FOLDER support — paste a folder link, app uses the photos inside",
    highlights: [
      "**📁 Drive folder links now work as Client Files references.** Previously v71.5's drive-fetcher only handled individual file URLs (`/file/d/<id>/view`). Pasting a folder URL (`/drive/folders/<id>`) failed with `Drive file isn't accessible` because folders have no bytes to download. Now: app detects folder URLs, lists every image inside via Drive API, recurses into sub-folders up to depth 2 (so `FOTOS / Sessão 9 — Abril 2026 / file.jpg` still resolves), and downloads up to MAX_REFERENCE_IMAGES (4) for Gemini.",
      "**🔍 Diagnostic now shows folder coverage.** When a folder is used, the references-used summary appends `4 of 47 images in folder used as references` so consultants see the cap is N-not-all without having to look it up. Each picked image is listed individually as `Fotos → IMG_5612.jpg` so it's obvious WHICH images Gemini actually saw.",
      "**⚙️ Depth + slot caps.** Listing is breadth-first so we pick images from each folder level before going deeper. Max recursion depth = 2 (covers the agency's typical session-photo nesting). Max images downloaded = remaining reference slot count (so a folder with 47 photos doesn't trigger 47 downloads when we only need 4).",
      "**🪵 Logging.** Drive API failures (folder list HTTP error, individual file media fetch error, etc.) log to Vercel function output with the specific folder/file id, so the next time something fails the dev tab points right at it.",
    ],
  },
  {
    version: "71.5",
    date: "2026-05-20",
    title: "Authenticated Drive fetch — read files shared with seo@wonder-ads.com without making them public",
    highlights: [
      "**🔐 Drive files no longer need to be public.** Most agency clients share their Drive files with `seo@wonder-ads.com` (Viewer access) rather than flipping them to anyone-with-link. The drive-fetcher now uses the **existing** service-account + domain-wide-delegation setup (same auth that already powers GSC + GA4) to read those files directly via the Drive v3 API. Two-step strategy: try authenticated first; fall back to the public anyone-with-link path if auth fails — both paths still work.",
      "**🧱 Shared Drives covered.** API calls pass `supportsAllDrives=true` so files clients put in a Shared Drive (not just My Drive) are reachable too.",
      "**🎯 Image-mime gate at the metadata step.** Authenticated fetch hits `/files/{id}?fields=name,mimeType` first, rejects anything that isn't an `image/*` mime before downloading the bytes. Avoids feeding PDFs or videos into Gemini as reference images.",
      "**📋 Failure copy updated.** When a Drive file can't be reached, the reason now reads `share it with seo@wonder-ads.com (Viewer is enough) OR make it anyone-with-link public` — actionable for either workflow.",
      "**Setup note — one-time Workspace admin step.** For authenticated fetch to work, the service account's domain-wide-delegation entry in Google Workspace Admin Console needs `https://www.googleapis.com/auth/drive.readonly` added to its scope list. If it's not yet authorized, the fetch silently falls back to the public path (no app crash). See the post-deploy message for the exact steps.",
    ],
  },
  {
    version: "71.4",
    date: "2026-05-20",
    title: "GMB Posts: references diagnostic, per-image + batch ZIP download, hard rule against invented logos",
    highlights: [
      "**🚨 Hard rule against invented logos.** The v71.3 build produced a wrong/distorted Sea Yourself logo. Fixed by stripping logo-inventing licence from the image prompt: \"Do NOT generate, invent, or guess a logo or brand mark. If a logo appears clearly in a reference image attached, you may reuse THAT EXACT logo; if you can't reproduce it accurately, leave the image logo-free entirely — a clean logo-free image is FAR better than a distorted or invented logo.\" Same rule applied to text overlays. Claude's `imagePrompt` schema description also now forbids requesting logos / brand names / text in the scene description.",
      "**🪪 References diagnostic on every result.** New `GmbPostsResult.referencesUsed[]` records every Client Files candidate's fate (used / failed / skipped) with a human reason — Drive links that 403, files with non-image mime-types, the cap-of-4-references rule, etc. Result page shows a collapsible summary: `✓ Used 3 brand references from Client Files · 1 failed` (or a prominent amber warning when 0 references loaded). Click to expand and see exactly which file failed, with the specific reason inline.",
      "**🖼️ Per-image download on every card.** New Download button next to Copy + Edit. Hits a proxy at `/api/seo-actions/[client]/gmb-posts/gmb-download?resultId=…&postId=…` that re-streams the Vercel Blob with `content-disposition: attachment` (Blob URLs serve `inline` by default, which opens in a new tab instead of saving). Filename = `{client}-gmb-{n}-{type}.png`.",
      "**📦 Batch ZIP download replaces \"Download available soon\".** Top-of-page button on GMB results is now `Download batch (N posts)`. Server-side `JSZip` packs every image + a `captions.txt` summary (post number, type, caption, localized CTA + URL, target keywords) into one ZIP named `{client}-gmb-batch-{resultId}.zip`. One click, one file, everything the consultant needs to schedule offline.",
      "**🛰️ Publish-to-GMB placeholder.** Disabled \"Publish (soon)\" button on every card so consultants see the roadmap; the actual GMB Business Profile API integration is a v72 task (needs OAuth + GMB API scope wiring).",
    ],
  },
  {
    version: "71.3",
    date: "2026-05-20",
    title: "GMB image gen: friendlier quota error + canonical model name",
    highlights: [
      "**🪙 Friendlier message when Gemini's billing isn't set up.** Gemini 2.5 Flash Image has a free-tier quota of **0** — image generation requires billing enabled on the linked Google Cloud project, even for the first request. We now detect `RESOURCE_EXHAUSTED` / `429` / `quota` strings in Gemini's error and re-throw with: `Google AI quota exhausted — Gemini image generation has NO free tier. Enable billing on the Google Cloud project this API key is linked to at https://console.cloud.google.com/billing (then regenerate; same key keeps working). At your volume the cost is ~$0.04 per image.` No more squinting at JSON to know what to do.",
      "**📛 Canonical model name first.** Google's API returns the canonical model in its error responses as `gemini-2.5-flash-preview-image` (note: `preview-image`, not `image-preview` — they swapped the order). Now the primary model id in our fallback chain. Older aliases stay as fallbacks for back-compat if Google renames again.",
    ],
  },
  {
    version: "71.2",
    date: "2026-05-20",
    title: "GMB Posts: kill the \"Nothing generated\" flash, surface actual Gemini errors, remove CTA helper line",
    highlights: [
      "**🪲 \"Nothing generated for this URL\" flash on Generate — fixed.** Root cause: `GmbPostsRunner`'s mount effect was double-firing (Strict Mode in dev + `startGeneration` dep churn from progressPct state updates). First fire correctly consumed sessionStorage and started generation; second fire saw the now-empty key and flipped status to `missing` — clobbering the in-flight generating UI. Fix: idempotent ref guard (`kickoffRef`) so the consume side-effect runs exactly once; also stabilised `startGeneration` by removing `progressPct` from its deps and using the functional setter form.",
      "**🪟 Actual Gemini error surfaced on every failed card.** Image-gen errors are now stored per-post (`GmbPost.imageError`) and the card renders the exact string the SDK / Gemini returned underneath the failure icon. So instead of the useless generic message, you'll see things like `promptBlockReason=SAFETY · finishReason=RECITATION` or `model returned no image bytes. Likely your API key tier doesn't have image generation enabled…` — enough to know whether it's a quota, a safety filter, or a missing permission.",
      "**🪵 Server-side logging.** Each Gemini call now logs the response shape (candidatesCount, finishReason, part types, promptFeedback) to Vercel function logs — strip the image bytes first so the logs stay readable. Useful for the dev tab: open the function for `/api/seo-actions/[client]/gmb-posts/gmb-generate` and you'll see exactly what Gemini returned for the failed call.",
      "**🧪 responseModalities now requests `[IMAGE, TEXT]`.** Some Gemini API versions reject IMAGE-only configs with \"modality IMAGE not supported alone\". Requesting both keeps the model honest about returning bytes while staying compatible across SDK versions.",
      "**🧼 Removed CTA helper text.** The \"Where the CTA button should send people. Defaults to the client's homepage when blank.\" line under the Default CTA URL field is gone — the label + placeholder already explain it.",
    ],
  },
  {
    version: "71.1",
    date: "2026-05-20",
    title: "GMB Posts fixes: mandatory site audit, working image gen, contact-page prefill, PT-PT CTAs",
    highlights: [
      "**🚨 Mandatory mini site audit before every GMB generation.** This was the root cause of the Sea Yourself → scuba diving disaster (mental-health clinic got caption about underwater diving because the brand name plays on \"sea\"). New `src/lib/seo-tools/mini-site-audit.ts` fetches the homepage + auto-detects + crawls a `/about` or `/sobre` sub-page, extracts title/meta/H1/body text/nav labels, and prepends the result to the Claude prompt as the **absolute-priority source of truth**. System prompt now contains a hard rule: \"NEVER infer from the brand name — the audit overrides the name.\" Generation aborts gracefully to a safe \"local-business welcome\" pattern when the audit can't disambiguate.",
      "**🖼️ Image generation actually works now.** Gemini was silently returning text-only descriptions instead of image bytes because the request was missing `responseModalities: [Modality.IMAGE]`. Added it. Also added a model fallback (`gemini-2.5-flash-image` → `gemini-2.5-flash-image-preview`) so a future Google rename doesn't break us. **Plus: errors now bubble up** — the progress stream surfaces the actual Gemini error message (\"quota exceeded\", \"safety filter blocked\", \"model not found\") instead of a generic \"image generation failed\".",
      "**📌 CTA URL auto-prefills with the client's actual Contact page.** New `src/lib/seo-tools/contact-page.ts` probes 11 common contact slugs (`/contactos`, `/contact`, `/contacto`, `/contactar`, `/kontakt`, etc.) against the client's website with HEAD requests; first 200-OK wins. Cached in-process per website for 6h. Pre-fills the CTA URL field server-side via the existing `defaults` prop on ActionRunner — no consultant re-typing the same URL across 14 clients.",
      "**🇵🇹 PT-PT (Portugal) not PT-BR — including CTA buttons.** System prompt now explicitly instructs European Portuguese with specific lexical examples (\"telemóvel\" not \"celular\", \"casa de banho\" not \"banheiro\"). CTA button labels render in the client's language at display time: Portuguese clients see `Saber mais` / `Reservar` / `Encomendar online` / `Comprar` / `Subscrever` / `Ligar agora`. ES, FR, IT, DE also wired up. Driven by `getClientGeo(slug).languageCode` — already populated for every roster client.",
      "**🧹 Result page state copy fixed.** The confusing \"No result yet + 100% progress bar\" overlap is gone. Progress card now hides the instant the result lands. \"Missing\" state (page opened directly without a generation in flight) gets dedicated copy with a link back to the action page, not the same \"No result yet\" string the post-success state was using.",
      "**Setup note.** No new env vars vs v71.0. If you're still seeing \"image generation failed\", check the surfaced error message — it now tells you exactly which Gemini issue triggered it.",
    ],
  },
  {
    version: "71.0",
    date: "2026-05-20",
    title: "GMB Posts Creation — AI captions + on-brand AI image generation grounded in client files",
    highlights: [
      "**🎨 Image generation grounded in the client's actual brand.** Switched the GMB Posts action from text-only to **image + caption** delivery. New `src/lib/seo-tools/gmb-image-gen.ts` wraps Google's Gemini 2.5 Flash Image (Nano Banana) and feeds it every uploaded image + every fetchable Google Drive file from the Client Files panel as reference material. The generator is explicitly told to match the references' photographic style, palette, and any visible logos — so generated images stay on-brand. Output saved to Vercel Blob, public URLs returned.",
      "**🔗 Google Drive integration (public links).** New `src/lib/drive-fetcher.ts` extracts the Drive file id from any of the common share-link shapes (`/file/d/<id>/view`, `?id=<id>`, `usercontent.google.com/download?id=<id>`), transforms to the direct-download URL, fetches the bytes, and gates on `content-type: image/*` so the Drive virus-scan-warning HTML can't accidentally be fed into Gemini as a reference. Any Drive link with anyone-with-link sharing works on day one.",
      "**✍️ Captions via Claude Haiku 4.5 with strict structured output.** Captions come from `generateObject` + a Zod schema (caption ≤1500 chars, CTA enum, CTA URL, target-keyword array, one-line reasoning, image prompt). Grounded in: Do's, Don'ts, Notes, onboarding form text, tracked target keywords, the consultant's theme + hard facts inputs. **Hard rules baked into the system prompt:** weave 1-2 target keywords naturally, never make medical claims, write Portuguese (Portugal) when the brief is Portuguese.",
      "**🪟 Real progress, not cycling fake messages.** The generator streams NDJSON events as it works: `Loading client brief… → Fetching reference images (N total) → Drafting N caption(s) with Claude → Generating N on-brand image(s) via Gemini Flash Image (parallel) → Saving to your workspace…`. The progress bar locks to actual phase, the bar message reflects what's actually happening server-side.",
      "**🪪 GMB-card preview UI with inline edit.** Each result renders as a real GMB-style card: square image, post-type chip, status chip, caption, target-keyword chips with 🎯 dots, CTA button, copy-caption, edit pencil. Click edit → caption + CTA + CTA URL + status all editable in place, character counter, save persists via `/gmb-update`. Status flows: draft → approved → published.",
      "**1–3 different posts per generation.** Consultant picks 1, 2, or 3 in the form. Claude produces DIFFERENT angles (e.g. service highlight + offer + patient testimonial), not variants of the same idea. One generation = a week of GMB content the consultant can schedule.",
      "**🧱 Plumbing.** New `src/lib/gmb-posts-store.ts` (types + KV CRUD + per-client index), new endpoints `/api/seo-actions/[slug]/gmb-posts/gmb-generate` (POST, streams NDJSON), `/gmb-result` (GET), `/gmb-update` (POST). Past results show up in the action's existing history grid via a `gmb-posts`-aware path in `/history`. `@google/genai` added as a dep; `GEMINI_API_KEY` documented in `.env.local.example` (free tier at aistudio.google.com).",
      "**Setup note.** Add `GEMINI_API_KEY` to Vercel → Settings → Environment Variables (Production + Preview + Development). Without it, the generation route returns a 503 with a clear setup hint — the rest of the app stays unaffected.",
    ],
  },
  {
    version: "70.5",
    date: "2026-05-19",
    title: "Roadmap routing fix + Overall SEO reorder + stale-badge bust",
    highlights: [
      "**🔀 Roadmap pillar card now opens the live board.** The 'Client SEO Roadmap' card was navigating to `/seo/[slug]/actions/client-roadmap` (the markdown action page) instead of `/seo/[slug]/roadmap` (the live operational board). Renamed in the pillar to **Roadmap**, blurb updated to `Live 12-week operational board — Claude fills it, you edit, status moves with the work.`, and the card now links to the board. Same fix flows to the Quick Actions panel for anyone who pinned it.",
      "**🧱 Centralised URL building so this can't drift again.** New `actionHref(clientSlug, action)` helper in `seo-pillars.ts` is now the single source of truth for every action card's URL. `ActionDef` gained an optional `href: (clientSlug) => string` override; consumers go through `actionHref()` and the override is honoured automatically. Greppable contract — if a new action ever needs a custom route, you set `href` on the def and every consumer follows.",
      "**📋 Overall SEO actions reordered.** New order on every project page: **Roadmap → SEO Audit → Keyword Research → Monthly Report**. Matches the operational flow consultants actually walk through.",
      "**♻️ Stale 'No roadmap yet' badge fixed.** The client page has `revalidate = 60`, so after a successful generate the `CurrentRoadmapStrip` was sitting on the cached version for up to a minute. Both `/api/roadmaps/[slug]` (PUT) and `/api/roadmaps/[slug]/generate` (POST) now call `revalidatePath('/seo/${slug}')` after saving, so the badge updates the instant the roadmap is persisted.",
      "**🪪 Slug preserved for back-compat.** The action's internal `slug` stays `client-roadmap` so any older history entries / deep links to `/seo/[slug]/actions/client-roadmap` still resolve. Only the display label and the destination URL changed.",
    ],
  },
  {
    version: "70.4",
    date: "2026-05-19",
    title: "Roadmap: pre-built grid always renders; AI just fills it (no more FUNCTION_INVOCATION_TIMEOUT)",
    highlights: [
      "**🧱 Board always exists.** Opening `/seo/[slug]/roadmap` now calls `ensureRoadmap(slug)` — if no roadmap is on file yet, a blank one (12 empty weeks, today as start date, no tasks) is created and persisted on the fly. The 'No roadmap yet for {client}' empty state is **gone**. You see all 12 weeks immediately and can hand-add tasks without any AI call.",
      "**⚡ Generate now runs on Haiku 4.5, not Sonnet.** Sonnet was consistently running 45-60s on the structured-output call and tripping Vercel's 60s function ceiling (`FUNCTION_INVOCATION_TIMEOUT`). Haiku 4.5 produces task names + sequencing of the same quality in ~10-20s. Combined with a tighter schema (dropped the `description` field — consultants type those inline when they edit a card anyway) + a lower task target (3-4 per week, 36-48 total instead of 60-70), the call now lands comfortably under the budget.",
      "**✂️ Schema + prompt diet.** Onboarding context capped 3k → 2k chars. maxOutputTokens 3500 → 2200. Design-rules block rewritten in shorthand. Same task-quality output for half the token budget.",
      "**🎯 Smarter top-bar button.** When the board is empty (no AI generation yet), the button shows `✨ Fill with AI` in brand gradient — visually distinct from the standard `Regenerate` (which appears only after the board has tasks). Same expanding panel underneath; the wording matches what's actually happening.",
    ],
  },
  {
    version: "70.3",
    date: "2026-05-19",
    title: "Roadmap: structured-output generation (no more JSON parse failures), any-day start, mind-map polish",
    highlights: [
      "**✅ JSON parse failures eliminated.** Roadmap generation switched from `generateText` + loose regex parsing to **`generateObject` + Zod schema** via the AI SDK. Anthropic is now invoked with a strict schema (12-week task array, week 1-12, pillar enum), and the SDK guarantees the response matches it. The `Claude did not return parseable roadmap JSON` error is gone — there's no parsing step left to fail.",
      "**📅 Any-day start date.** Dropped the artificial `(Monday of week 1)` constraint. Start date input now reads `(any day — week 1 begins here)`. Default flipped from next-Monday to today. The current-week math already worked for any start day; the label was the only thing forcing a Monday.",
      "**🗺️ Mind-map visual polish on the board.**",
      "- Month headers now centre between two connector lines (`──── MONTH 1 ────`) — the same tree-from-above look as the user's reference mind-map.",
      "- Top bar shows the client name as a brand-coloured chip, then a date-picker pill (`Starts 27/04/2026`), then the current-week badge.",
      "- Grid spacing tightened (`space-y-8` between months) so the 12 weeks read as 3 distinct blocks.",
      "**🧱 Plumbing.** Removed the no-longer-needed `parseJsonLoose` helper, `ClaudeRoadmap` type, and `ROADMAP_STATUSES` unused-export silencer. The route is ~80 lines shorter and entirely type-safe end-to-end.",
    ],
  },
  {
    version: "70.2",
    date: "2026-05-19",
    title: "Roadmap polish: progress UX, JSON crash fix, tighter prompt, simpler empty-state copy",
    highlights: [
      "**📝 One-line empty-state copy.** The roadmap board's first-time state shortened from 4 lines to one: `Generate a 12-week plan from {client}'s context — every task is editable after.` Stops blocking the eye on the way to the Generate button.",
      "**⏳ Live progress bar + paced status.** Clicking Generate now shows an indeterminate gradient progress bar with a cycling status line: `Loading client brief… → Pulling the last 15 actions… → Building the brief for Claude… → Drafting 12 weeks of tasks with Claude (usually 20–40s)… → Sequencing tasks across the grid… → Saving and archiving the previous roadmap…`. The bar is honest about being indeterminate (CSS keyframe `indeterminate` slides a brand-gradient strip across the track) — no fake percentages.",
      "**🛡️ JSON crash fixed.** Previously, when the Vercel function exceeded its 60s budget, the platform returned a plain-text error page and the client crashed with `Unexpected token 'A', \"An error o\"… is not valid JSON`. The fetch handler now reads the response as text first, attempts JSON safely, and surfaces the raw text in the error message when parsing fails. No more confusing parse errors — you see what the server actually said.",
      "**✂️ Tighter generate prompt to dodge the 60s ceiling.** Onboarding extract cap dropped 5k → 3k chars, history excerpts 200 → 100 chars, Claude maxOutputTokens 4000 → 3000. Same task quality (we're packing ~60 short rows, not prose); ~6-10s shaved off worst-case Claude time so the roundtrip lands comfortably under Vercel's 60s budget on Hobby.",
    ],
  },
  {
    version: "70.1",
    date: "2026-05-19",
    title: "Workable 12-week SEO Roadmap board per client (with AI generation + live current-week marker)",
    highlights: [
      "**🗺️ New roadmap board at `/seo/[slug]/roadmap`.** 3 months × 4 weeks = 12 columns laid out exactly like the screenshot. Each column shows tasks as cards. Status-coloured backgrounds: ⚪ Not started, 🔵 In progress (sky), 🟠 Pending client review (amber), 🟢 Implemented (emerald). Each card carries a pillar chip (Technical / On-Page / Off-Page / Local / Content / Research).",
      "**📍 Live current-week marker.** The roadmap stores a `startDate` (Monday of week 1). The board computes `currentWeekIndex` from `Date.now()` and highlights that column with a purple ring + a `▶ Week N of 12` badge in the top bar — so consultants always know exactly where they should be. Editable start date inline; the highlight slides instantly.",
      "**🎯 Full edit in the app.** Click any card → inline form with title, description, status, pillar, and week-move dropdown. Add task per week column. Delete task. All edits auto-save 600ms after the last keystroke via PUT `/api/roadmaps/[slug]` — no Save button to click.",
      "**🤖 AI generation grounded in everything we know.** New POST `/api/roadmaps/[slug]/generate` asks Claude for strict JSON (12 weeks × 4–7 tasks each), grounded in: client brief Do's/Don'ts/Notes, onboarding form extracted text, 15 most-recent action history entries across every action, top 20 target keywords by volume, and the previous roadmap if there is one (so it doesn't re-propose already-shipped work). Old roadmap auto-archives (kept in KV for audit).",
      "**🚨 Warnings banner — dismissable.** Three triggers, computed server-side on every read: (a) any task pending client review for ≥ 7 days, (b) ≥ 2 tasks from past weeks still not implemented (escalates to critical at 5+), (c) the current week is empty or has nothing in flight. Each warning has a stable id; consultants dismiss with X, dismissal persists on the roadmap.",
      "**📌 Client-page header strip.** The old typewriter `What are we working on now, boss?` prompt is replaced with a live roadmap strip: `Week N of 12 · X tasks · Y in flight · Z pending review` + chips for the first 3 current-week tasks (status dots colour-coded) + link straight to the board. When no roadmap exists, falls back to a `No roadmap yet — generate a 12-week plan` CTA.",
      "**🧱 Plumbing for follow-ups.** New `src/lib/roadmap-store.ts` (Roadmap + RoadmapTask types, KV CRUD, archive, currentWeekIndex, weekStartDate, computeWarnings, normaliseRoadmap). Distinct from the `client-roadmap` markdown action shipped in v70.0 — that's the deliverable, this is the operational view. Both stay in the app.",
    ],
  },
  {
    version: "70.0",
    date: "2026-05-19",
    title: "Monthly Report + Client SEO Roadmap actions under Overall SEO",
    highlights: [
      "**📊 Monthly Report action.** New `/seo/[slug]/actions/monthly-report` action under Overall SEO. Fields: reporting period (28d / last full month / quarter), highlights to spotlight, next-month focus. Grounding pack pulls **recent action history across every SEO action for the client** (last 15 entries — audits, KW research, content, GMB work) so Claude writes from what was actually done, not generic templates. Output: client-facing markdown in EN/PT-PT depending on the brief, with sections for `Resumo do mês`, `O que se fez`, `O que mexeu`, `Foco do próximo período`, `Pedidos ao cliente`. PDF + DOCX exports via the same pipeline the other actions use.",
      "**🗺️ Client SEO Roadmap action.** New `/seo/[slug]/actions/client-roadmap` action under Overall SEO. Fields: horizon (3/6/12 months), strategic focus, constraints (blackouts, bandwidth, budget). Grounds in **recent action history + target keyword shortlist + onboarding form** so the roadmap doesn't propose work that's already shipped. Output: thesis → quarter-by-quarter milestone tables (initiative · pilar · esforço · métrica · responsável) → risks → 4-week quick wins → reading metrics → validation asks. Generate-then-edit workflow: consultant downloads DOCX, tweaks language, sends.",
      "**🧩 Cross-action history aggregator.** New `listRecentHistoryAcrossActions(clientSlug, slugs, limit)` walks every action slug's KV bucket and returns the N most-recent entries sorted newest-first. Used by both new actions; opens the door for future synthesis-driven actions that need to know everything that's been done on an account.",
      "**📝 Prompt specs added to `seo-claude-prompt.ts`.** Per-action output spec for both new actions follows the same template the other Overall-SEO actions use — explicit sections, structured tables, hard rules around honesty, brief compliance, language match (PT-PT when client materials are Portuguese), no fabricated metrics. Roadmap explicitly checks the history pack so it never re-proposes work already in flight.",
    ],
  },
  {
    version: "69.9",
    date: "2026-05-19",
    title: "Deliverables: consultant emails, no leaked timestamps, DOCX export, 3-layer KW enrichment",
    highlights: [
      "**📧 Consultant email replaces seo@wonder-ads.com on every deliverable.** PDF cover line + running footer + DOCX cover now pull from `getConsultantEmailForSlug(slug)` — Luana's clients → `luana@`, Fran's → `fran@`, Yenisey's → `yeni@`. Unassigned slugs still fall back to the shared inbox. Replies land in the inbox of the person actually managing the project.",
      "**🕒 Generated-date no longer leaks the minute the run kicked off.** Result-page header shows date only (`DD/MM/YYYY`) instead of `DD/MM/YYYY HH:mm`. PDF cover's Report ID drops the `HHMM` block — what was `2026-05-19-1610-ab` displays as `2026-05-19-ab`. Same on the action's history grid. The full id with `HHMM` is preserved in URLs + storage so existing bookmarks still work.",
      "**🎯 Three-layer KW enrichment — every keyword now arrives with vol/mo AND KD.** Stage 5 now runs `keyword_overview/live` → `bulk_keyword_difficulty/live` → `keywords_data/google_ads/search_volume/live` in sequence, hitting each only for rows still missing the field. Stage 6 then copies values between accent/plural variants inside the same pack so duplicates can't disagree (e.g. `clínica ermesinde` and `clinica ermesinde` end up with the same vol + KD). Save route uses the same comprehensive enricher for AI-cluster keywords. KD=0 is treated as missing throughout.",
      "**📝 Download DOCX next to Download PDF on every action result.** New `/api/seo-actions/[slug]/[action]/results/[id]/docx` route renders the same markdown analysis Claude produced into a Word document with a branded cover page (logo wordmark, dept, title, client, audited date, head consultant, consultant email, report id). Built with the `docx` npm package; markdown → DOCX conversion walks the mdast tree from `remark-parse` + `remark-gfm` so headings, lists, tables, bold/italic, links, blockquotes, code blocks all survive. Consultants can now tweak wording before sending without retyping the report.",
    ],
  },
  {
    version: "69.8",
    date: "2026-05-18",
    title: "KR polish: real KD, no CPC, dedup near-identical variants, monthly header + chooser typewriter",
    highlights: [
      "**🎯 Real Keyword Difficulty everywhere.** The per-seed `keyword_suggestions` / `keyword_ideas` Labs endpoints return KD=0 for most low-volume locale keywords (DataforSEO doesn't compute KD on every row). New bulk `/keyword_overview/live` enrichment fires after the initial fetches and backfills KD on every row in the pack. Also fills missing `searchVolume` so every keyword has a vol/mo figure — no more `—` placeholders for AI-cluster keywords.",
      "**AI-cluster keywords enriched at save time.** When Claude infers a 40-60 keyword universe across 6 clusters from the onboarding form + geo, those rows now also get enriched with DataforSEO volume + KD via the same bulk endpoint inside `/api/seo-actions/[slug]/[action]/save` (maxDuration bumped 30s → 60s to accommodate). So the dashboard table shows real vol/mo and KD on cluster rows instead of `—`.",
      "**🧹 Near-identical variants collapse to one row.** New `normalizeKwForDedup` strips Portuguese accents, plural `s` suffixes, parenthetical asides (`(variante acentuada)`), and punctuation — then groups by the normalized key and keeps the most informative row (highest volume + accented spelling preferred). So `clínica ermesinde` / `clinica ermesinde` / `clínicas ermesinde` / `clinicas ermesinde` become **one** row instead of four. Merges missing fields from the losers (cluster context, volume, intent) into the winner.",
      "**No more CPC column.** Wonder Ads SEO is organic-only — CPC was misleading noise on a paid-traffic metric. Dropped from the dashboard table, the CSV export header, the PDF report, the markdown fact pack sent to Claude, and the system prompt instructions.",
      "**Keyword Universe → Keyword Data for {Month} | {Year}.** Header badge now anchors the data freshness explicitly to the month it was pulled (e.g. `Keyword Data for May | 2026`), so consultants don't accidentally treat 6-month-old research as current.",
      "**Department chooser polish.** Header no longer sits inside a card-like sticky strip — on the landing page it's transparent and floats over the starfield. The `Choose a Department` heading is now `Choose Your Department` and types in with a brand-coloured caret (respects `prefers-reduced-motion`).",
    ],
  },
  {
    version: "69.7",
    date: "2026-05-17",
    title: "Claude clusters in the dashboard table + auto-continuation when Claude is cut off",
    highlights: [
      "**🎯 Claude's clusters now feed the dashboard table.** New `kw-cluster-parser.ts` parses the `## Cluster map` section out of Claude's markdown — every cluster's keyword rows (with intent, priority, suggested page, why) become first-class rows in the dashboard alongside the raw DataforSEO data. So when Claude analyses 45 keywords across 6 clusters, all 45 show up in the table — not just the 7 from DataforSEO.",
      "**New 'AI Clusters' tab** in the dashboard showing only the cluster keywords, with their counts in the tab label. Also a new **'Group by: Cluster'** option that splits any tab into cluster-named buckets with select-all per cluster.",
      "**Persisted on the HistoryEntry** as `kwClusters: KwCluster[]` — reopening the result page hydrates clusters from KV alongside the raw pack, no re-parse needed.",
      "**🔄 Auto-continuation when Claude is cut off.** Detects when the mandatory 'Verificação final' Portuguese checklist is missing from the stream output (signals Claude hit the 60s ceiling). Automatically fires a new `/continue-kw-research` call with the last 2000 chars of the partial output asking Claude to resume from exactly where it stopped. Up to 2 continuation attempts per run, so a complex report can total ~180s of Claude work split across 3 function calls.",
      "**New `/api/seo-actions/[slug]/keyword-research/continue-kw-research` endpoint** — same fact pack, same system prompt, same PDF attachment as `/run-kw-research`, but the user message contains the partial + instructions to continue.",
    ],
  },
  {
    version: "69.6",
    date: "2026-05-17",
    title: "Sticky header + verbose DataforSEO diagnostics + dual-endpoint pulls + smoke test",
    highlights: [
      "**Sticky page header** with backdrop blur — the back button now stays visible at all times no matter how far you've scrolled. The `#section-actions` anchor was scrolling the header off-screen so 'Back to SEO DPT' looked missing.",
      "**Per-seed call log** in the progress stream — every DataforSEO `keyword_suggestions` / `keyword_ideas` call is now logged with the seed, endpoint, count returned, and status code. Easy to see exactly which seeds worked, which returned empty, and which errored.",
      "**Dual-endpoint pulls** — for each expansion seed, run BOTH `keyword_suggestions` AND `keyword_ideas` in parallel. They have different DataforSEO query mechanics and one often returns data where the other doesn't. Up to 12 parallel calls per run, capped to stay under the function budget.",
      "**Smoke test fallback** — if `suggestions + ideas` still equals 0 after all expansion calls, one canonical language-specific seed is tried (`dentista` for PT/ES/IT, `dentiste` for FR, `zahnarzt` for DE, `dentist` for EN). If even THAT returns 0, a 🚨 'DataforSEO connectivity / subscription issue' warning is surfaced in the progress stream with a deep link to the diagnostics endpoint.",
      "**dfsPost is now lenient on no-results status codes** — DataforSEO returning `40400` / `40500` means 'no items found' (a valid empty response, not an error). Previously these were thrown as errors; now they return empty + the status code is logged in diagnostics for visibility.",
      "**`/api/diagnostics/dataforseo-test` extended** — now also tests `keyword_suggestions/live` + `keyword_ideas/live` (with `?seed=` / `?location_code=` / `?language_code=` query params). Use this to verify the DataforSEO Labs subscription covers the keyword expansion endpoints.",
    ],
  },
  {
    version: "69.5",
    date: "2026-05-17",
    title: "Persistent back nav + real keyword-shortage fix (per-seed suggestions + emergency fallback)",
    highlights: [
      "**Persistent 'Back to X' pill in the page header** on every nested page (client → 'Back to SEO DPT', action → 'Back to <client>', result → 'Back to <action>'). Sits next to the WonderAds logo top-left so it's always visible without scrolling. PageShell now takes `backHref`/`backLabel` props; removed the inline back-links scattered across pages.",
      "**🐛 Real fix for the 'still only 7 keywords' bug** — the v69.4 multi-seed `keyword_ideas` call was returning near-empty because DataforSEO appears to take the intersection of related ideas across diverse seeds (when seeds are very different, intersection is tiny). Replaced with **per-seed `keyword_suggestions` calls in parallel** (one per expansion seed, capped at 8). `keyword_suggestions` is the right tool for expansion — typically 50-300 keywords per seed. Should now consistently return 100s of keywords for any client with a website + ranked footprint.",
      "**Stage 3 emergency fallback** — when total is still under 30 (very narrow domains), `pickTopicNouns()` mines single-word topical nouns from the domain footprint + user seed + onboarding extracted text (filtering brand tokens + EN/PT stopwords). Each noun is then run through `keyword_suggestions` for an extra expansion pass. Catches narrow-brand-only clinics.",
      "**Thin-universe warning** in the progress stream when total < 30 — explains likely causes (sparse domain rankings, narrow seed, sparse DataforSEO market data) so the consultant can react.",
    ],
  },
  {
    version: "69.4",
    date: "2026-05-17",
    title: "Keyword Research — fix '3 keywords' bug, phase-split for 60s timeout, GSC label, nav rename",
    highlights: [
      "**🐛 FIX: the 'only 3 keywords' bug** — Clínica Mimus and any other client with a brand-name auto-seed was returning a tiny shortlist because the seed-word filter on DataforSEO `ranked_keywords` only kept rows containing the seed tokens. Two-stage rebuild: (1) pull the domain's **full unfiltered ranked footprint** (up to 500 keywords) as the foundation, then (2) mine the top **8 non-branded high-ETV keywords** from that footprint + competitor top branded queries to seed `keyword_ideas` for broad expansion. Brand tokens (e.g. 'mimus') are explicitly excluded from expansion seeds.",
      "**⏱ Phase-split for the 60s timeout** — Claude was getting cut off mid-sentence on heavy runs (big onboarding PDF + 5 competitors). Split into `/prep-kw-research` (DataforSEO + onboarding fetch, saves pack to KV) → `/run-kw-research` (loads pack, attaches PDF natively, streams Claude). Each phase gets its own 60s budget. Mirrors the SEO Audit 4-phase pattern. Progress block now shows `Phase 1 / 2 — pulling keyword data` then `Phase 2` for Claude.",
      "**🔎 Tracked Keywords now labels its data source** — added 'Live · Google Search Console' uppercase subtitle under the title and clarified the body copy: 'Top Google queries for X pulled live from GSC.' Hard to confuse with the curated Target Keywords list now.",
      "**🧭 Project section nav updated** — inserted **02 Keyword Research** between 01 Do's & Don'ts and Data & Quick Actions (now 03). Renamed last item to **SEO Actions** (was 'Actions'). The 02 entry scrolls to the Target Keywords panel I shipped in v69.1.",
    ],
  },
  {
    version: "69.3",
    date: "2026-05-17",
    title: "Keyword Research polish — already-targeted badges, PDF re-extract, graceful geo fallback",
    highlights: [
      "**'🎯 Already targeted' badge** on every row in the Keyword Research dashboard whose keyword already lives in the client's Target Keywords list. Checkbox is disabled, row tinted emerald, header surfaces a count chip. Group-by view shows per-bucket disabled-count too. Sending a batch optimistically grows the targeted set so the rows we just shipped become disabled immediately — no refetch.",
      "**'Re-extract' button** on the Onboarding Form panel. POST to `/api/onboarding/[slug]/re-extract` re-runs the PDF text extractor + competitor miner + suggestedSeed heuristic on the existing file, no re-upload needed. Useful as the extractor heuristics improve over time.",
      "**Graceful geo fallback** when a city target returns zero data from DataforSEO (bad city code or city too small for Google Keyword Planner). `runKeywordResearch` automatically retries with the parent country, records the attempt in `KwResearchPack.fallbackInfo`, and surfaces it in two places: a `> ⚠️ Geo fallback:` line in the progress stream AND a prompt-level instruction telling Claude to note in the Overview that 'city-level data is unavailable for this geo' while still baking the original city's modifier into recommended keywords.",
    ],
  },
  {
    version: "69.2",
    date: "2026-05-17",
    title: "Keyword Research — mandatory Pre-flight Checklist + hard brief compliance",
    highlights: [
      "**SEO Claude now ends every Keyword Research report with a Pre-flight Checklist** (Verificação final) in Portuguese — the exact verification list Andre uses to sign off on a report. Each box is ticked only if Claude can honestly confirm it; unticked boxes get a one-line explanation naming the offending keywords for consultant review.",
      "**Hard brief compliance** elevated from soft guidance to a non-negotiable rule. Every keyword recommendation must pass three gates: respect the **Do's**, **NEVER violate a Don't** (offending keywords are dropped, not softened), and **integrate the Notes** (when a Note shapes a recommendation, it's cited inline with _'Per client Note: …'_).",
      "**Generic-keyword filter** added — single-word/category terms like 'psicologia', 'tratamento', 'dental', 'wellness' are now explicitly rejected unless the client is a national leader defending the term. Silent filter on suggestions/ideas; no more vanity-volume noise in the shortlist.",
      "**YMYL/legal compliance rule** — keywords that would force the client to make medical claims they can't legally make (cure-for-X, guaranteed-treatment-Y) are dropped automatically.",
      "Self-check ends with either `✅ Pronto para entrega — todas as verificações passam.` or `⚠️ Revisão necessária — N item(s) above precisam de validação do consultor antes de partilhar com o cliente.`",
    ],
  },
  {
    version: "69.1",
    date: "2026-05-17",
    title: "Keyword Research polish — city geo targeting, Target Keywords panel, smart seed fill, group-by, history seed display",
    highlights: [
      "**Geo Target is now city-aware.** Replaced the free-text country field with a curated grouped select covering **30+ country and city targets** across Portugal (Lisbon, Porto, Algarve, Coimbra, Braga, Madeira), Spain (Madrid, Barcelona, Valencia), Brazil (São Paulo, Rio, BH), UK, US, Canada, Australia, France, Germany, Italy, Belgium. Each entry carries the real DataforSEO `location_code` so volumes get scoped to that market.",
      "**Localised keyword instruction baked into the prompt.** When a city target is selected, Claude receives a hard rule telling it to bake the local modifier (Lisboa, NYC, Berlin, Roma, etc.) into recommended keywords where intent demands. Example: target Lisbon → 'dentista lisboa', 'all-on-4 lisboa'. The geo + language code also flow through every DataforSEO call.",
      "**Target Keywords panel** on every client page below the brief/files grid. Surfaces everything queued via the dashboard's 'Send to Tracked' button: sortable by keyword/volume/KD/added-date, intent chips, deep-link back to the kw-research run, manual Add (one per line or comma-separated), remove, CSV export, refresh.",
      "**Smart seed-topic auto-fill.** PDF extraction now mines the onboarding form for 'primary keywords' / 'main services' / 'business focus' fields (EN + PT). The matched phrase is stored as `suggestedSeed` on the OnboardingDoc and pre-populates the Keyword Research form's seed field. No more starting from a blank box when the client already told us what to focus on.",
      "**Dashboard 'Group by' control** — None / Intent / Source. Inserts sticky group headers with per-bucket select-all checkbox so consultants can bulk-select an entire intent or competitor footprint in one click.",
      "**Past results cards** for Keyword Research now show **seed + geo + intent** as a chip + summary line instead of the first markdown sentence — way more scannable when comparing past runs.",
    ],
  },
  {
    version: 69,
    date: "2026-05-17",
    title: "Keyword Research phase — conditional seed topic, onboarding-aware form, pillar-organised footer",
    highlights: [
      "**Seed Topic is now conditional.** When an onboarding form is uploaded for the client, the field becomes **optional** — Claude reads the form for focus signals (services, objectives, audience, competitors). When NO form is uploaded, the field stays **required** (system needs *something* to anchor on). The change is driven by a server-side onboarding lookup, so the form reflects reality the moment you hit the page.",
      "**'Use data from onboarding form' tickbox** sits at the top of the Keyword Research form. Defaults to ON when a form is present. Untick to run a clean DataforSEO-only research (e.g. for a discovery comparison). When ON, the seed field switches to 'Additional focus (optional)' with new placeholder + help text explaining its softer role.",
      "**Onboarding state is surfaced in-form**: emerald banner when present (with file name + competitor count if any), rose banner with a deep link to upload when missing.",
      "**API: `useOnboarding` flag respected.** New synthetic input drives whether the run reads the onboarding form. When ON and no seed is provided, the run auto-derives one from the client name and lets Claude drive focus from the PDF.",
      "**'More actions for X' footer redesigned.** Was a flat chip-soup that pushed all 23 actions into one wrapping row. Now a clean **5-column grid grouped by pillar** (Overall SEO · On-Page · Off-Page · Local · Content) with the pillar icon as header, a bullet dot per action that gradient-tints on hover, and a top-right 'All actions →' link back to the project page.",
      "**Version phase:** v68.x was the SEO Audit polish phase (concluded at v68.9). v69 opens the Keyword Research polish phase — minor versions v69.1, v69.2… as the action evolves.",
    ],
  },
  {
    version: "68.9",
    date: "2026-05-17",
    title: "Keyword Research goes full-stack — native PDF, competitor section, interactive dashboard, CSV + Target Keywords",
    highlights: [
      "**Claude now reads the onboarding PDF natively** via Anthropic's document content type — sees the form's actual layout/tables (not just regex-extracted text). The catch-all `/run` route attaches the PDF as a `file` content block on the user message; Claude can cite specific lines.",
      "**Prompt mandates competitor analysis.** Two new required sections in the keyword-research output: **What the client told us** (top services, objectives, audience, brand voice — quoted from the form) and **Competitor analysis** (per-competitor footprint, strongest keywords they win, gaps to attack, verdict 🎯/⚠️/⛔). Plus a hard rule: cross-reference every competitor's keywords against the client's and suggest the best gap-attacks.",
      "**Interactive Keyword Research Dashboard** below the report — tabs (All / Suggestions / Ideas / Already-ranking / Competitors), intent filter, full-text search, sortable columns (Volume / KD / CPC / Keyword), per-row select checkboxes. Sticky header on the table. Renders 2k+ keywords smoothly.",
      "**CSV export** — selected rows or all-filtered. File name auto-stamped with date.",
      "**'Send to Tracked' button** — pushes selected keywords into the per-client Target Keywords list (new `target-keywords:{slug}` KV store + `/api/target-keywords/[slug]` GET/POST/DELETE). Dedupes by lowercase keyword. Returns added/skipped counts.",
      "**Dashboard data persists** — new `kw-research-prep-store.ts` holds the raw `KwResearchPack` between `/run` and `/save`, then attaches it to the `HistoryEntry`. Reopening a result page hydrates the dashboard from history instead of re-running DataforSEO.",
      "**PDF download for Keyword Research.** Same WonderAds-branded cover as SEO Audit (consultant, wonder-ads.com, Audited date). The body adds a printable Keyword Universe section: 4 stat cards (total keywords, total volume/mo, already-ranking count, competitors analysed) + top 25 opportunities table + top 20 already-ranking + a per-competitor table for each named competitor.",
    ],
  },
  {
    version: "68.8",
    date: "2026-05-17",
    title: "Hotfix: restore Yenisey column + onboarding doc polish (heartbeat badge, PDF extraction, competitor pull)",
    highlights: [
      "**HOTFIX — SEO DPT Yenisey column restored.** Renaming Yenisey → Yenisey R. in v68.5 dropped 5 clients off the board (White Clinic, IHN, Fisio Restelo, Monte Mar, CDT) because `getSeoClients()` is wrapped in a 1h `unstable_cache` that still held the old 'Yenisey' string. Two fixes: (a) re-resolve consultant from slug at render time in `/seo` so any future rename ships instantly without waiting on the cache, (b) bumped the cache key to `seo-clients-v2` so the stale entry evicts on next request.",
      "**Onboarding badge is now state-driven.** Green chip when uploaded, **red + heartbeat-pulsing chip when missing** — impossible to forget a client's intake doc. Heartbeat respects `prefers-reduced-motion`.",
      "**PDF text extraction wired in** via `unpdf` (serverless-friendly PDF.js fork). Runs server-side on upload; extracted text + auto-mined competitor URLs are stored alongside the doc in KV. AI actions can now cite specific lines from the client's intake form.",
      "**Competitor keyword pull** added to Keyword Research. For each competitor URL/domain found in the onboarding form, DataforSEO pulls `ranked_keywords` filtered to the seed theme and injects them into the prompt as a per-competitor table. Claude is instructed to cross-reference and surface gaps.",
    ],
  },
  {
    version: "68.7",
    date: "2026-05-17",
    title: "Keyword Research scaffold — DataforSEO Labs + onboarding-aware",
    highlights: [
      "Keyword Research action now wires three DataforSEO Labs endpoints in parallel: **keyword_suggestions/live** (same-stem expansions of the seed), **keyword_ideas/live** (broader semantic ideas), and **ranked_keywords/live** filtered to the seed theme (what the domain already ranks for — quick wins).",
      "All queries respect the per-client **geo + language** from `client-geo.ts` — Portugal/pt for clinics, Canada/en for IHN, etc. — so the keyword universe matches the client's actual market.",
      "**Onboarding form auto-injected** into the prompt when uploaded. Claude is instructed to weight the keywords/themes the client named heavily, and to call out gaps between what the client says they want and what the keyword data backs up.",
      "Brand-new Claude output spec for keyword research: **Overview · Cluster map** (H3 per cluster with table, intent, KD range, combined volume) · **Quick wins (top 10)** · **Strategic bets** · **Gaps the data reveals** · **Tracking shortlist** for the Tracked Keywords dashboard.",
      "Priority emoji tags: 🟢 Quick win · 🟡 Strategic · 🔵 Long bet · ⚪ Watch. Intent labels: informational / commercial / transactional / navigational.",
      "Surfaces partial errors per endpoint (e.g. 'keyword_ideas subscription not enabled') without nuking the whole run.",
    ],
  },
  {
    version: "68.6",
    date: "2026-05-17",
    title: "Per-client Onboarding Form upload (feeds AI actions)",
    highlights: [
      "New **Onboarding Form** panel on every client page (above Brief + Client Files). Single-slot upload for the doc the client filled out during onboarding — main keywords to focus on, target audience, services, geo, brand voice, etc.",
      "Accepts **PDF, DOC, DOCX, TXT, MD** up to 200 MB via Vercel Blob (same client-direct upload path used for client files — no Vercel function size limit).",
      "Persisted in Vercel KV at `onboarding:{slug}` so it survives across all preview deployments and team members. Replace/Remove actions wired up.",
      "Sets up **Keyword Research** (next action) — the AI will read this doc to anchor the keyword universe on what the client actually cares about, not just what they currently rank for.",
    ],
  },
  {
    version: "68.5",
    date: "2026-05-17",
    title: "PDF polish: brand-gradient Download button, readable tables, consultant + site on cover",
    highlights: [
      "**Download PDF** button now uses the WonderAds purple→pink gradient with shadow — pops off the page instead of fading into the dark UI.",
      "**Cover page** now includes **Head SEO Consultant** (auto-resolved per client via the SEO DPT consultant map) and a prominent **wonder-ads.com** link under the meta block.",
      "Renamed **'Generated:' → 'Audited:'** on the cover meta — more accurate language for an audit report.",
      "Fixed the **white-on-white tables** in the PDF body (e.g. the 'What it means' table). MarkdownView's `text-white/85` was beating the print CSS — added scoped `!important` overrides for everything inside `.seo-markdown` so cells, headings, links, and code render dark on the white PDF.",
      "Renamed **Yenisey → Yenisey R.** across the SEO DPT (column header + consultant resolver) to match the Fran. R. / Luana N. naming pattern.",
    ],
  },
  {
    version: "68.4",
    date: "2026-05-17",
    title: "Always-visible PDF button, keyword sort by rank, no more wall-of-text",
    highlights: [
      "Download PDF button now always renders top-right of the result page. Disabled with 'Download available soon' while the audit is running; enables to 'Download PDF' the moment the result saves.",
      "Keywords table default sort changed to **Position ascending** (top rankings first) — the most actionable order for an SEO consultant.",
      "Dropped the **CPC column** from the keywords table — irrelevant for organic SEO work.",
      "Prompt update: SEO Claude can no longer enumerate inside paragraphs as '(1) X (2) Y (3) Z'. Multi-item lists must be rendered as real markdown lists, one item per line. Kills the wall-of-text Overview blocks.",
    ],
  },
  {
    version: "68.3",
    date: "2026-05-17",
    title: "Quick Actions order persists across all deployments",
    highlights: [
      "Quick Actions store moved from localStorage to Vercel KV. Root cause of the persistent 'my order keeps reverting' bug: Vercel preview deployments each get a unique origin (wonder-ads-workspace-<hash>-...vercel.app), so localStorage was being orphaned on every push. KV makes the order persistent across deployments, devices, and team members — one shared list for the SEO Department.",
      "New /api/quick-actions endpoint (GET + POST). The panel still feels instant — localStorage is kept as an optimistic cache for first paint while the API call settles in the background.",
    ],
  },
  {
    version: "68.2",
    date: "2026-05-17",
    title: "Print mode bypasses the app — actually renders the PDF now",
    highlights: [
      "The previous PDF was capturing the entire app page (back link, dark UI, Download PDF button) and adding the print layout on top — the cover never filled the page and body pages came out blank white. Fixed by rendering the print layout server-side, directly from page.tsx, when ?print=true. PageShell is bypassed entirely.",
      "PrintLayout returns its own <html><body> so Next.js skips the root layout chrome on print routes. Every page now has the brand header with WonderAds + SEO Department + the action/client breadcrumb.",
      "AutoPrint helper waits for images (logo + astronaut) to load before firing window.print() so the PDF never goes out with missing imagery.",
      "Core Web Vitals table added to the printable report — mobile + desktop side-by-side with field/lab data.",
      "Cover gradient now full-bleed at proper A4 width (210mm) — no more cropped right edge.",
    ],
  },
  {
    version: "68.1",
    date: "2026-05-17",
    title: "PDF fix, branded cover, CWV panel, DD/MM/YYYY dates",
    highlights: [
      "**PDF rendering fix.** The previous PDF came out blank because Chrome's print engine drops background colours by default. Added `print-color-adjust: exact` so the cover gradient + every brand element prints reliably.",
      "**Branded PDF cover redesigned.** Top-left: WonderAds butterfly + 'WonderAds' wordmark (matching the app header). Top-right: 'SEO Department · Audit Report'. Astronaut in the bottom-right corner, **no rotation** now. Body pages get a slim WA-branded header so they're not blank white anymore.",
      "**Core Web Vitals panel** on the dashboard. Real-user (CrUX) field data for LCP / INP / CLS + FCP + TTFB, mobile + desktop side-by-side, colour-coded per Google's good / needs-improvement / poor thresholds. Lighthouse scores chips inline (Perf / SEO / A11y / BP).",
      "**Dates everywhere are now DD/MM/YYYY.** New `src/lib/dates.ts` helpers (`formatDate`, `formatDateTime`, `formatDateLong`); all existing call sites migrated.",
      "**Stop button removed** during an audit — DataforSEO + PSI charges are already incurred, so it was misleading.",
      "**Quick Actions persistence** rewritten with a simpler localStorage-first store. Adds explicit `console.error` if a save ever fails (private mode / disabled storage), so silent reverts to default are now diagnosable.",
      "**Versioning convention while polishing SEO Audit:** minor versions (v68.1, v68.2, …) until the action is complete, then v69.",
    ],
  },
  {
    version: 68,
    date: "2026-05-17",
    title: "Branded PDF, top-page Download button, KD column",
    highlights: [
      "Download PDF button moved to the top of the result page (next to Back). Re-generate and Copy removed — the result page IS the persistent artefact.",
      "PDF report fully rebranded as a Wonder Ads SEO Department deliverable. Full-bleed cover page with WonderAds gradient + logo + client name + report ID + generated date + seo@wonder-ads.com contact + a cameo of the WA astronaut in the corner. Running footer on every other page with the SEO Department line + page numbers.",
      "Print typography fixed — inline code no longer renders as ugly grey boxes that broke the previous layouts. A4 page setup with proper margins (18mm top/sides, 22mm bottom for the footer).",
      "New 'KD' column on the keywords table — Keyword Difficulty from DataforSEO's competition score (0-100%). Was returned by the API but never displayed.",
      "Test any existing audit's PDF immediately at /seo/<client>/actions/seo-audit/results/<id>?print=true — no regeneration needed.",
    ],
  },
  {
    version: 67,
    date: "2026-05-17",
    title: "Audit split into 4 phases — PSI gets its own budget",
    highlights: [
      "Phase split now: /prep (sitemap + crawl + GSC), /prep-psi (PSI mobile + desktop), /prep-dataforseo (Labs + LLM Mentions), /run (Claude). Each fits comfortably under the 60s Vercel cap.",
      "Heavy English-language sites (IHN, 506 URLs, slow WordPress) were busting v66's combined PSI+DataforSEO phase. Now each runs in isolation with full headroom.",
      "Full audit wall-clock time slightly longer (~3 mins) but every phase is stable — no more 'No prep data found' on bigger clients.",
    ],
  },
  {
    version: 66,
    date: "2026-05-17",
    title: "Audit split into 3 phases — fixes IHN timeout",
    highlights: [
      "SEO Audit now runs in three phases instead of two. Each phase has its own 60s function budget so the deeper DataforSEO pull (limit 1000 + historical_serp_mode) can't kill Phase 1 anymore — which was happening on heavy English-language sites like IHN.",
      "Phase 1 = sitemap + crawl + PageSpeed + Search Console. Phase 2 = DataforSEO Labs + LLM Mentions. Phase 3 = SEO Claude analysis. From the UI it still looks like one continuous run.",
      "Same per-phase fact-pack-to-KV pattern as before, just split: /prep saves Phase 1 data, /prep-dataforseo appends Phase 2, /run reads the merged result.",
    ],
  },
  {
    version: 65,
    date: "2026-05-17",
    title: "Way deeper keyword pull from DataforSEO",
    highlights: [
      "Bumped DataforSEO ranked_keywords limit from 200 → 1000 (the API max).",
      "Added historical_serp_mode: 'all' so we capture keywords this domain ranks for live AND has ranked for recently — closes the gap with Semrush exports that include lost/historical rankings.",
      "Added load_rank_absolute for absolute SERP positions alongside group rank.",
      "Each keyword now carries a 'status' field (live / lost) so future UI can distinguish current rankings from reclaim opportunities.",
    ],
  },
  {
    version: 64,
    date: "2026-05-17",
    title: "AI Visibility: dual-window (Local + Global English)",
    highlights: [
      "AI Visibility panel now shows TWO windows side by side: Local (client's actual market — e.g. Portugal/pt for White Clinic) and Global English (US/en baseline). Each window has its own stats, top cited pages, and co-cited competitors.",
      "Plain-language explanations baked into the UI: 'Local = audience that buys from this client. Global English = wider US/en signal — useful when local is sparse since the LLM corpus is densest in English.'",
      "Every stat card in the AI Visibility section has a one-line sublabel explaining what it means (no more guessing what 'ASV' or 'mentions' mean).",
      "When local mentions = 0, the panel now explicitly tells you to check the Global English baseline — instead of staring at a depressing 0.",
      "Audits where the client is already US/en (none right now, but future-proofed) only query once. Other clients pay ~$0.04 extra in DataforSEO for the second window — well worth it.",
    ],
  },
  {
    version: 63,
    date: "2026-05-17",
    title: "DataforSEO billing-error banner",
    highlights: [
      "When DataforSEO returns 40200 'Payment Required' on every call, the dashboard now shows a clear rose-coloured banner with the exact link to add credits at app.dataforseo.com/billing.",
    ],
  },
  {
    version: 62,
    date: "2026-05-17",
    title: "Per-client geo, sortable keywords, dashboard polish",
    highlights: [
      "Per-client geo + language config — DataforSEO and LLM Mentions queries now run against the client's actual market. White Clinic = Portugal/PT, IHN = Canada/EN, InSync Design = Australia/EN, HDS Learning = Brazil/PT. No more US/EN data for Portuguese clinics.",
      "Top keywords table: 200 keywords (was 40), sortable headers (click Keyword / Pos / Volume / CPC / ETV), button pagination (First / ← / 1/8 / → / Last) — no more mouse-scroll, no more 40-keyword cap.",
      "Dashboard during a running audit: shows a clear 'Loading domain intelligence…' state instead of the post-mortem 're-generate' message.",
      "Polish: dropped the 'NEW' chip on AI Visibility, softened the LLM Mentions chip and sublabel readability, country shown as a tag (Portugal · pt) instead of small grey text.",
    ],
  },
  {
    version: 61,
    date: "2026-05-17",
    title: "AI Visibility panel + colour-tiered Authority + Backlinks live",
    highlights: [
      "Backlinks data now flows (you activated the subscription) — Authority Score, Total Backlinks, Referring Domains, Broken Backlinks populate properly. Whiteclinic: Authority 25/100, 4.8k backlinks, 320 referring domains.",
      "Brand-new AI Visibility panel powered by DataforSEO LLM Mentions API — total brand mentions in Google AI Overview / AI Mode + ChatGPT, AI search volume, top cited pages from this domain, and co-cited competitor + source domains.",
      "Authority card now uses a colour-tiered gradient (rose → amber → sky → emerald) so you see at a glance whether a client is weak / developing / established / strong.",
      "Every stat card has an icon + coloured accent (Trophy / Search / Activity / Link / DollarSign / Sparkles / Bot / Globe2). Visual scan time dropped meaningfully.",
      "SEO Claude prompt now references AI visibility data and calls out specific opportunities (pages cited 0×, competitors we lose to in LLM answers).",
    ],
  },
  {
    version: 60,
    date: "2026-05-17",
    title: "Richer dashboard, full-page PDF, fixed DataforSEO error handling",
    highlights: [
      "Found the Authority + Referring Domains '—' bug: DataforSEO returns HTTP 200 with status_code 40204 for subscription errors, my code was silently swallowing it. Now surfaces a banner: 'Activate the DataforSEO Backlinks subscription'. Action item: activate at app.dataforseo.com/backlinks-subscription.",
      "8 stat cards on the Domain dashboard (was 4): Authority, Organic Keywords, Est. Traffic, Referring Domains, Total Backlinks, Broken Backlinks, Paid Keywords, Top 10 Keywords. Missing values get a softer style so it's obvious what's pending data.",
      "Top ranked keywords bumped from 15 → 100 entries, with a scrollable container (sticky header). Adds CPC + URL columns.",
      "Full-page PDF: Download PDF now renders the Domain dashboard stats + top 30 keywords table alongside the analysis. No more analysis-only export.",
      "Brief panel ('What SEO Claude knows about <client>') hidden on SEO Audit + Keyword Research — they're data audits, not content generation. Brief still feeds the prompt as context; it just doesn't take up screen real estate.",
      "Analysis formatting overhauled: priority issues + findings now render as breathable mini-blocks with severity emoji (🔴 Critical / 🟠 High / 🟡 Medium / 🟢 Low). More line-height, softer inline code background, larger heading spacing.",
    ],
  },
  {
    version: 59,
    date: "2026-05-17",
    title: "Dashboard populates live, before Claude finishes writing",
    highlights: [
      "Domain dashboard now appears the moment Phase 1 finishes — pulled from the saved prep — so you see Authority Score / keywords / traffic while SEO Claude is still writing the analysis underneath.",
      "Backup path: /save also returns the metrics, so the dashboard updates if the prep-fetch missed.",
      "New /api/seo-actions/[client]/[action]/prep-data endpoint exposes the cached prep (metrics + meta) for the client to read mid-generation.",
    ],
  },
  {
    version: 58,
    date: "2026-05-17",
    title: "Fix: audits now actually persist to history",
    highlights: [
      "Critical fix — every audit until now was being lost on save. Vercel was killing the function before the KV write completed, so 'past results' were just transient streamed state that vanished on refresh.",
      "Save moved to a separate /save endpoint that runs in ~100ms instead of competing with the Claude stream for the 60s budget. Past results grids now actually populate, result URLs are bookmarkable, PDFs survive a refresh.",
      "Same fix applied to the catch-all route for non-audit actions.",
    ],
  },
  {
    version: 57,
    date: "2026-05-17",
    title: "Audit: 5 priority issues, objective tone, Re-generate button",
    highlights: [
      "SEO Audit now lists the top 5 priority issues (was 10). If only 3 real Critical/High issues exist, it lists 3 — no padding.",
      "Tone rewritten to be objective and engineer-voiced: no evaluative language ('excellent', 'concerning'), no hedging, no narrative transitions. State measurement, gap, fix.",
      "Every result page has a one-click Re-generate button — reuses the same inputs to spawn a fresh result page. Use it when an audit ran before an integration was wired (e.g. DataforSEO) and you want to refresh.",
    ],
  },
  {
    version: 56,
    date: "2026-05-17",
    title: "Audit Phase 1 errors are now visible — plus a DataforSEO test endpoint",
    highlights: [
      "If Phase 1 of the SEO Audit fails (orchestrator crash, KV write fail), the error is now persisted to KV so Phase 2 surfaces a real message — 'Phase 1 failed at step <stage>: <error>' instead of the generic 'No prep data found'.",
      "New /api/diagnostics/dataforseo-test endpoint hits DataforSEO with one cheap call and returns the upstream response (HTTP status + their internal status_code + cost). Use this to confirm credentials work outside the audit flow.",
      "Domain dashboard now distinguishes 'Not connected' (env missing) from 'Not in this result' (env set but the audit run didn't capture metrics). Tells you which fix to apply.",
    ],
  },
  {
    version: 55,
    date: "2026-05-17",
    title: "Integration diagnostics — see what's wired at a glance",
    highlights: [
      "New /api/diagnostics/env endpoint reports which integration env vars are present + their length (no values leak). Hit it to verify what the runtime actually sees.",
      "Action page 'Live tools' chips are now colour-coded by status — green (configured), amber (optional, soft-degrades), red (missing). Hover for the exact env vars + a hint on what to fix.",
      "DataforSEO check moved from a module-load constant to a per-call function so any Vercel module caching can't bake in a stale 'not configured' value.",
    ],
  },
  {
    version: 54,
    date: "2026-05-17",
    title: "SEO Audit split into two phases — fits the 60s Hobby budget",
    highlights: [
      "SEO Audit now runs in two phases under the hood: /prep gathers all live data (sitemap, crawl, PSI, Search Console, DataforSEO) and stores it in KV; /run reads it and streams SEO Claude's analysis. Each phase has its own 60s budget so big audits no longer get killed mid-write.",
      "From the UI it still looks like one continuous run — the result page streams both phases back-to-back with the same progress bar and separator.",
      "Audit prep is cached in KV for an hour (auto-expires) so a failed analysis run can be retried without re-paying the DataforSEO + PSI bill.",
    ],
  },
  {
    version: 53,
    date: "2026-05-16",
    title: "Domain dashboard, deeper audit prompt, PDF fixes",
    highlights: [
      "Audit result page now leads with a Semrush-style Domain dashboard — Authority Score, Organic Keywords (+ top-3/top-10 counts), Estimated Organic Traffic, Referring Domains, and a Top Ranked Keywords table with position + intent + volume + ETV.",
      "Powered by DataforSEO (pay-per-use, ~$0.007 per audit). Set DATAFORSEO_LOGIN + DATAFORSEO_PASSWORD in Vercel env to activate — until then the dashboard shows a 'Connect' CTA.",
      "SEO Audit prompt completely rewritten: 10 priority issues (was 3), proper executive summary (verdict + 2-3 paragraphs), Quick wins + Strategic bets sections, Scorecard now has a 'What it means' column for non-SEO readers.",
      "Tool-progress noise hidden from the result card — the saved analysis renders clean, the progress bar shows the steps.",
      "PDF download fixed: button only enables once KV save completes (was opening empty page), and the print stylesheet is now properly typeset (page breaks per H2, smaller table fonts, real margins).",
    ],
  },
  {
    version: 52,
    date: "2026-05-16",
    title: "Result pages, depth selector, progress bar, PDF download",
    highlights: [
      "Each generation now creates its own permanent page at /seo/<client>/actions/<action>/results/<resultId> (resultId = YYYY-MM-DD-HHMM-xx). Bookmark it, share it, come back to it.",
      "SEO Audit gets a Crawl depth selector — Quick (10 pages) / Standard (25) / Deep (50) / All (up to 100). Concurrency scales with depth so the audit stays fast.",
      "Real progress bar (%) on the result page — counts tool steps as they complete, then time-based estimate during SEO Claude's writing phase.",
      "Download PDF button on every result page — opens a print-styled view that auto-triggers your browser's Save-as-PDF dialog.",
      "Action page is now form + a 'Past results' grid (cards link to each result page) — the sidebar history is gone.",
    ],
  },
  {
    version: 51,
    date: "2026-05-16",
    title: "SEO Audit crawls 2× more pages",
    highlights: [
      "Sample crawl cap bumped from 12 → 25 pages so small/mid sites get fully (or almost fully) covered. Concurrency raised 5 → 8 so it stays fast.",
      "Progress label now shows the ratio — 'Sample crawl — 25 of 54 pages' instead of just '12 pages' — so you can see how much of the site we actually hit.",
    ],
  },
  {
    version: 50,
    date: "2026-05-16",
    title: "Friendlier action errors",
    highlights: [
      "Action runner now parses server error responses and shows only the human message instead of dumping the raw JSON envelope.",
    ],
  },
  {
    version: 49,
    date: "2026-05-16",
    title: "SEO Audit goes site-wide — sitemap, sample crawl & Search Console",
    highlights: [
      "SEO Audit is now site-wide: discovers the sitemap, samples up to 12 representative pages, crawls them all, runs PageSpeed (mobile + desktop) on the homepage, and pulls Search Console (28-day clicks/impressions/CTR/position + delta vs prev, top pages, top queries with movement, registered sitemaps + errors).",
      "Website URL field is pre-filled with the current client's domain so you can just hit Generate.",
      "Action page header tightened — small inline logo + breadcrumb, shorter blurb, no more orphaned big logo next to a wall of text.",
      "Live-tool chip row now lists Sitemap, Sample crawl, PSI Mobile, PSI Desktop, Search Console so you know exactly what's about to run.",
      "Tool progress streams to the page step by step (✓ Sitemap discovery — 189 URLs, 5 sitemap(s)), then SEO Claude's site-wide analysis follows — scorecard table (mobile vs desktop), Search Console signals, findings grouped by impact, 30/60/90 plan.",
    ],
  },
  {
    version: 48,
    date: "2026-05-16",
    title: "SEO Audit goes live — real PageSpeed + HTML measurements",
    highlights: [
      "SEO Audit now runs Google PageSpeed Insights (mobile + desktop) and a live HTML crawl of the target page before SEO Claude analyses it. Cites the exact Lighthouse scores, Core Web Vitals, failed audits and on-page facts.",
      "Tool progress streams to the page in real time (page fetch, PSI mobile, PSI desktop) so you see what's happening, then the analysis streams underneath.",
      "Mobile + desktop PSI run in parallel to stay inside the 60s function budget.",
      "Action pages now show a 'Live tools' chip row when an action runs real measurements (Page HTML, PSI Mobile, PSI Desktop).",
      "Add PAGESPEED_API_KEY to Vercel env to lift the shared-IP rate limit (free key at console.cloud.google.com → enable PageSpeed Insights API).",
      "Action pages load instantly — Notion client lookups now use Next.js cross-request cache (hourly revalidate) and a loading.tsx skeleton fills the gap on cold navigation.",
    ],
  },
  {
    version: 47,
    date: "2026-05-16",
    title: "SEO Claude per client — every action is now functional",
    highlights: [
      "Every one of the 22 SEO actions now has its own page at /seo/<client>/actions/<action>, with a live streaming output and full history of past generations.",
      "Spawned a 'SEO Claude — <client>' persona per client. Every generation is conditioned on that client's Do's, Don'ts, and Notes — and a built-in 2025–2026 SEO best-practice playbook (E-E-A-T, AI Overviews readiness, YMYL guardrails, local pack, link earning).",
      "Per-action input forms collect just what each action needs (e.g. Write Blog Article asks for topic + primary/secondary keywords + word count; GMB Reviews Responder asks for the review text + sentiment).",
      "Past generations are persisted to Vercel KV (last 30 per client per action) — expand any entry to review, copy, or delete it.",
      "Quick Actions and the pillar action cards now link directly to each action's page. Pinning is by slug under the hood (storage v2).",
      "Action output renders as full Markdown — headings, tables, lists, and fenced JSON-LD blocks for schema generations.",
    ],
  },
  {
    version: 46,
    date: "2026-05-16",
    title: "Editable Quick Actions & polish",
    highlights: [
      "Quick Actions is now editable in-app: click 'Edit' on the panel to add, remove, or reorder actions.",
      "Pin / unpin any action directly from the Actions pillars below — the pin icon syncs both lists live.",
      "Each pillar action now shows its parent pillar's icon (page, globe, map pin, pen, gauge) instead of a generic sparkle.",
      "On shared 'SEO & ADS' clients, the website chip is now pushed to the far right of the header tags.",
      "Quick Actions selection persists in your browser and syncs across tabs.",
    ],
  },
  {
    version: 45,
    date: "2026-05-15",
    title: "SEO actions reorg",
    highlights: [
      "New full-width 'Overall SEO' container at the top of every client's Actions, holding SEO Audit and Keyword Research.",
      "Keyword Research moved out of Local SEO into Overall SEO.",
      "Renamed Topic Cluster Plan → Blog Roadmap, and Review Response Drafts → GMB Reviews Responder.",
      "Refreshed pillar icons: On-Page SEO now uses a page icon, Off-Page SEO uses a globe.",
      "Removed the vertical guide line behind action rows for a cleaner look.",
    ],
  },
  {
    version: 44,
    date: "2026-05-14",
    title: "Clock & footer polish",
    highlights: [
      "The 'Working on this for' timer now counts seconds too (HH:MM:SS).",
      "Header clock shows a single, smaller milliseconds digit.",
      "Footer now points to andre@wonder-ads.com for bug reports and feedback.",
    ],
  },
  {
    version: 43,
    date: "2026-05-14",
    title: "Header clock, session timer & protected changelog",
    highlights: [
      "The header now shows a live local-time clock with milliseconds in place of the wonder-ads.com link.",
      "On SEO and ADS project pages, a 'Working on this for' timer counts up from when you opened the client.",
      "The changelog page is now behind a superadmin password.",
    ],
  },
  {
    version: 42,
    date: "2026-05-14",
    title: "Bigger client logo on project pages",
    highlights: [
      "The client logo chip on SEO and ADS project pages is larger, balancing the header with the tags and prompt beside it.",
    ],
  },
  {
    version: 41,
    date: "2026-05-14",
    title: "SEO Actions playbook + section nav",
    highlights: [
      "New Actions section on every SEO project — four pillars (On-Page, Off-Page, Local SEO, Content) of one-click workflows, animated as you scroll in.",
      "Replaced 'Open in Notion' with a numbered section nav (Do's & Don'ts · Data & Quick Actions · Actions) that tracks your scroll position and jumps to a section on click.",
    ],
  },
  {
    version: 40,
    date: "2026-05-14",
    title: "Richer GA4 trend chart + project prompts",
    highlights: [
      "GA4 trend chart is bigger and now interactive — gridlines, date axis, peak/total, and a hover tooltip showing each day's sessions.",
      "GA4 panel on SEO projects defaults to the Organic Search channel.",
      "Every project page now shows the animated 'What are we working on now, boss?' prompt under the client name.",
      "Department pages now ask 'Which project are we working on now, boss?'",
    ],
  },
  {
    version: 39,
    date: "2026-05-14",
    title: "Expanded GA4 Metrics panel",
    highlights: [
      "GA4 panel now has a date-range picker (7 days–12 months) and a channel filter — view all traffic or just Organic Search, Direct, Paid, Social, etc.",
      "Ten SEO metrics: users, new users, sessions, pageviews, pages/session, engagement, bounce rate, time per user, conversions and conversion rate — each with trend vs the previous period.",
    ],
  },
  {
    version: 38,
    date: "2026-05-14",
    title: "Don't cache failed analytics responses",
    highlights: [
      "GA4 and keyword API routes no longer CDN-cache error responses — a transient auth blip could otherwise freeze a panel on 'Access needed' for an hour.",
    ],
  },
  {
    version: 37,
    date: "2026-05-14",
    title: "Live GA4 Metrics on SEO projects",
    highlights: [
      "GA4 Metrics panel now pulls real Google Analytics 4 data — users, sessions, engagement, conversions, with 30-day trend arrows and a live sparkline.",
      "Each client's GA4 property is auto-detected by website domain; reuses the same Google service account as Search Console.",
    ],
  },
  {
    version: 36,
    date: "2026-05-14",
    title: "Sort & taller keyword list",
    highlights: [
      "Tracked Keywords can be sorted by top traffic, best position, or recent growth.",
      "Doubled the keyword list's scroll height so more rows are visible at once.",
    ],
  },
  {
    version: 35,
    date: "2026-05-14",
    title: "Keyword date ranges + project layout polish",
    highlights: [
      "Tracked Keywords has a date-range picker (7 days up to 12 months) and the list now scrolls — up to 50 keywords per client.",
      "Client Files panel stretches to match the brief column height.",
      "Moved the 'Synced across SEO & ADS' tag up to the client header, next to the website link.",
    ],
  },
  {
    version: 34,
    date: "2026-05-14",
    title: "Auto-detect each client's Search Console property",
    highlights: [
      "Tracked Keywords now looks up each client's actual Search Console property instead of assuming a domain property — so clients on URL-prefix properties (e.g. White Clinic, IHN) resolve correctly too.",
    ],
  },
  {
    version: 33,
    date: "2026-05-14",
    title: "Search Console via domain-wide delegation",
    highlights: [
      "Tracked Keywords now authenticates by impersonating a Workspace user that already owns every client property — no per-client access grant needed in Search Console.",
    ],
  },
  {
    version: 32,
    date: "2026-05-14",
    title: "Live keyword rankings + full-width project layout",
    highlights: [
      "Tracked Keywords now pulls live data from Google Search Console — top queries, average position, and 28-day position change.",
      "Project pages are full-width: Client Brief (Do's, Don'ts, Notes) on the left, Client Files on the right.",
      "Shared clients show a 'Synced across SEO & ADS' note on the brief so it's clear edits land in both departments.",
    ],
  },
  {
    version: 31,
    date: "2026-05-14",
    title: "Client Files + White Clinic in ADS DPT",
    highlights: [
      "New Client Files panel on every project page — upload images/videos or paste Google Drive links. Stays in sync across the SEO & ADS departments, just like the brief.",
      "White Clinic added to the ADS DPT (Germano C., Google + Meta).",
      "Shared clients now show a 'SEO & ADS Client' badge on their project page instead of a single-department tag.",
      "B-Life logo chip moved to a white background.",
      "Removed the redundant 'Not set' tag from the Client Brief header.",
    ],
  },
  {
    version: 30,
    date: "2026-05-14",
    title: "Homepage department order",
    highlights: [
      "Swapped WEB and ADS on the homepage grid — order is now SEO · ADS · WEB · COMMERCIAL.",
    ],
  },
  {
    version: 29,
    date: "2026-05-14",
    title: "Logo chip fixes — Clínica em Casa SVG + exact CDT orange",
    highlights: [
      "Clínica em Casa: swapped the framed apple-touch-icon for the clean SVG icon-logo — no more weird inner border on the white chip.",
      "CDT + IHN chip orange corrected to #F6A800 — the exact orange sampled from CDT's own logo asset.",
    ],
  },
  {
    version: 28,
    date: "2026-05-14",
    title: "Per-client logo chip — new colours, tighter padding, better sources",
    highlights: [
      "B-Life: swapped to the official green 'B♥LIFE' logo (no 'Care'). 900px wide, tight padding so it fills the chip.",
      "Clínica Mimus: re-fetched the 2025 logo — proper mark now visible.",
      "White Clinic and Clínica em Casa: tight padding so the logo fills the chip instead of sitting in a sea of whitespace.",
      "IHN + CDT: chip background is now CDT-brand orange (#F9B600).",
      "WonderAds: chip background is now dark (#10131a) so the butterfly pops.",
      "Senior Resort: chip background is now army green (#4B5320).",
      "LogoBgMode now accepts a custom hex colour; LogoSizing supports 'tight' for clients with sparse logos.",
    ],
  },
  {
    version: 27,
    date: "2026-05-14",
    title: "Logo chip contrast fix for B-Life and Monte Mar",
    highlights: [
      "B-Life and Monte Mar logos are white-on-transparent — invisible on the white chip in v26. Both now render on a dark near-black chip so the white marks are visible.",
      "New LOGO_BG_OVERRIDES map + getLogoBgMode helper — easy to flip any other client to a dark chip if needed.",
      "Brand-coloured glow behind each chip still shows the per-client palette.",
    ],
  },
  {
    version: 26,
    date: "2026-05-14",
    title: "Real brand logos on every client card and project page",
    highlights: [
      "Swapped the Notion emoji icons for each client's actual brand logo. Logos pulled directly from each client's website (apple-touch-icon → og:image → nav <img> in that order) and saved into /public/logos.",
      "All 16 SEO clients now show their real mark: Monte Mar got the proper SVG, Clínica Mimus, IHN, White Clinic, etc. all show their brand identity.",
      "New LogoChip component: white background + per-client brand-gradient glow behind. Contrast is universal — any logo's colours show cleanly.",
      "Cards on /seo, /ads, plus the icon chip on every /seo/[slug] and /ads/[slug] page all use it.",
      "Clínica Empatia (no website yet) keeps the 💗 emoji on a gradient chip until you provide a logo.",
    ],
  },
  {
    version: 25,
    date: "2026-05-14",
    title: "Website URL on project pages + 3-pane SEO project workspace",
    highlights: [
      "Every SEO and ADS client page now shows the client's website as a clickable chip next to the 'SEO/ADS DPT · CLIENT' tag.",
      "New three-column section under the Client Brief on every SEO project page:",
      "Left — Quick Actions: Write SEO Blog · Meta Title & Description · Keyword Research · On-Page Audit · Backlink Outreach · Schema Markup. Disabled until the per-project Claude chat lands.",
      "Middle — GA4 Metrics placeholder: 4 metric tiles + a ghost sparkline. Marked 'Not connected' until the GA4 Data API is wired.",
      "Right — Tracked Keywords placeholder: 5 ghost rows showing rank + trend. Marked 'Not connected' until DataForSEO lands.",
    ],
  },
  {
    version: 24,
    date: "2026-05-14",
    title: "ADS DPT: Core tier override + channel tags",
    highlights: [
      "All ADS DPT clients now show as 'Core' tier on the listing — per-department override so InSync Design stays 'Lite' on SEO.",
      "Each ADS card gets two channel tags: 'Google Ads' (multicolor gradient) and 'Meta Ads' (Meta-blue gradient).",
      "Per-client channel array — easy to drop one for a future client without code surgery.",
    ],
  },
  {
    version: 23,
    date: "2026-05-14",
    title: "Clean client pages — drop Notion content list & ADS placeholder",
    highlights: [
      "Removed the auto-rendered Notion sub-pages list from every /seo/[slug] page (Reports, Backlinks, etc. — those Notion-tagged rows).",
      "Removed the 'Coming soon' placeholder from every /ads/[slug] page.",
      "Each project page is now: header + Client Brief, with empty space ready for custom containers next.",
      "'Open in Notion' link on SEO pages kept for quick jumps back to the source page.",
    ],
  },
  {
    version: 22,
    date: "2026-05-14",
    title: "Fix stale brief render + ADS DPT header polish",
    highlights: [
      "Fix: edits to Do's/Don'ts now show up on refresh and across tabs immediately. The ClientBrief client component fetches the live KV record on mount, so the static-cached server render can't show stale data.",
      "ADS DPT: removed the gradient megaphone icon square in the header.",
      "ADS DPT tagline updated to: 'Strategy, propose creative plans, briefs and activate campaigns.'",
    ],
  },
  {
    version: 21,
    date: "2026-05-14",
    title: "ADS DPT polish + real-time brief sync",
    highlights: [
      "World map on the ADS DPT header — Canada · Portugal · Australia highlighted.",
      "Typewriter prompt 'What are we working on today, boss?' under the count badge.",
      "All 3 ADS clients now attributed to Germano C. (per-department consultant; the same client keeps its SEO-side consultant on /seo).",
      "Real-time brief sync across tabs via BroadcastChannel — add a Do on /seo/ihn and it appears instantly on /ads/ihn.",
      "30s polling backstop covers cross-device sync without burning the Upstash free tier.",
    ],
  },
  {
    version: 20,
    date: "2026-05-13",
    title: "Editable Client Brief, synced across departments",
    highlights: [
      "Do's, Don'ts, and Notes are now editable in the app — '+ Add' button, click any item to edit inline, hover to delete.",
      "Persistence backed by Vercel KV (Upstash Redis). Optimistic UI snaps instantly while the server confirms in the background.",
      "Shared clients (IHN, InSync Design) sync across /seo and /ads automatically — edits to either side update the same record.",
      "Server-side fallback: when the KV record is empty, the static defaults from client-briefs.ts still render.",
    ],
  },
  {
    version: 19,
    date: "2026-05-13",
    title: "Client page header — icon and title side-by-side",
    highlights: [
      "On every SEO and ADS client page, the gradient icon square now sits to the left of the badge + title (was stacked vertically).",
      "On SEO pages, the 'Open in Notion' link moves to the top-right of the header so the icon + title group can sit together.",
    ],
  },
  {
    version: 18,
    date: "2026-05-13",
    title: "ADS DPT clients + cross-department brief sync",
    highlights: [
      "Replaced the ADS DPT 'Channels' list with a Clients section: IHN, InSync Design, Clínica Empatia.",
      "Each ADS client now has its own /ads/[slug] page (mirrors /seo/[slug]).",
      "Briefs are shared by slug — IHN and InSync Design show the same Do's/Don'ts on both the SEO and ADS pages. Edit once, both update.",
      "Added Clínica Empatia (ADS-only) with a thematic peach→rose→magenta palette and 💗 icon.",
    ],
  },
  {
    version: 17,
    date: "2026-05-13",
    title: "Client Brief on every SEO project page",
    highlights: [
      "New Do's and Don'ts panels on every /seo/[slug] page — emerald for Do's, rose for Don'ts.",
      "Notes panel underneath when there are general instructions for the client.",
      "Briefs live in src/lib/client-briefs.ts — easy to edit and version-controlled.",
      "Single source of truth that v18 (per-project chat) and ADS DPT will both read from.",
    ],
  },
  {
    version: 16,
    date: "2026-05-13",
    title: "Typewriter prompt copy fix",
    highlights: [
      "Fixed missing 'we' in the typewriter prompt: now reads 'What are we working on today, boss?'.",
    ],
  },
  {
    version: 15,
    date: "2026-05-13",
    title: "Typewriter prompt on SEO DPT",
    highlights: [
      "Added a typewriter-animated prompt under the 16-clients badge that types out 'What are working on today, boss?' on every page load.",
      "Brand-gradient caret continues blinking after the text finishes typing.",
      "Respects prefers-reduced-motion (no caret blink for users who opted out).",
    ],
  },
  {
    version: 14,
    date: "2026-05-13",
    title: "Public changelog",
    highlights: [
      "Footer version is now a clickable link.",
      "New /changelog page with a timeline of every workspace release.",
      "The latest entry pulses; older entries fade in on a staggered timeline.",
    ],
  },
  {
    version: 13,
    date: "2026-05-13",
    title: "SEO DPT cleanup — 3 columns, no gradient effects, map fix",
    highlights: [
      "Dropped client C. Saccor (offboarded). André's column gone too.",
      "Switched to 3 consultant columns: Fran. R. · Yenisey · Luana N.",
      "Cards inside each column sort top → bottom by tier: Growth → Core → Lite.",
      "Removed gradient text effects from SEO DPT title and the count badge.",
      "Cropped Antarctica + colored bleed from the world map.",
    ],
  },
  {
    version: 12,
    date: "2026-05-13",
    title: "Remove homepage shooting stars",
    highlights: [
      "Pulled the meteors background effect off the department-selector page.",
      "Background goes back to soft brand gradient halos only.",
    ],
  },
  {
    version: 11,
    date: "2026-05-13",
    title: "Title polish, tier badges, 4 consultant columns",
    highlights: [
      "Dropped the shimmer animation on the SEO DPT title; scaled it ~10% smaller.",
      "Equalised the count-badge sizes — '17' and 'clients' now match.",
      "Renamed Institute of Holistic Nutrition → IHN, Corrida do Tempo → CDT.",
      "Added Lite · Core · Growth tier badges on every client card.",
      "Added consultant Luana N. (took over Aeger Prima, A. Domingos, Senior Resort, Safe Away, Clínica em Casa).",
    ],
  },
  {
    version: 10,
    date: "2026-05-12",
    title: "Real per-client brand colours",
    highlights: [
      "Extracted brand colours from each of the 17 client websites.",
      "Applied per-client gradients to the card icon, glow, and bottom accent.",
      "Where the site exposed too few colours, a thematic complement was chosen.",
    ],
  },
  {
    version: 9,
    date: "2026-05-12",
    title: "Layout polish + consultant overrides",
    highlights: [
      "Renamed Mimus Clínica Dentária → Clínica Mimus.",
      "Added per-client consultant overrides (Fran. R. and Yenisey).",
      "Moved the KPIs card to a full-width row below the clients grid.",
      "Tightened KPIs copy (no more duplicated 'framework').",
      "Made the SEO DPT title bigger with a rich 5-stop gradient.",
    ],
  },
  {
    version: 8,
    date: "2026-05-12",
    title: "Per-client palettes, world map, shooting stars",
    highlights: [
      "Thematic gradient palette per client (placeholders until real colours).",
      "Added a world map next to the SEO DPT header highlighting client countries.",
      "Shooting-star meteors on the homepage background.",
      "Animated count badge '17 clients' next to the title.",
      "Head Consultant André on every card.",
    ],
  },
  {
    version: 7,
    date: "2026-05-12",
    title: "Live Notion integration",
    highlights: [
      "Wired /seo to live Notion data — clients now load from the SEO Space.",
      "Each card opens a dynamic /seo/[slug] page rendering that client's Notion content.",
      "ISR re-fetches from Notion every 60s — Notion edits go live within a minute.",
    ],
  },
  {
    version: 6,
    date: "2026-05-12",
    title: "Tagline, layout, KPIs translation",
    highlights: [
      "New tagline: 'Organic growth in Google and AIs. #1 SEO & GEO Agency in Portugal.'",
      "KPIs hero moves below the project grid on /seo.",
      "Translated the SEO KPIs framework page from Portuguese to English.",
    ],
  },
  {
    version: 5,
    date: "2026-05-12",
    title: "Switch SEO Claude to direct Anthropic billing",
    highlights: [
      "Replaced the Vercel AI Gateway provider with @ai-sdk/anthropic.",
      "Chat now bills directly to your Anthropic account.",
      "Default model: claude-sonnet-4-6.",
    ],
  },
  {
    version: 4,
    date: "2026-05-12",
    title: "SEO Claude chat + project grids + KPIs page",
    highlights: [
      "SEO Claude streaming chat panel on /seo (Vercel AI Gateway).",
      "Project grids on /seo (16 cards), /ads (6), /web (6).",
      "SEO DPT KPIs hero card linking to /seo/kpis.",
      "New /seo/kpis page rendering the performance framework.",
    ],
  },
  {
    version: 3,
    date: "2026-05-12",
    title: "Footer copy fix",
    highlights: ["'All Copy Rights Reserved' → 'All Rights Reserved'."],
  },
  {
    version: 2,
    date: "2026-05-12",
    title: "Hero copy refinements",
    highlights: [
      "Capitalised the word 'Department' in the hero heading.",
      "Removed the descriptive paragraph below the heading.",
      "Updated footer copyright text.",
      "Tightened section margins (~5% less scroll).",
    ],
  },
  {
    version: 1,
    date: "2026-05-12",
    title: "Initial workspace",
    highlights: [
      "Next.js 15 + TypeScript + Tailwind v4 + App Router scaffold.",
      "Dark theme with hub-and-spoke layout and the Wonder Ads brand gradient.",
      "Four department cards: SEO · WEB · ADS · COMMERCIAL.",
      "Deployed on Vercel Hobby at wonder-ads-workspace.vercel.app.",
    ],
  },
];

export function getCurrentVersion(): number | string {
  return CHANGELOG[0]?.version ?? 1;
}
